diff --git a/README.md b/README.md
index d4ef585..f113bbd 100644
--- a/README.md
+++ b/README.md
@@ -1,4 +1,4 @@
-# 八字 × 紫微 × 太乙 合盤工具 v0.9.29
+# 八字 × 紫微 × 太乙 合盤工具 v0.9.36
 
 一份工具、一次輸入，同時驅動八字、紫微與太乙排盤，並將三法的人生曲線疊在同一條西元年軸上比對。
 
@@ -15,6 +15,129 @@
 
 部署：整個資料夾放上 GitHub Pages 即可，入口為 `index.html`。三個子頁仍可單獨開啟使用，原功能完全保留。
 
+## v0.9.36 財運訊號改為推象口徑（八字體質 × 紫微大限環境 × 四化）
+
+### 改動
+- 財運卡主區改為 **推象財路**（`wuxing-mainline.js` → `wealthSignals`，schema `xiaoliu.wealth-tuixiang/v1`，不參與計分）。財＝日主所剋之五行，訊號由七層推出，不再以「某顆星化祿」為準：
+  - **大限層**：主星為財帶大限祿 +3／權 +2／無化 +1／忌 −2；官殺帶祿（被管得財）+2；食傷帶祿（作品變現）+2；宮支為財（非忌）+1；對宮為財（財在外）+1；題目在財帛／田宅 +1；主星比劫 −1、主星化忌 −1。強≥3、中 2、弱 1，≤0 不列；附年齡段貨幣與空宮／對宮標籤。
+  - **流年層**：流年干為財 +1（喜再 +1）、支為財 +1；流年祿落大限命宮主星 +2（主星為財再 +1）、權落財星 +1、忌落主星 −2；大限財路成立（中以上）+1。門檻 2 分。
+- 舊口徑（八字喜用透干＋祿神、紫微武曲／太陰／天府化祿、雙法共振）收進摺疊區供對照，存檔 `wealthResonance` 保留；新增 `resonance.wealthTuixiang`。
+- 推象七層卡的配對統計標籤同步改為新線名。
+
+### 首次對帳（1976-10-14 亥時女）
+- 大限：1998–2007 太陰財化祿＋對宮天同財 → **強**；2018–2027 天機官殺化祿＋財帛 → **強**；2028–2037 破軍財化權＋對宮天相財 → **強**；2008–2017 紫府比劫 → 不列（產出多入袋少）；2038–2047 只有對宮巨門財 → 弱（出口在外）。與命主實史四段同向。
+- 流年：2025 天機化祿入大限（強）、2032 壬子干支皆財（強）、2033 癸丑破軍化祿入大限主星（5 分，最高）——2032／2033 與命主既有的「2032 財年」標記一致。
+
+### v0.9.36.1 排版修正
+推象財路列改用專屬版型（徽章＋期間一行、理由另行可換行），不再套 `research-ui.css` 的 `.anchor-line` 兩欄 grid（三個子元素會被擠成直排）；舊口徑列的第三欄改為整列換行。
+
+### 驗證
+test-parent（新增五條推象財路斷言，另將 v0.9.28 可信度／可靠度／PDF 高低點等殘留的三線斷言全數改為 `pairs.hebing`）、test-children 全過。
+
+## v0.9.35 所有太乙比對改為「太乙 × 八字＋紫微」；新增八字＋紫微流年曲線
+
+### 改動
+- **八字＋紫微流年曲線**（`wuxing-mainline.js` → `scoreAnnual`，id `wuxing-year`）：逐年分＝所在大限環境分（宮支＋主星＋大限四化）＋大運層（該年 luckGz 對環境助拆）＋**流年層**（流年干支對「宮支＋主星」助拆指數 × 15 × 環境正負）＋流年干四化落大限命宮主星（祿+10／權+7／科+3／忌−10，中州派十干表）。流年干支取八字 `annual[].gz`。
+- **中性環境的正負**：環境分為 0 時退而取主星關係（身強以食傷／財／官殺為正、印／比劫為負，身弱反之），再退宮支喜忌——修正 v0.9.31 「環境 0 分即整段歸零」的缺口（Nakai 2028–2037）。
+- **核心比對線改為兩線**：`CORE_IDS`＝{大限：wuxing-line × taiyi-xianli；流年：wuxing-year × taiyi-xingnian}。以下全部只比「太乙 × 八字＋紫微」，**移除太乙 × 八字、太乙 × 紫微的個別比對**：
+  - 太乙錨定可信區段（大限斜率／位置補判、流年未來位置）：單一佐證線分級——A＝佐證分 ≥70 且反向 <35；C＝佐證分 ≥35；衝突＝反向 ≥35 且佐證 <35。**B 級不再出現**（僅讀舊檔時顯示「B 雙法支持（舊檔）」）。八字可靠度係數沿用於此線。
+  - 單盤轉折與領先／落後：每層只剩一組「太乙 × 八字＋紫微」，`comparison:'hebing'`，快照 `pairs.hebing`。
+  - 太乙流年高低點 ±1 年呼應：只查八字＋紫微流年曲線（快照版本 `annual-extrema-echo/v6-hebing`，欄位 `hebing`）。
+  - 曲線共振卡：共高帶／共同功課帶只看核心兩線；畫面只顯示核心配對，其餘配對仍寫入存檔 `resonance.pairs` 供回查。
+  - PDF 報告同步改版（錨定、轉折、高低點頁）。
+- 存檔 `resonance.syncAlerts.version` 升 `taiyi-anchored-zones/v2-hebing`，加 `companion` 欄位。
+- 「顯示曲線」的「八字＋紫微」同時控制大限與流年兩條線。
+
+### 首次對帳（1976-10-14 亥時女，僅供觀察，非結論）
+- 大限：八字＋紫微 × 限例太乙 同向率 59%、相關 −0.55；錨定：上行 A 1988–1993（求學段，實史 16 歲住校）、下行 A 1994–2008、衝突 2009–2012／2013–2017、C 2018–2037（可信度 0）、C 2044–2052。轉折貼合度 31。
+- 流年：同向率 54%、相關 −0.22；2026–2048 整段標「衝突區」（太乙高、八字＋紫微低）。
+- 這條線與太乙相關為負，且 2026–2048 直接反向——如 v0.9.31 所記，這是內容分歧（象層說火土加重／土剋水靠出手，太乙說天下潮汐高），不是計算錯；技能 v1.3 起明訂「象定形狀、分數定尺度，不以貼合太乙為目標」。
+
+### 驗證
+`node test-parent.js`、`node test-children.js` 全過（三線語意的舊斷言全數改寫為兩線：合成序列數、配對數、A／C／衝突門檻、單一佐證線快照欄位、流年四條線）；`test-research-ui.js` 除沙箱本就失敗的太乙月表段外全過（`.metric-grid` 4→2、PDF 每頁 4 條線）。
+
+## v0.9.34 顯示曲線勾選：新增「八字＋紫微」，預設只開它與太乙
+
+- 「顯示曲線」多一項 **八字＋紫微**（＝五行主線曲線，青色虛線，`methodOf('wuxing-line')='hebing'`）。
+- **預設**：八字＋紫微 ✔、太乙 ✔、八字 ✘、紫微 ✘（`VIEW_DEFAULT_HIDDEN={bazi,ziwei}`）。localStorage 鍵升 `xiaoliu_hebing_research_ui_v2`，舊使用者第一次載入即套新預設；四項全關時回到預設。
+- 流年層沒有「八字＋紫微」線（曲線僅大限層），該層預設只剩太乙一條——要看八字流年／紫微逐年請勾回。
+- 分析（共振、錨定、轉折、財運）仍以完整資料計算，不受勾選影響；PDF 報告匯出時一律畫全部曲線（大限層 4 條、流年層 3 條）。
+- `wuxing-mainline.js` 改為 `onload` 補畫：模組若晚於排盤完成才載入，自動重繪一次（修正 jsdom 測試時序）。
+- 測試：test-parent（新增預設勾選／兩條線／全開四條／6 組配對斷言）、test-children 全過；test-research-ui 已改為新預設，惟該檔既有的太乙月表斷言（`bailiu-liuyue-info` 12 列）在沙箱環境本就失敗（Chart.js CDN 不可達，與本版無關），跳過該段後其餘全過。
+
+## v0.9.33 年齡段貨幣換算
+
+- 推象七層卡每一大限的「象」下方加一行**年齡段**（依大限中點虛歲取段）：童年 1–／求學 13–／初入職場 23–／成家立業 33–／中年主事 43–／盛年轉向 53–／退休初期 63–／晚年 73–。每段給四個貨幣：財（我剋）、管（剋我）、眾（比劫）、出口——例：求學段「被管得財」＝被學校與家規管、領零用錢與學業成果；初入職場段同一個象＝被主管管、領薪水。形狀不變，貨幣隨年齡換。
+- 表在 `wuxing-mainline.js` `AGE_STAGES`，逐段可改；`buildMainline()` 回傳加 `ageStage`／`verdict.ageNote`。詳情框同步顯示。
+- 驗證：test-parent 新增年齡段斷言，全過。回測件：Nakai 1988–1997 求學段 → 16 歲外縣市住校（被管、出口在外）。
+
+## v0.9.32 推象七層卡（原「五行主線」卡改版）
+
+### 改動
+- 母頁卡片改名「推象七層」，每一大限一個區塊，依序列出：①體質（卡頭）②宮支生剋＋喜忌＋**日主在該支的十二長生位**（`stageOf`：陽干順、陰干逆；己土：酉長生、午臨官（祿）、巳帝旺）③主星（空宮借對宮標示）④大限四化 ⑤**大運兩面**（大運干支各自對日主的十神＋喜忌、食傷標「洩口」；對環境的助拆指數 m，逐年以 `luckGz` 對齊）⑥**對宮主星**與五行、對日主關係 ⑦宮名題目 ⇒ 象（判詞）。
+- **空宮借對宮規則**自動標籤：借到喜神／成財／成洩口 → 「機會在外（強制）」；借到忌神 → 「守本位・不外求」；本宮有主星、對宮供喜神且無忌神 → 「出口在外（宜）」。
+- 詳情框（點年）帶出：主線、十二長生位、標籤、象、題目。
+- `wuxing-mainline.js` `buildMainline()` 回傳新增 `stage`／`lucks[]`／`oppStars[]`／`oppZhi`／`oppPalace`／`oppHelpful`／`tag`／`topic`；舊欄位不變。曲線計分（v0.9.31）未動。
+
+### 驗證
+- test-parent／test-children 全過；新增：巳帝旺／午臨官（祿）、辛卯運辛金洩口、出口在外標籤、題目層。
+- 1976-10-14 亥時女 2038–2047：巳（帝旺）× 太陽火 × 辛卯運（辛金洩口、卯木助火環境）× 對宮巨門水 → 出口在外（宜）——與技能 v1.3 回測表一致。
+
+### 與技能對應
+hebing-sanpan-interpreter v1.3 第三章之二「推象七層」直接引用本卡前六層；第 7 層題目與流年補讀由技能寫進命書。曲線一致率為附錄假說，不作目標。
+
+## v0.9.31 五行主線曲線（假說級第四線，與太乙比對）
+
+### 目的
+把 v0.9.30 的五行主線（判詞＝形狀）加上「大運對大限環境的生剋」，算成一條可畫、可與太乙限例做兩兩統計的曲線。**這是可被否定的假說**：若它與太乙的方向一致率沒有高過紫微大限（Nakai 盤 89%），就只是多一條線。
+
+### 計分（`wuxing-mainline.js` → `scoreMainline`；權重 `W` 皆為草案）
+- 環境層：宮支喜忌 ±10、主星五行喜忌 ±15（每顆、借對宮 ×0.5、取平均）、大限四化落主星 祿+15／權+10／科+5／忌−15。
+- 大運層（B 案：大運對環境的生剋，非大運對日主——避免與八字大運分重算同一份資料）：大運干（×1）支（×2）對「宮支＋主星」——生環境 +1、同 +0.5、被環境洩 −0.5、剋環境 −1、被環境剋 −0.25 → 助拆指數 m；大運分＝m × 20 × sign(環境分)。逐年以 `annual[].luckGz` 對齊（大限與大運起訖不同步時按年加權），超出八字逐年範圍者大運層為 0。
+- 已知缺口：環境分為 0 時大運層無方向（Nakai 2028–2037 即此例）。
+
+### 顯示
+- 大限合盤圖多一條青色虛線「五行主線曲線」；`aux:true`——**不參與共振帶（共高／共同功課帶）**，只進兩兩統計；存檔 `resonance.pairs` 自動含它與各線的配對。
+- 五行主線卡新增「曲線分」欄（環境分／大運分／逐項），卡底列出與各線的方向一致率與相關度。
+- 詳情框（點年）帶出該大限曲線分。
+
+### 首次對帳（1976-10-14 亥時女）
+- 曲線高低序：2018–2027（+25.8）＞1998–2007（+22.5）＞2008–2017（−4.4）——與命主實史（被管得財＞穩定給＞產出多入袋少）同序。
+- 與太乙限例：方向一致 72%、相關 −0.39（紫微大限 89%／0.07，八字大運 53%／0.08）。**未高過紫微，且相關為負**——主因 2038–2047 巳宮太陽（火宮火星，對己土雙忌）判為 −36.7，而太乙在 2037–2049 拉高帶。此分歧本身是待驗證的內容，不是 bug。
+- 與八字大運相關 −0.57：符合設計（大運層讀的是大運對環境，與大運對日主部分反向）。
+
+### 驗證
+`node test-parent.js`、`node test-children.js` 全過（序列數 4→5、配對 6→10 已更新；新增五行主線曲線四條斷言）。
+
+## v0.9.30 五行主線（八字體質 × 紫微大限環境）
+
+### 方法（Nakai 2026-09-08「五行基底法」，三段大限回測三中）
+八字命局是**自身的五行結構**，紫微十二宮是**十二種環境的五行供給**。每一大限以日主為基底相乘：
+- **宮支五行 → 對日主的生剋 → 產出形態**（做什麼：金洩土＝做作品、木剋土＝被框架管…）
+- **主星五行 → 對日主的生剋 → 分配結果**（成果歸誰：水＝財、土＝比劫共分、木＝官殺定價…）
+- **大限四化落在主星 → 方向**（祿給／權自主／科名／忌收）
+- 四化定形狀不定尺度；尺度由分數層（八字大運分、紫微能量分）與當期現實決定。
+
+口徑固定：宮支＋主星（不取宮干、納音）；雙五行主星以主五行判讀、次五行括號併列；空宮借對宮主星並標「借對宮」。
+主星五行採 Nakai 裁定：廉貞木火、貪狼水木、巨門水、七殺金（主／次順序見 `STAR_ELEMENT`，對調只改表）。
+
+### 改動
+- `ziwei-engine.js` 與 `ziwei.html` 內嵌副本：新增 `ZHI_ELEMENT`／`GAN_ELEMENT`／`STAR_ELEMENT`；`palaces[].stars[]` 帶 `element`／`element2`；`daList[]` 帶 `zhiElement`／`stars[]`（含 `natalHua`／`daHua`／`borrowed`）／`frame`／`frameStars[]`。
+- `ziwei.html` 橋接 payload：`daen[]` 加 `zhi`／`zhiElement`／`gan`／`hua`／`huaText`／`stars`／`frameStars`；新增頂層 `palaces[]`。
+- `bazi.html` 橋接 payload：新增 `dayMaster {gan, element, yinYang}`。
+- 新檔 `wuxing-mainline.js`：純函式模組（瀏覽器／node 皆可），`buildMainline(bazi, ziwei)` 產出每一大限的主線與判詞；判詞表 `VERDICT` 是口徑草稿，逐格可改、不動邏輯。已回測格（官殺＋祿、比劫＋無、財＋祿、財＋權）標 ✓。
+- `index.html`：財運卡下方新增「五行主線」卡（一列一大限）；點曲線任一年，詳情框帶出該大限主線；**不參與任何曲線計分**。
+- 存檔契約升 **`xiaoliu.hebing-save/v3`**（v1／v2 唯讀相容；讀檔一律引擎重算）。舊存檔讀入後主線卡會提示重新排盤。
+
+### 驗證
+- `node test-children.js`、`node test-parent.js` 全數通過（新增 8 條 v3 斷言：欄位齊備、未宮天機祿、午宮破軍權、主線卡十二列、判詞含已回測格）。
+- 1976-10-14 亥時女四段回測：酉宮太陰祿→「穩定給」、申宮紫府→「產出多入袋少」、未宮天機祿→「被管得財」、午宮破軍權→「自己主導財」。
+
+### 待裁定 / pending
+- 廉貞、貪狼主次五行順序（現主火／主木）。
+- `VERDICT` 判詞 25 格中未回測者為草稿，待實例逐格校訂。
+- hebing-sanpan-interpreter 技能第三章改吃 v3 新欄位（技能端）。
+
 ## v0.9.29 曆書延長至 2100（節氣表同源化）
 
 ### 問題
diff --git a/bazi.html b/bazi.html
index 0baaa40..7429bfb 100644
--- a/bazi.html
+++ b/bazi.html
@@ -4846,6 +4846,9 @@ function downloadJson() {
         specialPatternDecision: chart.strength.details&&chart.strength.details.specialPatternDecision ? JSON.parse(JSON.stringify(chart.strength.details.specialPatternDecision)) : null
       } : null,
       wealthSignals: baziWealthSignals(chart, cycle, ys, annual),
+      /* v0.9.30：日主明列（五行主線用；v2 只能繞 wealthSignals.luShen.dayStem 取） */
+      dayMaster: (function(){ var g=chart.pillars.dayPillar.charAt(0); var gi='甲乙丙丁戊己庚辛壬癸'.indexOf(g);
+                   return { gan:g, element:BaziEngine.getElementOf(g), yinYang: gi%2===0?'陽':'陰' }; })(),
       startAge: cycle.startAge,
       curGz: curGz,
       pillars: cycle.pillars.map(function(pl){
diff --git a/index.html b/index.html
index 4037caa..2709559 100644
--- a/index.html
+++ b/index.html
@@ -67,6 +67,9 @@ iframe{width:100%; height:1400px; border:1px solid var(--line); border-radius:0
 .mode-row select{font-family:inherit; font-size:.85rem; border:1px solid var(--line); border-radius:6px; padding:3px 6px; background:#fff; color:var(--ink);}
 .legend{display:flex; flex-wrap:wrap; gap:14px; font-size:.8rem; color:var(--muted); margin:8px 0 4px;}
 .legend i{display:inline-block; vertical-align:middle; margin-right:5px;}
+/* v0.9.36.2：圖例每一條各佔一行；lgExt 內的多條擴充圖例展平成獨立項目 */
+.legend>span{flex:0 0 100%;}
+#lgExt{display:contents;}
 .lg-bazi{width:22px; height:0; border-top:3px solid var(--gold);}
 .lg-ziwei{width:22px; height:0; border-top:2px solid var(--ziwei);}
 .lg-da{width:14px; height:0; border-top:1px dashed var(--gold);}
@@ -100,6 +103,25 @@ iframe{width:100%; height:1400px; border:1px solid var(--line); border-radius:0
   background:#fffaf0; padding:12px 14px; display:block;
 }
 .sync-card h3{font-size:.92rem; letter-spacing:.18em; color:var(--moss); margin-bottom:8px;}
+/* v0.9.30 五行主線表 */
+.wx-table{width:100%; border-collapse:collapse; font-size:.78rem; line-height:1.6;}
+.wx-block{border-bottom:1px solid rgba(0,0,0,.1); padding:8px 4px 10px;}
+.wx-block.wx-cur{background:rgba(95,143,78,.07);}
+.wx-head{display:flex; flex-wrap:wrap; align-items:baseline; gap:4px 8px; margin-bottom:4px;}
+.wx-score{margin-left:auto; color:#2f7f8f; white-space:nowrap;}
+.wx-layers{margin:0 0 4px 1.2em; padding:0;}
+.wx-layers li{margin:1px 0;}
+.wx-tag-go{color:#c0392b;} .wx-tag-out{color:#3f7d36;} .wx-tag-stay{color:#b9ad96;}
+.wx-table th,.wx-table td{border-bottom:1px solid rgba(0,0,0,.08); padding:6px 6px; vertical-align:top; text-align:left;}
+.wx-table th{font-weight:600; color:var(--moss); letter-spacing:.06em; white-space:nowrap;}
+.wx-table td.wx-yr{white-space:nowrap; color:var(--muted);}
+.wx-table tr.wx-cur td{background:rgba(95,143,78,.07);}
+.wx-line{font-family:var(--font-serif);}
+.wx-verdict{color:#1a1411;}
+.wx-verdict.wx-ok::before{content:'✓ '; color:#3f7d36;}
+.wx-hua{font-size:.72rem; color:var(--muted);}
+.wx-tag{display:inline-block; font-size:.66rem; padding:0 5px; border:1px solid currentColor; border-radius:3px; margin-left:4px;}
+.wx-fav-喜{color:#3f7d36;} .wx-fav-忌{color:#c0392b;} .wx-fav-中{color:#b9ad96;}
 .sync-intro{font-size:.76rem; color:var(--muted); line-height:1.7; margin-bottom:10px;}
 .sync-mode{border-top:1px dashed var(--line); padding-top:9px; margin-top:9px;}
 .sync-mode:first-of-type{border-top:none; padding-top:0; margin-top:0;}
@@ -119,6 +141,12 @@ iframe{width:100%; height:1400px; border:1px solid var(--line); border-radius:0
 .anchor-grade.c{color:#765d2d; background:#efe0b7;}
 .anchor-grade.conflict{color:#fff; background:#9b5b4f;}
 .anchor-score{font-size:.72rem; color:var(--muted);}
+/* v0.9.36.1 推象財路列：徽章＋期間一行，理由另起一行可換行（不用 anchor-line 的兩欄 grid） */
+.wt-row{display:block;}
+.wt-head{display:flex; flex-wrap:wrap; align-items:center; gap:6px 8px;}
+.wt-head .wt-period{white-space:normal;}
+.wt-why{display:block; font-size:.74rem; color:#8a8071; line-height:1.6; margin-top:2px; white-space:normal;}
+.wt-why b{display:inline; margin:0; color:#3f7d36;}
 .anchor-legend{display:flex; flex-wrap:wrap; gap:6px; margin:0 0 9px; font-size:.72rem; color:var(--muted);}
 .anchor-legend span{border:1px solid var(--line); border-radius:999px; padding:1px 7px; background:rgba(255,255,255,.6);}
 .sync-remind{font-size:.75rem; color:var(--muted); margin-top:5px;}
@@ -280,8 +308,9 @@ iframe{width:100%; height:1400px; border:1px solid var(--line); border-radius:0
           <button type="button" id="applyRange">套用</button>
         </div>
         <div class="toolbar-group method-switches"><span class="toolbar-label">顯示曲線</span>
-          <label class="method-bazi"><input type="checkbox" data-method="bazi" checked>八字</label>
-          <label class="method-ziwei"><input type="checkbox" data-method="ziwei" checked>紫微</label>
+          <label class="method-bazi"><input type="checkbox" data-method="bazi">八字</label>
+          <label class="method-ziwei"><input type="checkbox" data-method="ziwei">紫微</label>
+          <label class="method-hebing"><input type="checkbox" data-method="hebing" checked>八字＋紫微</label>
           <label class="method-taiyi"><input type="checkbox" data-method="taiyi" checked>太乙</label>
           <label><input type="checkbox" id="viewBounds" checked>交界／干支</label>
           <label><input type="checkbox" id="viewMarks" checked>轉折標籤</label>
@@ -313,11 +342,17 @@ iframe{width:100%; height:1400px; border:1px solid var(--line); border-radius:0
       </div>
 
       <div class="sync-card" id="wealthCard">
-        <h3>財運訊號</h3>
-        <div class="sync-intro">八字與紫微各自獨立判定的財運訊號，<b>不參與任何曲線計分</b>，只作標示。八字看「喜用神透干＋年干或日干祿神落於該柱地支」；紫微看「武曲／太陰／天府化祿，且落於該層命宮、財帛宮或田宅宮」。兩法年份重疊處標為「雙法財運共振」——共振代表兩套系統在同一段時間各自指向財，仍非吉凶宣判，也不保證財富結果。</div>
+        <h3>財運訊號（八字體質 × 紫微大限環境 × 四化）</h3>
+        <div class="sync-intro">以推象七層的口徑推財路，<b>不參與任何曲線計分</b>，只作標示。財＝日主所剋之五行。<b>大限層</b>：主星為財帶大限祿／權、官殺帶祿（被管得財）、食傷帶祿（作品變現）、宮支為財、對宮為財（財在外）、題目在財帛／田宅；主星比劫或化忌則扣分。<b>流年層</b>：流年干支對日主為財、流年祿落大限命宮主星（主星為財加倍）、流年忌落主星則扣分；大限財路成立者流年加一分。強≥3、中 2、弱 1；財的貨幣依年齡段換算。舊口徑（八字、紫微各自獨立訊號）收在下方摺疊區供對照。</div>
         <div id="wealthGrid"><div class="sync-wait">尚未排盤——八字與紫微完成後，這裡會顯示財運訊號。</div></div>
       </div>
 
+      <div class="sync-card" id="wuxingCard">
+        <h3>推象七層（八字體質 × 紫微大限環境）</h3>
+        <div class="sync-intro">八字命局是自身的五行結構，紫微十二宮是十二種環境的五行供給。每一大限以日主為底走七層：<b>①體質 ②宮支（生剋＋喜忌＋日主十二長生位）③主星（空宮借對宮）④大限四化 ⑤大運兩面（對日主的洩口／管束、對環境的助拆）⑥對宮出口 ⑦宮名題目</b> ⇒ 這十年的<b>象／機會／出口</b>，再依<b>年齡段</b>換算貨幣（求學段的「財」是零用錢與學業，職場段是薪水，中年是資產與現金流）。空宮借到喜神＝「機會在外（強制）」；本宮有象、對宮供喜神＝「出口在外（宜）」。象定形狀、分數定尺度；口徑固定「宮支＋主星」，雙五行以主五行判讀。<b>不參與任何曲線計分</b>，判詞為口徑草稿、可逐格修改。</div>
+        <div id="wuxingGrid"><div class="sync-wait">尚未排盤——八字與紫微完成後，這裡會顯示每一大限的五行主線。</div></div>
+      </div>
+
       <div class="turn-card" id="turnCard">
         <h3>單盤轉折與領先／落後分析</h3>
         <div class="turn-intro">以太乙為主線，分別和八字、紫微比對同方向轉強／轉弱點；同時比較最佳時間差相關度、方向一致率與轉折吻合率，判斷太乙較常領先、同期或落後，以及線型較貼近哪一法。本卡只描述這一張命盤，不能直接推論所有命盤的普遍規律。</div>
@@ -388,7 +423,9 @@ var NS='xl-merge-bridge';
 var $=function(id){ return document.getElementById(id); };
 var STATE={ bazi:null, ziwei:null, ext:[], ready:{bazi:false,ziwei:false,taiyi:false},
             pending:null, sel:null, zmode:'daen' };
-var VIEW={range:'all',from:null,to:null,hidden:{},bounds:true,marks:true,domain:null};
+/* v0.9.34：預設只顯示「八字＋紫微」（五行主線曲線）與太乙；八字、紫微單線預設隱藏 */
+var VIEW_DEFAULT_HIDDEN={bazi:true,ziwei:true};
+var VIEW={range:'all',from:null,to:null,hidden:Object.assign({},VIEW_DEFAULT_HIDDEN),bounds:true,marks:true,domain:null};
 var EXPORT_VIEW=false;
 var PDF_BUSY=false;
 var DEADBAND=0.1; // 正規化後 |v|≤0.1 視為平緩，不計入同向/背離
@@ -538,6 +575,16 @@ function buildSeries(mode){
                    big:(r.clsKey==='peak'||r.clsKey==='valley') };
         })
       });
+      /* v0.9.31 五行主線曲線（假說級）：環境層＋大運對環境生剋，各自正規化；aux=不參與共振帶，只進兩兩統計 */
+      if(B&&window.WuxingMainline&&Z.daen.some(function(d){return d.stars;})&&STATE.showWuxingLine!==false){
+        try{
+          var wsc=WuxingMainline.scoreMainline(B,Z);
+          var wMax=10; wsc.forEach(function(r){ if(Math.abs(r.score)>wMax) wMax=Math.abs(r.score); });
+          var wpts=wsc.map(function(r){ return { x:(r.y0+r.y1+1)/2, v:r.score/wMax, raw:r.score, parts:r.parts, x0:r.y0, x1:r.y1+1, row:r }; });
+          list.push({ id:'wuxing-line', label:'八字＋紫微（五行主線）', color:'#2f7f8f', width:1.8, dash:'6 4', core:true,
+                      pts:wpts, boundaries:[], marks:[] });
+        }catch(e){ console.error('wuxing line',e); }
+      }
     }else{
       var maxAbs=6;
       Z.years.forEach(function(y){ if(Math.abs(y.score)>maxAbs) maxAbs=Math.abs(y.score); });
@@ -556,6 +603,16 @@ function buildSeries(mode){
                    mixed:p.yr.label==='mixed' };
         })
       });
+      /* v0.9.35 八字＋紫微流年曲線：大限環境分＋大運層＋流年干支對環境助拆＋流年四化落大限主星 */
+      if(B&&window.WuxingMainline&&Z.daen&&Z.daen.some(function(d){return d.stars;})&&STATE.showWuxingLine!==false){
+        try{
+          var wan=WuxingMainline.scoreAnnual(B,Z);
+          var waMax=10; wan.forEach(function(r){ if(Math.abs(r.score)>waMax) waMax=Math.abs(r.score); });
+          var wapts=wan.map(function(r){ return { x:r.year, v:r.score/waMax, raw:r.score, parts:r.parts, gz:r.gz, row:r }; });
+          if(wapts.length) list.push({ id:'wuxing-year', label:'八字＋紫微（五行主線・流年）', color:'#2f7f8f', width:1.5, dash:'6 4', core:true,
+                                       pts:wapts, boundaries:[], marks:[] });
+        }catch(e){ console.error('wuxing year',e); }
+      }
     }
   }
   // 擴充序列（太乙等）：以序列自身 max|value| 正規化；帶 level 者僅在對應合盤層級顯示
@@ -565,7 +622,7 @@ function buildSeries(mode){
     var mx=1e-9;
     s.points.forEach(function(p){ if(Math.abs(p.value)>mx) mx=Math.abs(p.value); });
     list.push({
-      id:s.id, label:s.label, color:s.color||'#7d5a44', width:2,
+      id:s.id, label:s.label, color:s.color||'#7d5a44', width:2, core:(s.id==='taiyi-xianli'||s.id==='taiyi-xingnian'),
       pts:s.points.map(function(p){ return { x:p.year, v:p.value/mx, raw:p.value }; }),
       boundaries:(s.boundaries||[]).map(function(b){ return { x:b.year, top:b.label, sub:'' }; }),
       specialYears:s.specialYears||null,
@@ -612,11 +669,14 @@ function resonance(list){
   if(years.length<3) return null;
   // 共振帶：全序列同時 > 或 < 平緩帶
   var bands=[], cur=null;
+  /* v0.9.35：共高／共同功課帶只看核心兩線（八字＋紫微、太乙）；其餘線仍進兩兩統計供存檔回查 */
+  var coreIdx=list.map(function(s,i){return s.core?i:-1;}).filter(function(i){return i>=0;});
+  if(coreIdx.length<2) coreIdx=list.map(function(s,i){return i;});
   years.forEach(function(row){
-    var kind=null;
-    if(row.vs.every(function(v){ return v> DEADBAND; })) kind='hi';
-    else if(row.vs.every(function(v){ return v< -DEADBAND; })) kind='lo';
-    var common=kind?Math.min.apply(null,row.vs.map(function(v){return Math.abs(v);})):0;
+    var kind=null, cv=coreIdx.map(function(i){return row.vs[i];});
+    if(cv.every(function(v){ return v> DEADBAND; })) kind='hi';
+    else if(cv.every(function(v){ return v< -DEADBAND; })) kind='lo';
+    var common=kind?Math.min.apply(null,cv.map(function(v){return Math.abs(v);})):0;
     if(kind&&cur&&cur.kind===kind&&row.yr===cur.y1+1){
       cur.y1=row.yr; cur._sum+=common; cur._n++;
     }
@@ -739,7 +799,7 @@ function renderOverlay(){
     if(!EXPORT_VIEW && VIEW.hidden[methodOf(s.id)])return;
     g+='<g clip-path="url(#ovPlotClip)" data-series="'+s.id+'">';
     var poly=s.pts.map(function(p){ return X(p.x).toFixed(1)+','+Y(p.v).toFixed(1); }).join(' ');
-    g+='<polyline points="'+poly+'" fill="none" stroke="'+s.color+'" stroke-width="'+s.width+'" stroke-linejoin="round" stroke-linecap="round" opacity="0.9"/>';
+    g+='<polyline points="'+poly+'" fill="none" stroke="'+s.color+'" stroke-width="'+s.width+'"'+(s.dash?' stroke-dasharray="'+s.dash+'"':'')+' stroke-linejoin="round" stroke-linecap="round" opacity="0.9"/>';
     if(s.id!=='ziwei-year'&&s.id!=='bazi-annual'){ // 逐年序列點太密，不畫底點
       s.pts.forEach(function(p){
         g+='<circle cx="'+X(p.x).toFixed(1)+'" cy="'+Y(p.v).toFixed(1)+'" r="3.4" fill="'+s.color+'" stroke="#fffdf8" stroke-width="1"/>';
@@ -776,9 +836,11 @@ function renderOverlay(){
   var extLegend='';
   list.forEach(function(s){
     if(s.id==='bazi'||s.id==='bazi-annual'||s.id==='ziwei-daen'||s.id==='ziwei-year') return;
-    var desc=s.id==='taiyi-xianli'?'（十二宮歲段中點斜線，含時宮加權，各自正規化）'
+    var desc=s.id==='wuxing-line'?'（八字體質×紫微大限環境×大限四化＋大運對環境生剋，各自正規化；與太乙比對的核心線）'
+            :s.id==='wuxing-year'?'（大限環境＋大運層＋流年干支對環境助拆＋流年四化落大限主星，各自正規化；與太乙比對的核心線）'
+            :s.id==='taiyi-xianli'?'（十二宮歲段中點斜線，含時宮加權，各自正規化）'
             :s.id==='taiyi-xingnian'?'（虛歲逐歲＝限例分＋行年加權分，各自正規化）':'';
-    extLegend+='<span><i style="display:inline-block;vertical-align:middle;margin-right:5px;width:22px;height:0;border-top:2px solid '+s.color+'"></i>'+s.label+desc+'</span>　';
+    extLegend+='<span><i style="display:inline-block;vertical-align:middle;margin-right:5px;width:22px;height:0;border-top:2px solid '+s.color+'"></i>'+s.label+desc+'</span>';
   });
   $('lgExt').innerHTML=extLegend;
   $('viewScope').textContent='圖表 '+x0+'–'+x1+' · 分析以完整資料計算';
@@ -802,7 +864,10 @@ function renderReso(reso){
     return bs.map(function(b){ return b.y0===b.y1?(''+b.y0):(b.y0+'–'+b.y1); }).join('、');
   };
   var html='';
-  reso.pairs.forEach(function(p){
+  var coreLabels={'八字＋紫微（五行主線）':1,'八字＋紫微（五行主線・流年）':1,'限例太乙':1,'太乙行年趨勢':1};
+  var shown=reso.pairs.filter(function(p){return coreLabels[p.a]&&coreLabels[p.b];});
+  if(!shown.length) shown=reso.pairs;
+  shown.forEach(function(p){
     html+='<span>'+p.a+' × '+p.b+'：同向率 <b>'+(p.sameRate===null?'—':p.sameRate+'%')+'</b>'+
           '（同向 '+p.sameN+' 年／背離 '+p.oppN+' 年／平緩 '+p.flatN+' 年）'+
           '　形狀相似度 <b>'+(p.r===null?'—':p.r.toFixed(2))+'</b>（'+rWord(p.r)+'）</span>';
@@ -817,10 +882,13 @@ function renderReso(reso){
 }
 
 /* ---------- 三線同步提醒（固定抓八字／紫微／太乙三條核心線） ---------- */
+/* v0.9.35：核心比對線＝「八字＋紫微」（五行主線曲線）與太乙，兩線。八字、紫微單線不再各自與太乙比對。 */
+var CORE_IDS={daen:{hebing:'wuxing-line',taiyi:'taiyi-xianli'},year:{hebing:'wuxing-year',taiyi:'taiyi-xingnian'}};
+var CORE_N=2, HEBING_NAME='八字＋紫微';
 function coreThreeSeries(mode){
   var ids=mode==='daen'
-    ?['bazi','ziwei-daen','taiyi-xianli']
-    :['bazi-annual','ziwei-year','taiyi-xingnian'];
+    ?[CORE_IDS.daen.hebing,CORE_IDS.daen.taiyi]
+    :[CORE_IDS.year.hebing,CORE_IDS.year.taiyi];
   var all=buildSeries(mode);
   return ids.map(function(id){
     for(var i=0;i<all.length;i++) if(all[i].id===id) return all[i];
@@ -978,19 +1046,17 @@ function dominantTiming(pair){
 }
 function buildTurningModeSnapshot(mode,birthY){
   var core=coreThreeSeries(mode);
-  if(core.length!==3) return null;
-  var bazi=core[0],ziwei=core[1],taiyi=core[2];
-  var bt=turningPoints(bazi,mode),zt=turningPoints(ziwei,mode),tt=turningPoints(taiyi,mode);
-  var bp=turningPairAnalysis(taiyi,bazi,mode,tt,bt);
-  var zp=turningPairAnalysis(taiyi,ziwei,mode,tt,zt);
+  if(core.length!==CORE_N) return null;
+  var hebing=core[0],taiyi=core[1];
+  var ht=turningPoints(hebing,mode),tt=turningPoints(taiyi,mode);
+  var hp=turningPairAnalysis(taiyi,hebing,mode,tt,ht);
   var rows=tt.map(function(t,i){
     var age=turningAge(t.year,birthY);
     return {year:t.year,type:t.type,label:t.label,strength:t.strength,realAge:age.real,virtualAge:age.virtual,
-            bazi:bp.match.matches[i],ziwei:zp.match.matches[i]};
+            hebing:hp.match.matches[i]};
   });
-  var comparison=Math.abs(bp.fitScore-zp.fitScore)<5?'close':(bp.fitScore>zp.fitScore?'bazi':'ziwei');
-  return {mode:mode,config:Object.assign({},TURN_CONFIG[mode]),turns:{taiyi:tt,bazi:bt,ziwei:zt},
-          pairs:{bazi:bp,ziwei:zp},rows:rows,comparison:comparison};
+  return {mode:mode,config:Object.assign({},TURN_CONFIG[mode]),turns:{taiyi:tt,hebing:ht},
+          pairs:{hebing:hp},rows:rows,comparison:'hebing'};
 }
 function buildTurningSnapshot(){
   var B=STATE.bazi&&STATE.bazi.ok?STATE.bazi.data:null;
@@ -1034,20 +1100,17 @@ function fmtYear(y){return Math.abs(y-Math.round(y))<1e-6?String(Math.round(y)):
 function turnModeHtml(mode,title,birthY){
   var snap=buildTurningModeSnapshot(mode,birthY);
   if(!snap) return '<div class="turn-mode"><h4>'+title+'</h4><div class="sync-wait">三線資料尚未齊備，暫時無法分析轉折關係。</div></div>';
-  var bp=snap.pairs.bazi,zp=snap.pairs.ziwei;
-  var fit=snap.comparison==='close'
-    ?'太乙與八字、紫微的綜合貼合度接近（差距小於 5 分）'
-    :(snap.comparison==='bazi'?'太乙線型目前較貼近八字':'太乙線型目前較貼近紫微');
-  var conclusion=fit+'；太乙 × 八字'+dominantTiming(bp)+'，太乙 × 紫微'+dominantTiming(zp)+'。';
+  var hp=snap.pairs.hebing;
+  var conclusion='太乙 × '+HEBING_NAME+'：綜合貼合度 '+hp.fitScore+'；'+dominantTiming(hp)+'。';
   var rows=snap.rows.length?snap.rows.map(function(r){
     return '<tr><td>'+fmtYear(r.year)+'<br><span style="color:var(--muted)">實歲 '+fmtYear(r.realAge)+'／虛歲 '+fmtYear(r.virtualAge)+'</span></td>'+ 
-      '<td><span class="turn-type '+r.type+'">'+r.label+'</span></td><td>'+turnMatchCell(r.bazi)+'</td><td>'+turnMatchCell(r.ziwei)+'</td></tr>';
-  }).join(''):'<tr><td colspan="4">依目前門檻未辨識出有效太乙轉折。</td></tr>';
+      '<td><span class="turn-type '+r.type+'">'+r.label+'</span></td><td>'+turnMatchCell(r.hebing)+'</td></tr>';
+  }).join(''):'<tr><td colspan="3">依目前門檻未辨識出有效太乙轉折。</td></tr>';
   var cfg=snap.config;
   return '<div class="turn-mode"><h4>'+title+'</h4><div class="turn-conclusion">'+conclusion+'</div>'+ 
-    '<div class="turn-pairs">'+turnPairHtml('八字',bp)+turnPairHtml('紫微',zp)+'</div>'+ 
+    '<div class="turn-pairs">'+turnPairHtml(HEBING_NAME,hp)+'</div>'+ 
     '<details class="turn-detail"><summary>檢視太乙轉折逐筆配對（共 '+snap.rows.length+' 筆）</summary><div class="turn-table-wrap"><table class="turn-table">'+
-    '<thead><tr><th>太乙轉折</th><th>類型</th><th>八字對應</th><th>紫微對應</th></tr></thead><tbody>'+rows+'</tbody></table></div></details>'+ 
+    '<thead><tr><th>太乙轉折</th><th>類型</th><th>'+HEBING_NAME+'對應</th></tr></thead><tbody>'+rows+'</tbody></table></div></details>'+ 
     '<div class="turn-method">判定：'+(mode==='daen'?'原始大限節點':'三年移動平均')+'；最小幅度 '+cfg.minDelta+'；至少維持 '+cfg.minRun+(mode==='daen'?' 個線段':' 年')+
     '；同方向配對窗 ±'+cfg.matchWindow+' 年，±1 年視為同期。綜合貼合度＝最佳時間差相關 40%＋方向一致率 30%＋轉折吻合率 30%。</div></div>';
 }
@@ -1056,21 +1119,19 @@ function renderTurningAnalysis(){
   var birthY=B?B.birthYear:parseInt($('f-year').value);
   $('turnCard').style.display='';
   if(!birthY){
-    $('turnGrid').innerHTML='<div class="sync-wait">尚未排盤——三法大限與流年曲線齊備後，這裡會顯示轉折關係。</div>';
+    $('turnGrid').innerHTML='<div class="sync-wait">尚未排盤——八字＋紫微與太乙的大限、流年曲線齊備後，這裡會顯示轉折關係。</div>';
     return;
   }
   $('turnGrid').innerHTML=turnModeHtml('daen','大限轉折關係',birthY)+turnModeHtml('year','流年轉折關係',birthY);
 }
 /* 大限提醒採折線斜率：上行須三線同升；下行須太乙下降＋八字或紫微任一線下降。 */
 function slopeOverlapBands(mode,core,kind){
-  var ids=mode==='daen'
-    ?{bazi:'bazi',ziwei:'ziwei-daen',taiyi:'taiyi-xianli'}
-    :{bazi:'bazi-annual',ziwei:'ziwei-year',taiyi:'taiyi-xingnian'};
+  var ids=CORE_IDS[mode];
   var byId={};
   core.forEach(function(s){byId[s.id]=s;});
-  var bazi=byId[ids.bazi], ziwei=byId[ids.ziwei], taiyi=byId[ids.taiyi];
-  if(!bazi||!ziwei||!taiyi) return [];
-  var series=[bazi,ziwei,taiyi], lo=-Infinity, hi=Infinity;
+  var hebing=byId[ids.hebing], taiyi=byId[ids.taiyi];
+  if(!hebing||!taiyi) return [];
+  var series=[hebing,taiyi], lo=-Infinity, hi=Infinity;
   series.forEach(function(s){
     if(!s.pts.length) return;
     lo=Math.max(lo,s.pts[0].x);
@@ -1088,22 +1149,17 @@ function slopeOverlapBands(mode,core,kind){
   for(var i=0;i<cuts.length-1;i++){
     var x0=cuts[i], x1=cuts[i+1], span=x1-x0;
     if(span<=SLOPE_EPS) continue;
-    var b0=sampleAt(bazi,x0), b1=sampleAt(bazi,x1);
-    var z0=sampleAt(ziwei,x0), z1=sampleAt(ziwei,x1);
+    var h0=sampleAt(hebing,x0), h1=sampleAt(hebing,x1);
     var t0=sampleAt(taiyi,x0), t1=sampleAt(taiyi,x1);
-    if(b0===null||b1===null||z0===null||z1===null||t0===null||t1===null){cur=null;continue;}
-    var bSlope=(b1-b0)/span, zSlope=(z1-z0)/span, tSlope=(t1-t0)/span;
+    if(h0===null||h1===null||t0===null||t1===null){cur=null;continue;}
+    var hSlope=(h1-h0)/span, tSlope=(t1-t0)/span;
     var up=kind==='hi';
-    var bMatch=up?bSlope>SLOPE_EPS:bSlope< -SLOPE_EPS;
-    var zMatch=up?zSlope>SLOPE_EPS:zSlope< -SLOPE_EPS;
+    var hMatch=up?hSlope>SLOPE_EPS:hSlope< -SLOPE_EPS;
     var tMatch=up?tSlope>SLOPE_EPS:tSlope< -SLOPE_EPS;
-    var qualified=up?(tMatch&&bMatch&&zMatch):(tMatch&&(bMatch||zMatch));
+    var qualified=tMatch&&hMatch;
     if(qualified){
-      var companions=[];
-      var pairStrength=[];
-      if(!up&&bMatch){ companions.push('八字'); pairStrength.push(Math.min(-tSlope,-bSlope)); }
-      if(!up&&zMatch){ companions.push('紫微'); pairStrength.push(Math.min(-tSlope,-zSlope)); }
-      var strength=up?Math.min(tSlope,bSlope,zSlope):Math.max.apply(null,pairStrength);
+      var companions=[HEBING_NAME];
+      var strength=up?Math.min(tSlope,hSlope):Math.min(-tSlope,-hSlope);
       /* 太乙為主：跨到太乙下一個折線節點時另開一組；只跨八字／紫微節點則可續接。 */
       var taiyiNode=taiyi.pts.some(function(p){return Math.abs(p.x-x0)<=SLOPE_EPS;});
       if(cur&&Math.abs(x0-cur._x1)<=SLOPE_EPS&&!taiyiNode){
@@ -1124,7 +1180,7 @@ function slopeOverlapBands(mode,core,kind){
     b.slopeStrength=Math.round((b._weighted/b._duration)*1000)/1000;
     b.trendStart=Math.round(b._x0*1000)/1000;
     b.trendEnd=Math.round(b._x1*1000)/1000;
-    b.companions=['八字','紫微'].filter(function(name){return !!b._companions[name];});
+    b.companions=[HEBING_NAME].filter(function(name){return !!b._companions[name];});
     delete b._x0; delete b._x1; delete b._weighted; delete b._duration; delete b._companions;
   });
   return bands;
@@ -1132,13 +1188,11 @@ function slopeOverlapBands(mode,core,kind){
 
 /* 流年提醒採「位置」且只列今年起的未來：連續符合年份合併為一組。 */
 function futurePositionBands(mode,core,fromYear){
-  var ids=mode==='daen'
-    ?{bazi:'bazi',ziwei:'ziwei-daen',taiyi:'taiyi-xianli'}
-    :{bazi:'bazi-annual',ziwei:'ziwei-year',taiyi:'taiyi-xingnian'};
+  var ids=CORE_IDS[mode];
   var byId={}; core.forEach(function(s){byId[s.id]=s;});
-  var bazi=byId[ids.bazi], ziwei=byId[ids.ziwei], taiyi=byId[ids.taiyi];
-  if(!bazi||!ziwei||!taiyi) return [];
-  var series=[bazi,ziwei,taiyi], lo=-Infinity, hi=Infinity;
+  var hebing=byId[ids.hebing], taiyi=byId[ids.taiyi];
+  if(!hebing||!taiyi) return [];
+  var series=[hebing,taiyi], lo=-Infinity, hi=Infinity;
   series.forEach(function(s){
     if(!s.pts.length) return;
     lo=Math.max(lo,s.pts[0].x);
@@ -1147,17 +1201,13 @@ function futurePositionBands(mode,core,fromYear){
   if(!isFinite(lo)||!isFinite(hi)) return [];
   var bands=[], cur=null;
   for(var yr=Math.max(Math.ceil(lo),fromYear);yr<=Math.floor(hi);yr++){
-    var bv=sampleAt(bazi,yr), zv=sampleAt(ziwei,yr), tv=sampleAt(taiyi,yr);
-    if(bv===null||zv===null||tv===null){cur=null;continue;}
+    var hv=sampleAt(hebing,yr), tv=sampleAt(taiyi,yr);
+    if(hv===null||tv===null){cur=null;continue;}
     var kind=null, companions=[], strength=0;
-    if(bv>DEADBAND&&zv>DEADBAND&&tv>DEADBAND){
-      kind='hi'; strength=Math.min(Math.abs(bv),Math.abs(zv),Math.abs(tv));
-    }else if(tv< -DEADBAND&&(bv< -DEADBAND||zv< -DEADBAND)){
-      kind='lo';
-      var pairs=[];
-      if(bv< -DEADBAND){companions.push('八字');pairs.push(Math.min(Math.abs(tv),Math.abs(bv)));}
-      if(zv< -DEADBAND){companions.push('紫微');pairs.push(Math.min(Math.abs(tv),Math.abs(zv)));}
-      strength=Math.max.apply(null,pairs);
+    if(hv>DEADBAND&&tv>DEADBAND){
+      kind='hi'; strength=Math.min(Math.abs(hv),Math.abs(tv));
+    }else if(tv< -DEADBAND&&hv< -DEADBAND){
+      kind='lo'; companions.push(HEBING_NAME); strength=Math.min(Math.abs(tv),Math.abs(hv));
     }
     if(kind&&cur&&cur.kind===kind&&yr===cur.y1+1){
       cur.y1=yr; cur._sum+=strength; cur._n++;
@@ -1170,7 +1220,7 @@ function futurePositionBands(mode,core,fromYear){
   }
   bands.forEach(function(b){
     b.strength=Math.round((b._sum/b._n)*1000)/1000;
-    b.companions=['八字','紫微'].filter(function(name){return !!b._companions[name];});
+    b.companions=[HEBING_NAME].filter(function(name){return !!b._companions[name];});
     delete b._sum; delete b._n; delete b._companions;
   });
   return bands;
@@ -1178,11 +1228,11 @@ function futurePositionBands(mode,core,fromYear){
 
 /* 大限第二階段：斜率近似但未正式成立時，才用數值位置補判。 */
 function daenPositionFallbackBands(core,kind){
-  var ids={bazi:'bazi',ziwei:'ziwei-daen',taiyi:'taiyi-xianli'};
+  var ids=CORE_IDS.daen;
   var byId={}; core.forEach(function(s){byId[s.id]=s;});
-  var bazi=byId[ids.bazi], ziwei=byId[ids.ziwei], taiyi=byId[ids.taiyi];
-  if(!bazi||!ziwei||!taiyi) return [];
-  var series=[bazi,ziwei,taiyi], lo=-Infinity, hi=Infinity;
+  var hebing=byId[ids.hebing], taiyi=byId[ids.taiyi];
+  if(!hebing||!taiyi) return [];
+  var series=[hebing,taiyi], lo=-Infinity, hi=Infinity;
   series.forEach(function(s){
     if(!s.pts.length) return;
     lo=Math.max(lo,s.pts[0].x);
@@ -1191,30 +1241,19 @@ function daenPositionFallbackBands(core,kind){
   if(!isFinite(lo)||!isFinite(hi)) return [];
   var bands=[], cur=null, lastYear=Math.floor(hi)-1;
   for(var yr=Math.ceil(lo);yr<=lastYear;yr++){
-    var b0=sampleAt(bazi,yr), b1=sampleAt(bazi,yr+1);
-    var z0=sampleAt(ziwei,yr), z1=sampleAt(ziwei,yr+1);
+    var h0=sampleAt(hebing,yr), h1=sampleAt(hebing,yr+1);
     var t0=sampleAt(taiyi,yr), t1=sampleAt(taiyi,yr+1);
-    if(b0===null||b1===null||z0===null||z1===null||t0===null||t1===null){cur=null;continue;}
-    var bs=b1-b0, zs=z1-z0, ts=t1-t0;
+    if(h0===null||h1===null||t0===null||t1===null){cur=null;continue;}
+    var hs=h1-h0, ts=t1-t0;
     var up=kind==='hi';
-    var primary=up
-      ?(bs>SLOPE_EPS&&zs>SLOPE_EPS&&ts>SLOPE_EPS)
-      :(ts< -SLOPE_EPS&&(bs< -SLOPE_EPS||zs< -SLOPE_EPS));
+    var primary=up?(hs>SLOPE_EPS&&ts>SLOPE_EPS):(hs< -SLOPE_EPS&&ts< -SLOPE_EPS);
     var slopeMatches=up
-      ?[bs,zs,ts].filter(function(v){return v>SLOPE_EPS;}).length
-      :[bs,zs,ts].filter(function(v){return v< -SLOPE_EPS;}).length;
-    var near=up?slopeMatches>=2:slopeMatches>=1;
+      ?[hs,ts].filter(function(v){return v>SLOPE_EPS;}).length
+      :[hs,ts].filter(function(v){return v< -SLOPE_EPS;}).length;
+    var near=slopeMatches>=1;
     var companions=[], position=false, strength=0;
-    if(up){
-      position=b0>DEADBAND&&z0>DEADBAND&&t0>DEADBAND;
-      if(position) strength=Math.min(Math.abs(b0),Math.abs(z0),Math.abs(t0));
-    }else{
-      position=t0< -DEADBAND&&(b0< -DEADBAND||z0< -DEADBAND);
-      var pairs=[];
-      if(position&&b0< -DEADBAND){companions.push('八字');pairs.push(Math.min(Math.abs(t0),Math.abs(b0)));}
-      if(position&&z0< -DEADBAND){companions.push('紫微');pairs.push(Math.min(Math.abs(t0),Math.abs(z0)));}
-      if(pairs.length) strength=Math.max.apply(null,pairs);
-    }
+    position=up?(h0>DEADBAND&&t0>DEADBAND):(h0< -DEADBAND&&t0< -DEADBAND);
+    if(position){ strength=Math.min(Math.abs(h0),Math.abs(t0)); if(!up) companions.push(HEBING_NAME); }
     if(!primary&&near&&position){
       if(cur&&yr===cur.y1+1){
         cur.y1=yr; cur._sum+=strength; cur._n++; cur.nearSlopeCount=Math.max(cur.nearSlopeCount,slopeMatches);
@@ -1229,7 +1268,7 @@ function daenPositionFallbackBands(core,kind){
   }
   bands.forEach(function(b){
     b.strength=Math.round((b._sum/b._n)*1000)/1000;
-    b.companions=['八字','紫微'].filter(function(name){return !!b._companions[name];});
+    b.companions=[HEBING_NAME].filter(function(name){return !!b._companions[name];});
     delete b._sum; delete b._n; delete b._companions;
   });
   return bands;
@@ -1275,21 +1314,20 @@ function syncColumnHtml(mode,kind,bands,birthY,limit){
   return '<div class="sync-col '+(up?'up':'down')+'"><b>'+title+'</b>'+body+
          '<div class="sync-remind">'+reminder+'</div></div>';
 }
-/* ---------- 太乙錨定可信區段 ----------
-   太乙是主線；八字、紫微各自依方向／位置／轉折提供證據。
-   大限：方向 40、位置 35、轉折 25；流年：位置 50、方向 25、轉折 25。
-   A＝兩條佐證線皆達 70；B＝至少一條達 70 且另一條未強烈反向；
-   C＝至少有一項有效支持；兩條佐證線都明顯反向則標示衝突。 */
+/* ---------- 太乙錨定可信區段（v0.9.35：單一佐證線）----------
+   太乙是主線；佐證線只有一條——「八字＋紫微」五行主線曲線（大限用 wuxing-line、流年用 wuxing-year），
+   依方向／位置／轉折提供證據。大限：方向 40、位置 35、轉折 25；流年：位置 50、方向 25、轉折 25。
+   A＝佐證分達 70 且反向分低於 35；C＝佐證分達 35；反向分達 35 而佐證分未達 35 則標示衝突。
+   B 級（原「至少一法達 70」）在單一佐證線下不再出現，保留文字僅供讀舊檔。
+   八字可靠度係數（v0.9.28）沿用於此線——它的喜忌來自八字端。 */
 var ANCHOR_CONFIG={
   daen:{direction:40,position:35,turn:25,turnWindow:5,minYears:2,futureOnly:false},
   year:{direction:25,position:50,turn:25,turnWindow:2,minYears:2,futureOnly:true}
 };
 function anchorSeriesMap(mode,core){
-  var ids=mode==='daen'
-    ?{bazi:'bazi',ziwei:'ziwei-daen',taiyi:'taiyi-xianli'}
-    :{bazi:'bazi-annual',ziwei:'ziwei-year',taiyi:'taiyi-xingnian'};
+  var ids=CORE_IDS[mode];
   var byId={}; (core||[]).forEach(function(s){byId[s.id]=s;});
-  return {bazi:byId[ids.bazi],ziwei:byId[ids.ziwei],taiyi:byId[ids.taiyi]};
+  return {hebing:byId[ids.hebing],taiyi:byId[ids.taiyi]};
 }
 function anchorPosition(v){
   return v>DEADBAND?1:(v< -DEADBAND?-1:0);
@@ -1343,12 +1381,10 @@ function anchorPairEvidence(mode,name,rows,taiyi,other,band){
           positionRate:Math.round(posRate*100),positionEffective:posEff,
           turnMatches:turnMatches,oppositeTurns:oppositeTurns};
 }
-function anchorBandGrade(bazi,ziwei){
-  var best=Math.max(bazi.score,ziwei.score),maxOpp=Math.max(bazi.opposition,ziwei.opposition);
-  if(bazi.score>=70&&ziwei.score>=70&&maxOpp<35) return 'A';
-  if((bazi.score>=70&&ziwei.opposition<70)||(ziwei.score>=70&&bazi.opposition<70)) return 'B';
-  if(bazi.opposition>=35&&ziwei.opposition>=35&&best<35) return 'conflict';
-  if(best>=35) return 'C';
+function anchorBandGrade(hp){
+  if(hp.score>=70&&hp.opposition<35) return 'A';
+  if(hp.opposition>=35&&hp.score<35) return 'conflict';
+  if(hp.score>=35) return 'C';
   return null;
 }
 /* ---------- 八字可靠度係數（v0.9.28）----------
@@ -1372,55 +1408,48 @@ function baziReliability(){
 }
 /* 可信度＝支持減反向（v0.9.28）。舊版對 B、C 級只取最強支持線分數、
    完全不扣反向證據，會把「八字支持 72、紫微反向 65」顯示成 72%。 */
-function anchorBandConfidence(grade,bazi,ziwei){
-  if(grade==='conflict') return Math.min(bazi.opposition,ziwei.opposition);
-  var b=Math.max(0,bazi.score-bazi.opposition),z=Math.max(0,ziwei.score-ziwei.opposition);
-  if(grade==='A') return Math.min(b,z);
-  return Math.max(b,z);
+function anchorBandConfidence(grade,hp){
+  if(grade==='conflict') return hp.opposition;
+  return Math.max(0,hp.score-hp.opposition);
 }
 function buildAnchoredBands(mode,core,fromYear){
-  var map=anchorSeriesMap(mode,core),bazi=map.bazi,ziwei=map.ziwei,taiyi=map.taiyi;
-  if(!bazi||!ziwei||!taiyi) return [];
-  var raw={bazi:bazi,ziwei:ziwei,taiyi:taiyi};
+  var map=anchorSeriesMap(mode,core),hebing=map.hebing,taiyi=map.taiyi;
+  if(!hebing||!taiyi||!hebing.pts.length||!taiyi.pts.length) return [];
+  var raw={hebing:hebing,taiyi:taiyi};
   var trend=mode==='year'
-    ?{bazi:preparedTurnSeries(bazi,mode),ziwei:preparedTurnSeries(ziwei,mode),taiyi:preparedTurnSeries(taiyi,mode)}
+    ?{hebing:preparedTurnSeries(hebing,mode),taiyi:preparedTurnSeries(taiyi,mode)}
     :raw;
-  var lo=Math.ceil(Math.max(bazi.pts[0].x,ziwei.pts[0].x,taiyi.pts[0].x));
-  var hi=Math.floor(Math.min(bazi.pts[bazi.pts.length-1].x,ziwei.pts[ziwei.pts.length-1].x,taiyi.pts[taiyi.pts.length-1].x));
+  var lo=Math.ceil(Math.max(hebing.pts[0].x,taiyi.pts[0].x));
+  var hi=Math.floor(Math.min(hebing.pts[hebing.pts.length-1].x,taiyi.pts[taiyi.pts.length-1].x));
   if(ANCHOR_CONFIG[mode].futureOnly) lo=Math.max(lo,fromYear||new Date().getFullYear());
   var bands=[],cur=null;
   for(var y=lo;y<=hi;y++){
-    var tv=sampleAt(taiyi,y),bv=sampleAt(bazi,y),zv=sampleAt(ziwei,y);
-    if(tv===null||bv===null||zv===null){cur=null;continue;}
-    var tdir=anchorDirection(trend.taiyi,y,mode),bdir=anchorDirection(trend.bazi,y,mode),zdir=anchorDirection(trend.ziwei,y,mode);
-    var tp=anchorPosition(tv),bp=anchorPosition(bv),zp=anchorPosition(zv),kind=null;
+    var tv=sampleAt(taiyi,y),hv=sampleAt(hebing,y);
+    if(tv===null||hv===null){cur=null;continue;}
+    var tdir=anchorDirection(trend.taiyi,y,mode),hdir=anchorDirection(trend.hebing,y,mode);
+    var tp=anchorPosition(tv),hpv=anchorPosition(hv),kind=null;
     if(mode==='daen') kind=tdir>0?'up':(tdir<0?'down':(tp>0?'up':(tp<0?'down':null)));
     else kind=tp>0?'up':(tp<0?'down':null);
     if(!kind){cur=null;continue;}
-    var bSupport=(tdir&&tdir===bdir)||(tp&&tp===bp),zSupport=(tdir&&tdir===zdir)||(tp&&tp===zp);
-    var bOpp=(tdir&&bdir&&tdir===-bdir)||(tp&&bp&&tp===-bp);
-    var zOpp=(tdir&&zdir&&tdir===-zdir)||(tp&&zp&&tp===-zp);
-    if(!(bSupport||zSupport||(bOpp&&zOpp))){cur=null;continue;}
-    var row={y:y,tv:tv,tdir:tdir,bazi:{v:bv,dir:bdir},ziwei:{v:zv,dir:zdir}};
+    var hSupport=(tdir&&tdir===hdir)||(tp&&tp===hpv);
+    var hOpp=(tdir&&hdir&&tdir===-hdir)||(tp&&hpv&&tp===-hpv);
+    if(!(hSupport||hOpp)){cur=null;continue;}
+    var row={y:y,tv:tv,tdir:tdir,hebing:{v:hv,dir:hdir}};
     if(cur&&cur.kind===kind&&y===cur.y1+1){cur.y1=y;cur.rows.push(row);}
     else{cur={y0:y,y1:y,kind:kind,rows:[row]};bands.push(cur);}
   }
   return bands.map(function(b){
-    var bp=anchorPairEvidence(mode,'bazi',b.rows,taiyi,bazi,b);
-    var zp=anchorPairEvidence(mode,'ziwei',b.rows,taiyi,ziwei,b);
-    /* v0.9.28：八字端自評的可靠度在此進入分級，原始值保留供回查。 */
+    var hp=anchorPairEvidence(mode,'hebing',b.rows,taiyi,hebing,b);
+    /* v0.9.28：八字端自評的可靠度在此進入分級（五行主線的喜忌來自八字端），原始值保留供回查。 */
     var rel=baziReliability();
-    bp.reliability=rel; bp.scoreRaw=bp.score; bp.oppositionRaw=bp.opposition;
-    bp.score=Math.round(bp.score*rel); bp.opposition=Math.round(bp.opposition*rel);
-    var grade=anchorBandGrade(bp,zp),tDirYears=b.rows.filter(function(r){return !!r.tdir;}).length;
+    hp.reliability=rel; hp.scoreRaw=hp.score; hp.oppositionRaw=hp.opposition;
+    hp.score=Math.round(hp.score*rel); hp.opposition=Math.round(hp.opposition*rel);
+    var grade=anchorBandGrade(hp),tDirYears=b.rows.filter(function(r){return !!r.tdir;}).length;
     if(!grade||b.y1-b.y0+1<ANCHOR_CONFIG[mode].minYears) return null;
-    b.grade=grade;b.confidence=anchorBandConfidence(grade,bp,zp);
+    b.grade=grade;b.confidence=anchorBandConfidence(grade,hp);
     b.basis=mode==='year'?'position':(tDirYears>=Math.ceil(b.rows.length/2)?'slope':'position-fallback');
-    b.pairs={bazi:bp,ziwei:zp};
-    b.supporters=['八字','紫微'].filter(function(n){
-      var p=n==='八字'?bp:zp;
-      return p.score>=35&&p.score>=p.opposition;
-    });
+    b.pairs={hebing:hp};
+    b.supporters=(hp.score>=35&&hp.score>=hp.opposition)?[HEBING_NAME]:[];
     b.companions=b.supporters.slice();
     b.strength=Math.round(b.confidence*10)/1000;
     delete b.rows;
@@ -1435,12 +1464,12 @@ function pickAnchoredBands(bands,kind,limit){
   return picked.sort(function(a,b){return a.y0-b.y0||a.y1-b.y1;});
 }
 function anchorGradeText(grade){
-  return grade==='A'?'A 強共識':(grade==='B'?'B 雙法支持':(grade==='C'?'C 部分支持':'衝突區'));
+  return grade==='A'?'A 強共識':(grade==='B'?'B 雙法支持（舊檔）':(grade==='C'?'C 部分支持':'衝突區'));
 }
 function anchoredBandHtml(b,birthY){
   var real0=b.y0-birthY,real1=b.y1-birthY,virt0=real0+1,virt1=real1+1;
   /* v0.9.28：折扣本身必須看得見，否則等於把證據強度再藏一次。 */
-  var rel=b.pairs&&b.pairs.bazi?b.pairs.bazi.reliability:1;
+  var rel=b.pairs&&(b.pairs.hebing||b.pairs.bazi)?(b.pairs.hebing||b.pairs.bazi).reliability:1;
   var relTag=(rel!==undefined&&rel<1)?'<span class="anchor-rel">八字可靠度 '+Math.round(rel*100)+'%</span>':'';
   var legacy=(typeof SAVE_LEGACY!=='undefined'&&SAVE_LEGACY)?'<span class="anchor-rel">舊版口徑</span>':'';
   return '<li><div class="anchor-line"><span class="anchor-grade '+b.grade.toLowerCase()+'">'+anchorGradeText(b.grade)+'</span>'+
@@ -1542,15 +1571,14 @@ function buildAnnualExtremaSnapshot(core,birthY){
     var B=STATE.bazi&&STATE.bazi.ok?STATE.bazi.data:null;
     birthY=B?B.birthYear:parseInt($('f-year').value);
   }
-  if(!birthY||!core||core.length!==3) return null;
-  var map=anchorSeriesMap('year',core),series=[map.bazi,map.ziwei,map.taiyi];
+  if(!birthY||!core||core.length!==CORE_N) return null;
+  var map=anchorSeriesMap('year',core),series=[map.hebing,map.taiyi];
   if(series.some(function(s){return !s||!s.pts||!s.pts.length;})) return null;
   var lo=Math.ceil(Math.max.apply(null,series.map(function(s){return s.pts[0].x;})));
   var hi=Math.floor(Math.min.apply(null,series.map(function(s){return s.pts[s.pts.length-1].x;})));
   if(hi-lo<2) return null;
   var target={
-    bazi:{peak:localExtremaPoints(map.bazi,'peak',lo,hi),trough:localExtremaPoints(map.bazi,'trough',lo,hi)},
-    ziwei:{peak:localExtremaPoints(map.ziwei,'peak',lo,hi),trough:localExtremaPoints(map.ziwei,'trough',lo,hi)}
+    hebing:{peak:localExtremaPoints(map.hebing,'peak',lo,hi),trough:localExtremaPoints(map.hebing,'trough',lo,hi)}
   };
   var specialYears=map.taiyi.specialYears||{};
   function annotateMatch(match){
@@ -1564,14 +1592,13 @@ function buildAnnualExtremaSnapshot(core,birthY){
       var row={year:p.year,value:p.value,plateauStart:p.plateauStart,plateauEnd:p.plateauEnd,
               realAge:p.year-birthY,virtualAge:p.year-birthY+1,
               specialSignals:taiyiSpecialSignalsForSpan(specialYears,p.plateauStart,p.plateauEnd,birthY),
-              bazi:annotateMatch(matchLocalExtreme(p,target.bazi[kind],ANNUAL_EXTREMA_WINDOW,kind)),
-              ziwei:annotateMatch(matchLocalExtreme(p,target.ziwei[kind],ANNUAL_EXTREMA_WINDOW,kind))};
+              hebing:annotateMatch(matchLocalExtreme(p,target.hebing[kind],ANNUAL_EXTREMA_WINDOW,kind))};
       return row;
-    }).filter(function(row){return !!(row.bazi||row.ziwei);})
+    }).filter(function(row){return !!row.hebing;})
       .slice(0,ANNUAL_EXTREMA_LIMIT)
       .sort(function(a,b){return a.year-b.year;});
   }
-  return {version:'annual-extrema-echo/v5',birthYear:birthY,commonRange:{y0:lo,y1:hi},
+  return {version:'annual-extrema-echo/v6-hebing',birthYear:birthY,commonRange:{y0:lo,y1:hi},
           matchWindow:ANNUAL_EXTREMA_WINDOW,limit:ANNUAL_EXTREMA_LIMIT,
           positionRule:'support-peak-above-deadband-support-trough-below-deadband',
           positionThreshold:{peak:DEADBAND,trough:-DEADBAND},
@@ -1606,38 +1633,37 @@ function extremaMatchText(name,match,kind){
 }
 function annualExtremaRowHtml(row,kind,index){
   var matches='';
-  if(row.bazi) matches+='<div class="annual-extrema-method">'+extremaMatchText('八字',row.bazi,kind)+'</div>';
-  if(row.ziwei) matches+='<div class="annual-extrema-method">'+extremaMatchText('紫微',row.ziwei,kind)+'</div>';
+  if(row.hebing) matches+='<div class="annual-extrema-method">'+extremaMatchText(HEBING_NAME,row.hebing,kind)+'</div>';
   return '<div class="annual-extrema-row"><div class="annual-extrema-source">'+(index+1)+'. 太乙'+(kind==='peak'?'高點':'低點')+'｜西元 '+extremaSpanText(row)+
     '｜實歲 '+row.realAge+'／虛歲 '+row.virtualAge+extremaSignalsHtml(row.specialSignals,'太乙')+'</div><div class="annual-extrema-matches">'+matches+'</div></div>';
 }
 function annualExtremaColumnHtml(rows,kind){
-  var body=rows.length?rows.map(function(r,i){return annualExtremaRowHtml(r,kind,i);}).join(''):'<div class="annual-extrema-empty">共同資料範圍內沒有八字或紫微呼應的'+(kind==='peak'?'高點':'低點')+'。</div>';
+  var body=rows.length?rows.map(function(r,i){return annualExtremaRowHtml(r,kind,i);}).join(''):'<div class="annual-extrema-empty">共同資料範圍內沒有八字＋紫微呼應的'+(kind==='peak'?'高點':'低點')+'。</div>';
   return '<div class="annual-extrema-col"><b>'+(kind==='peak'?'太乙最高 '+ANNUAL_EXTREMA_LIMIT+' 個局部高點':'太乙最低 '+ANNUAL_EXTREMA_LIMIT+' 個局部低點')+'</b>'+body+'</div>';
 }
 function annualExtremaHtml(snapshot){
-  if(!snapshot) return '<div class="annual-extrema"><h5>太乙流年高低點 ±'+ANNUAL_EXTREMA_WINDOW+' 年呼應</h5><div class="annual-extrema-empty">三條流年資料尚不足，暫時無法比較。</div></div>';
+  if(!snapshot) return '<div class="annual-extrema"><h5>太乙流年高低點 ±'+ANNUAL_EXTREMA_WINDOW+' 年呼應</h5><div class="annual-extrema-empty">兩條流年資料尚不足，暫時無法比較。</div></div>';
   return '<div class="annual-extrema"><h5>太乙流年高低點 ±'+ANNUAL_EXTREMA_WINDOW+' 年呼應</h5><div class="annual-extrema-intro">'+
-    '以太乙流年最高／最低各 '+ANNUAL_EXTREMA_LIMIT+' 個局部點為錨，查看八字與紫微是否在前後 '+ANNUAL_EXTREMA_WINDOW+' 年出現同類高點／低點。'+
+    '以太乙流年最高／最低各 '+ANNUAL_EXTREMA_LIMIT+' 個局部點為錨，查看「八字＋紫微」流年曲線是否在前後 '+ANNUAL_EXTREMA_WINDOW+' 年出現同類高點／低點。'+
     '佐證線高點須同時位於高檔（> +0.1），低點須同時位於低檔（< −0.1）；低檔反彈或高檔回落不算呼應。'+
     '命中特殊年份時，以「祿馬交馳」或「祿主飛祿」短標籤提示。'+
-    '只列八字或紫微至少一法有呼應的太乙點；兩法皆未呼應者隱藏。入選後依年齡由小到大排列。比較範圍為三線共同年份 '+snapshot.commonRange.y0+'–'+snapshot.commonRange.y1+'；連續同值平台只算 1 個點。</div><div class="annual-extrema-grid">'+
+    '只列八字＋紫微有呼應的太乙點；未呼應者隱藏。入選後依年齡由小到大排列。比較範圍為兩線共同年份 '+snapshot.commonRange.y0+'–'+snapshot.commonRange.y1+'；連續同值平台只算 1 個點。</div><div class="annual-extrema-grid">'+
     annualExtremaColumnHtml(snapshot.peaks,'peak')+annualExtremaColumnHtml(snapshot.troughs,'trough')+'</div></div>';
 }
 function syncModeHtml(mode,title,limit,birthY){
   var core=coreThreeSeries(mode);
-  if(core.length!==3){
+  if(core.length!==CORE_N){
     var need=mode==='daen'
-      ?[{id:'bazi',name:'八字大運'},{id:'ziwei-daen',name:'紫微大限'},{id:'taiyi-xianli',name:'限例太乙'}]
-      :[{id:'bazi-annual',name:'八字流年'},{id:'ziwei-year',name:'紫微逐年'},{id:'taiyi-xingnian',name:'太乙行年'}];
+      ?[{id:CORE_IDS.daen.hebing,name:'八字＋紫微大限曲線'},{id:CORE_IDS.daen.taiyi,name:'限例太乙'}]
+      :[{id:CORE_IDS.year.hebing,name:'八字＋紫微流年曲線'},{id:CORE_IDS.year.taiyi,name:'太乙行年'}];
     var have={}; core.forEach(function(s){have[s.id]=true;});
     var missing=need.filter(function(x){return !have[x.id];}).map(function(x){return x.name;});
-    return '<div class="sync-mode"><h4>'+title+'</h4><div class="sync-wait">三線資料尚未齊備，等待：'+missing.join('、')+'</div></div>';
+    return '<div class="sync-mode"><h4>'+title+'</h4><div class="sync-wait">兩線資料尚未齊備，等待：'+missing.join('、')+'</div></div>';
   }
   var bands=buildAnchoredBands(mode,core,new Date().getFullYear());
   var up=pickAnchoredBands(bands,'up',limit),down=pickAnchoredBands(bands,'down',limit);
   var extrema=mode==='year'?annualExtremaHtml(buildAnnualExtremaSnapshot(core,birthY)):'';
-  return '<div class="sync-mode"><h4>'+title+'</h4><div class="anchor-legend"><span>A：八字、紫微皆強支持</span><span>B：至少一法完整支持</span><span>C：部分線索</span><span>衝突：兩法皆反向</span></div><div class="sync-cols">'+
+  return '<div class="sync-mode"><h4>'+title+'</h4><div class="anchor-legend"><span>A：八字＋紫微強支持</span><span>C：部分線索</span><span>衝突：八字＋紫微反向</span></div><div class="sync-cols">'+
          anchoredColumnHtml(mode,'up',up,birthY,limit)+anchoredColumnHtml(mode,'down',down,birthY,limit)+'</div>'+extrema+'</div>';
 }
 /* ---------- 財運訊號（v0.9.28）----------
@@ -1682,12 +1708,96 @@ function wealthListHtml(title,rows,fmt,empty){
 function ziweiHitText(hits){
   return (hits||[]).map(function(h){return h.star+'化祿→'+h.palace;}).join('、');
 }
+/* ---------- 五行主線（v0.9.30） ---------- */
+function buildWuxingMainline(){
+  var B=STATE.bazi&&STATE.bazi.ok?STATE.bazi.data:null;
+  var Z=STATE.ziwei&&STATE.ziwei.ok?STATE.ziwei.data:null;
+  if(!B||!Z||!window.WuxingMainline) return [];
+  try{ return WuxingMainline.buildMainline(B,Z); }catch(e){ console.error('wuxing mainline',e); return []; }
+}
+function wuxingRowHtml(r,cur,sc){
+  var starsTxt=r.stars.map(function(s){ return s.name+'<span class="wx-hua">'+s.element+(s.element2?'('+s.element2+')':'')+'</span>'+REL_VERB_[s.rel]+'（'+s.rel+'）'+(s.daHua?'<span class="wx-tag">'+s.daHua+'</span>':''); }).join('＋');
+  var oppTxt=r.oppStars.length?r.oppStars.map(function(s){ return s.name+'<span class="wx-hua">'+s.element+'</span>'+REL_VERB_[s.rel]+'（'+s.rel+'・<span class="wx-fav-'+s.fav+'">'+s.fav+'</span>）'; }).join('＋'):'（空）';
+  var luckTxt=r.lucks.length?r.lucks.map(function(l){ return l.text.replace(/洩口/,'<b>洩口</b>'); }).join('<br>'):'（此段超出八字逐年範圍）';
+  var tagHtml=r.tag?'<span class="wx-tag wx-tag-'+(r.tag.indexOf('機會')===0?'go':r.tag.indexOf('出口')===0?'out':'stay')+'">'+r.tag+'</span>':'';
+  return '<div class="wx-block'+(cur?' wx-cur':'')+'">'+
+    '<div class="wx-head"><span class="wx-yr">'+r.y0+'–'+r.y1+'（虛歲 '+r.start+'–'+r.end+'）</span> <b>'+r.ganZhi+'・'+r.palaceName+'</b>'+
+      '<span class="wx-hua">　能量分 '+r.score+(r.clsWord?'・'+r.clsWord:'')+'</span>'+tagHtml+
+      (sc?'<span class="wx-score">曲線分 '+(sc.score>0?'+':'')+sc.score+'<span class="wx-hua">（環境 '+(sc.envScore>0?'+':'')+sc.envScore+'・大運 '+(sc.luckScore>0?'+':'')+sc.luckScore+'）</span></span>':'')+
+    '</div>'+
+    '<ol class="wx-layers">'+
+      '<li><b>宮支</b> '+r.zhi+'<span class="wx-hua">'+r.zhiElement+'</span>'+REL_VERB_[r.zhiRel]+'（'+r.zhiRel+'・<span class="wx-fav-'+r.zhiFav+'">'+r.zhiFav+'</span>）・日主'+r.dayMaster.gan+'在'+r.zhi+'為<b>'+r.stage+'</b>　<span class="wx-hua">形態：'+r.verdict.form+'</span></li>'+
+      '<li><b>主星</b> '+(r.starsBorrowed?'<span class="wx-tag">借對宮</span>':'')+starsTxt+'　<span class="wx-hua">分配：'+r.verdict.share+'</span></li>'+
+      '<li><b>四化</b> '+r.huaText+'　<span class="wx-hua">方向：'+r.verdict.dir+'</span></li>'+
+      '<li><b>大運兩面</b> '+luckTxt+'</li>'+
+      '<li><b>對宮</b> '+r.oppZhi+'・'+r.oppPalace+'：'+oppTxt+(r.oppHelpful?'　<span class="wx-hua">→ 出口在外</span>':'')+'</li>'+
+      '<li><b>題目</b> '+r.palaceName+'＝'+r.topic+'</li>'+
+    '</ol>'+
+    '<div class="wx-verdict'+(r.verdict.verified?' wx-ok':'')+'">象：'+r.verdict.text+(r.tag?'　｜　'+r.tag:'')+'</div>'+
+    '<div class="wx-hua">年齡段 '+r.verdict.ageNote+'</div>'+
+    (sc?'<div class="wx-hua">曲線分項：'+sc.parts.map(function(x){return x.label+(x.delta>0?'+':'')+x.delta;}).join('　')+'</div>':'')+
+  '</div>';
+}
+var REL_VERB_={比劫:'同我',食傷:'我生',財:'我剋',官殺:'剋我',印:'生我'};
+/* 模組晚於排盤完成才載入時（測試環境或慢網），補畫一次 */
+window.__wuxingLoaded=function(){ if(STATE.bazi&&STATE.bazi.ok&&STATE.ziwei&&STATE.ziwei.ok&&buildSeries().length){ renderOverlay(); } };
+function renderWuxingMainline(){
+  var rows=buildWuxingMainline();
+  var B=STATE.bazi&&STATE.bazi.ok?STATE.bazi.data:null;
+  if(!rows.length){
+    var why=(B&&STATE.ziwei&&STATE.ziwei.ok&&!(STATE.ziwei.data.daen||[]).some(function(d){return d.stars;}))
+      ?'此存檔為 v2 以前格式，缺主星欄位——請重新排盤以產生五行主線。'
+      :'尚未排盤——八字與紫微完成後，這裡會顯示每一大限的五行主線。';
+    $('wuxingGrid').innerHTML='<div class="sync-wait">'+why+'</div>'; return;
+  }
+  var dm=rows[0].dayMaster;
+  var head='<div class="sync-intro">①體質：日主 <b>'+dm.gan+dm.element+'</b>'+
+    (B&&B.strength&&B.strength.level?'・'+B.strength.level:'')+
+    '・喜 '+((B&&B.favorable)||[]).join('')+'・忌 '+((B&&B.unfavorable)||[]).join('')+'</div>';
+  var Zd=STATE.ziwei.data.daen||[];
+  var sc=[]; try{ sc=WuxingMainline.scoreMainline(B,STATE.ziwei.data,rows); }catch(e){}
+  var html='<div class="wx-table">'+rows.map(function(r,i){ return wuxingRowHtml(r,!!(Zd[i]&&Zd[i].isCur),sc[i]); }).join('')+'</div>';
+  /* v0.9.31：與太乙限例的兩兩統計（來自 resonance pairs） */
+  var reso=(STATE.zmode==='daen')?resonance(buildSeries()):null, stat='';
+  if(reso&&reso.pairs){
+    reso.pairs.filter(function(pr){return pr.a==='八字＋紫微（五行主線）'||pr.b==='八字＋紫微（五行主線）';}).forEach(function(pr){
+      var other=pr.a==='八字＋紫微（五行主線）'?pr.b:pr.a;
+      stat+='<span style="margin-right:14px">× '+other+'：方向一致 '+(pr.sameRate===null?'—':pr.sameRate+'%')+'（'+pr.effN+' 年）・相關 '+(pr.r===null?'—':pr.r.toFixed(2))+' '+rWord(pr.r)+'</span>';
+    });
+  }
+  var wxNote='<div class="sync-intro" style="margin-top:8px"><b>五行主線曲線（假說級）</b>＝環境層（宮支喜忌 ±'+WuxingMainline.W.zhi+'、主星五行喜忌 ±'+WuxingMainline.W.star+'、大限四化 祿+'+WuxingMainline.W.lu+'／權+'+WuxingMainline.W.quan+'／科+'+WuxingMainline.W.ke+'／忌'+WuxingMainline.W.ji+'）＋大運層（大運干支對「宮支＋主星」的生剋→助拆指數×'+WuxingMainline.W.luck+'×環境正負）。權重為草案；此線即「八字＋紫微」核心比對線（v0.9.35 起所有太乙比對只用它），不動八字、紫微單線計分。象定形狀、分數定尺度；不以貼合太乙為目標。逐年 luckGz 止於八字曲線範圍，之後大運層為 0。'+(stat?'<br>'+stat:'')+'</div>';
+  $('wuxingGrid').innerHTML=head+html+wxNote;
+}
+/* ---------- 財運訊號・推象口徑（v0.9.36） ---------- */
+function buildWealthTuixiang(){
+  var B=STATE.bazi&&STATE.bazi.ok?STATE.bazi.data:null;
+  var Z=STATE.ziwei&&STATE.ziwei.ok?STATE.ziwei.data:null;
+  if(!B||!Z||!window.WuxingMainline||!(Z.daen||[]).some(function(d){return d.stars;})) return null;
+  try{ return WuxingMainline.wealthSignals(B,Z); }catch(e){ console.error('wealth tuixiang',e); return null; }
+}
+function wealthTuixiangHtml(ws){
+  var g=function(gr){ return gr==='強'?'a':gr==='中'?'b':'c'; };
+  var da=wealthListHtml('財路・大限',ws.daen,function(r){
+    return '<li><div class="wt-row"><div class="wt-head"><span class="anchor-grade '+g(r.grade)+'">'+r.grade+'</span>'+
+      '<span class="wt-period">'+r.ganZhi+' '+r.palaceName+'｜西元 '+spanText(r.y0,r.y1)+'｜虛歲 '+spanText(r.start,r.end)+'</span>'+
+      (r.tag?'<span class="wx-tag wx-tag-'+(r.tag.indexOf('機會')===0?'go':r.tag.indexOf('出口')===0?'out':'stay')+'">'+r.tag+'</span>':'')+'</div>'+
+      '<span class="wt-why">'+r.reasons.join('；')+'｜<b>財＝'+r.currency+'</b></span></div></li>';
+  },'各大限皆未推出財路');
+  var yr=wealthListHtml('財路・流年',ws.annual,function(r){
+    return '<li><div class="wt-row"><div class="wt-head"><span class="anchor-grade '+g(r.grade)+'">'+r.grade+'</span>'+
+      '<span class="wt-period">'+r.year+' '+r.gz+'｜大限 '+r.daGanZhi+' '+r.palaceName+'</span></div>'+
+      '<span class="wt-why">'+r.reasons.join('；')+'</span></div></li>';
+  },'各流年皆未推出財路（門檻 2 分）');
+  return '<div class="sync-mode"><h4>推象財路（日主 '+ws.dayMaster.gan+ws.dayMaster.element+'，財＝'+({木:'土',火:'金',土:'水',金:'木',水:'火'})[ws.dayMaster.element]+'）</h4><div class="sync-cols">'+da+yr+'</div></div>';
+}
 function renderWealthSignals(){
   var snap=buildWealthSnapshot();
   if(!snap){
     $('wealthGrid').innerHTML='<div class="sync-wait">尚未排盤——八字與紫微完成後，這裡會顯示財運訊號。</div>';
     return;
   }
+  var ws=buildWealthTuixiang();
+  var tx=ws?wealthTuixiangHtml(ws):'<div class="sync-mode"><h4>推象財路</h4><div class="sync-wait">需八字與紫微（v3 主星欄位）皆排盤完成——舊存檔請重新排盤。</div></div>';
   var bw=snap.bazi,zw=snap.ziwei;
   var bDa=wealthListHtml('八字財運・大限',(bw&&bw.daen)||[],function(r){
     return '<li><div class="anchor-line"><span class="anchor-grade c">八字</span>'+
@@ -1719,10 +1829,11 @@ function renderWealthSignals(){
       '<span>'+r.year+'</span>'+
       '<span class="anchor-rel">八字 '+r.bazi.gz+'・紫微 '+ziweiHitText(r.ziwei.hits)+'</span></div></li>';
   },(bw&&zw)?'兩法流年的財運訊號沒有重疊年份':'需八字與紫微皆排盤完成');
-  $('wealthGrid').innerHTML=
+  $('wealthGrid').innerHTML=tx+
+    '<details class="turn-detail"><summary>舊口徑：八字、紫微各自獨立訊號與雙法共振（v0.9.28，僅供對照）</summary>'+
     '<div class="sync-mode"><h4>雙法財運共振</h4><div class="sync-cols">'+rDa+rYr+'</div></div>'+
     '<div class="sync-mode"><h4>八字財運訊號</h4><div class="sync-cols">'+bDa+bYr+'</div></div>'+
-    '<div class="sync-mode"><h4>紫微財運訊號</h4><div class="sync-cols">'+zDa+zYr+'</div></div>';
+    '<div class="sync-mode"><h4>紫微財運訊號</h4><div class="sync-cols">'+zDa+zYr+'</div></div></details>';
 }
 function renderSyncAlerts(){
   var B=STATE.bazi&&STATE.bazi.ok?STATE.bazi.data:null;
@@ -1730,9 +1841,10 @@ function renderSyncAlerts(){
   $('syncCard').style.display='';
   if(!birthY){
     $('ovPdf').disabled=true;
-    $('syncGrid').innerHTML='<div class="sync-wait">尚未排盤——完成三法合盤後，這裡會顯示太乙錨定的可信區段。</div>';
+    $('syncGrid').innerHTML='<div class="sync-wait">尚未排盤——八字＋紫微與太乙齊備後，這裡會顯示太乙錨定的可信區段。</div>';
     renderTurningAnalysis();
     renderWealthSignals();
+    renderWuxingMainline();
     return;
   }
   var da=syncModeHtml('daen','大限運勢',6,birthY);
@@ -1740,8 +1852,9 @@ function renderSyncAlerts(){
   $('syncGrid').innerHTML=da+yr;
   renderTurningAnalysis();
   renderWealthSignals();
+  renderWuxingMainline();
   /* PDF 報告須同時具備大限、流年各三條核心線，避免輸出不完整報告。 */
-  $('ovPdf').disabled=PDF_BUSY||!(coreThreeSeries('daen').length===3&&coreThreeSeries('year').length===3);
+  $('ovPdf').disabled=PDF_BUSY||!(coreThreeSeries('daen').length===CORE_N&&coreThreeSeries('year').length===CORE_N);
 }
 function syncSnapshotRows(rows,birthY){
   return rows.map(function(b){
@@ -1760,7 +1873,7 @@ function syncSnapshotRows(rows,birthY){
 }
 function syncModeSnapshot(mode,limit,birthY){
   var core=coreThreeSeries(mode);
-  if(core.length!==3) return null;
+  if(core.length!==CORE_N) return null;
   var bands=buildAnchoredBands(mode,core,new Date().getFullYear());
   var up=pickAnchoredBands(bands,'up',limit),down=pickAnchoredBands(bands,'down',limit);
   return { limit:limit, up:syncSnapshotRows(up,birthY), down:syncSnapshotRows(down,birthY) };
@@ -1769,9 +1882,9 @@ function buildSyncSnapshot(){
   var B=STATE.bazi&&STATE.bazi.ok?STATE.bazi.data:null;
   var birthY=B?B.birthYear:parseInt($('f-year').value);
   if(!birthY) return null;
-  return { version:'taiyi-anchored-zones/v1', deadband:DEADBAND,
+  return { version:'taiyi-anchored-zones/v2-hebing', deadband:DEADBAND, companion:'wuxing-mainline (bazi+ziwei)',
            displayOrder:'chronological-after-grade-confidence-selection',
-           grades:{A:'both-support-70',B:'one-support-70',C:'partial-support-35',conflict:'both-opposition-35'},
+           grades:{A:'hebing-support-70-opposition-under-35',C:'hebing-support-35',conflict:'hebing-opposition-35-support-under-35'},
            weights:{daen:{direction:40,position:35,turn:25},year:{direction:25,position:50,turn:25}},
            daenRule:'taiyi-anchor-slope-primary', yearRule:'taiyi-anchor-future-position-primary-3y-smooth',
            futureFromYear:new Date().getFullYear(), slopeEpsilon:SLOPE_EPS,
@@ -1798,6 +1911,11 @@ function showDetail(yr){
             '<span class="tag" style="color:'+dr.clsColor+';border-color:'+dr.clsColor+'">'+dr.clsWord+'</span>'+
             (dr.isCur?'<span class="tag">今</span>':'')+(dr.isShen?'<span class="tag">身宮</span>':'')+
             '<br><span style="font-size:.8rem;color:var(--muted)">'+dr.advice+'</span>';
+      /* v0.9.30 五行主線 */
+      var wx=null; buildWuxingMainline().forEach(function(r){ if(yr>=r.y0&&yr<=r.y1) wx=r; });
+      if(wx) html+='<br>推象｜'+wx.line+'・日主在'+wx.zhi+'為'+wx.stage+(wx.tag?'・<b>'+wx.tag+'</b>':'')+'<br><span style="font-size:.8rem;color:var(--muted)">→ 象：'+wx.verdict.text+'｜題目：'+wx.topic+'｜'+wx.verdict.ageNote+'</span>';
+      var wl=null; buildSeries().forEach(function(s){ if(s.id==='wuxing-line') s.pts.forEach(function(p){ if(yr>=p.x0&&yr<p.x1) wl=p; }); });
+      if(wl) html+='<br>五行主線曲線｜'+(wl.raw>0?'+':'')+wl.raw+'<span style="font-size:.78rem;color:var(--muted)">（環境 '+(wl.row.envScore>0?'+':'')+wl.row.envScore+'・大運 '+(wl.row.luckScore>0?'+':'')+wl.row.luckScore+'）</span>';
     }
     var zp=null;
     (Z.years||[]).forEach(function(p){ if(p.year===yr) zp=p; });
@@ -1860,7 +1978,7 @@ function showDetail(yr){
 }
 
 /* 私人研究介面 v1：僅控制呈現；不修改引擎、正規化或分析取樣。 */
-function methodOf(id){return id.indexOf('bazi')===0?'bazi':id.indexOf('ziwei')===0?'ziwei':'taiyi';}
+function methodOf(id){return (id==='wuxing-line'||id==='wuxing-year')?'hebing':id.indexOf('bazi')===0?'bazi':id.indexOf('ziwei')===0?'ziwei':'taiyi';}
 function renderResearchDetail(html,yr){
   var chunks=html.split('<br>'), groups={bazi:[],ziwei:[],taiyi:[]},current='taiyi';
   chunks.shift();
@@ -1878,7 +1996,7 @@ function renderResearchDetail(html,yr){
   if(svg&&line){var a=+svg.dataset.x0,b=+svg.dataset.x1,x=44+860*(yr-a)/(b-a);line.setAttribute('x1',x);line.setAttribute('x2',x);line.setAttribute('visibility',yr>=a&&yr<=b?'visible':'hidden');}
   rememberDetailState();
 }
-function saveView(){try{localStorage.setItem('xiaoliu_hebing_research_ui_v1',JSON.stringify({bounds:VIEW.bounds,marks:VIEW.marks,hidden:VIEW.hidden}));}catch(e){}}
+function saveView(){try{localStorage.setItem('xiaoliu_hebing_research_ui_v2',JSON.stringify({bounds:VIEW.bounds,marks:VIEW.marks,hidden:VIEW.hidden}));}catch(e){}}
 function viewRender(){saveView();if(buildSeries().length)renderOverlay();document.querySelectorAll('[data-range]').forEach(function(b){b.setAttribute('aria-pressed',b.dataset.range===VIEW.range?'true':'false');});}
 document.querySelectorAll('[data-range]').forEach(function(b){b.onclick=function(){VIEW.range=b.dataset.range;viewRender();};});
 $('applyRange').onclick=function(){var a=Number($('viewFrom').value),b=Number($('viewTo').value);if(!$('viewFrom').value||!$('viewTo').value||!Number.isInteger(a)||!Number.isInteger(b)||a>=b||!VIEW.domain||b<VIEW.domain[0]||a>VIEW.domain[1]){$('viewNotice').textContent='請輸入有效起迄年，起年須早於迄年，並與曲線範圍重疊。';return;}VIEW.range='custom';VIEW.from=a;VIEW.to=b;$('viewNotice').textContent='範圍只影響圖表；百分比與分析仍以完整資料計算。';viewRender();};
@@ -1891,7 +2009,7 @@ $('yearPrev').onclick=function(){selectResearchYear((STATE.sel===null?new Date()
 $('yearNext').onclick=function(){selectResearchYear((STATE.sel===null?new Date().getFullYear():STATE.sel)+1);};
 $('yearNow').onclick=function(){selectResearchYear(new Date().getFullYear());};
 var openStates={};
-try{var stored=JSON.parse(localStorage.getItem('xiaoliu_hebing_research_ui_v1')||'null');if(stored){VIEW.bounds=stored.bounds!==false;VIEW.marks=stored.marks!==false;VIEW.hidden=stored.hidden||{};if(['bazi','ziwei','taiyi'].every(function(k){return VIEW.hidden[k];}))VIEW.hidden={};}openStates=JSON.parse(localStorage.getItem('xiaoliu_hebing_research_details_v1')||'{}');}catch(e){}
+try{var stored=JSON.parse(localStorage.getItem('xiaoliu_hebing_research_ui_v2')||'null');if(stored){VIEW.bounds=stored.bounds!==false;VIEW.marks=stored.marks!==false;VIEW.hidden=stored.hidden||Object.assign({},VIEW_DEFAULT_HIDDEN);if(['bazi','ziwei','hebing','taiyi'].every(function(k){return VIEW.hidden[k];}))VIEW.hidden=Object.assign({},VIEW_DEFAULT_HIDDEN);}openStates=JSON.parse(localStorage.getItem('xiaoliu_hebing_research_details_v1')||'{}');}catch(e){}
 $('viewBounds').checked=VIEW.bounds;$('viewMarks').checked=VIEW.marks;
 document.querySelectorAll('[data-method]').forEach(function(c){c.checked=!VIEW.hidden[c.dataset.method];});
 function rememberDetailState(){
@@ -1901,7 +2019,7 @@ var SAVE_LEGACY=false;   /* 讀入 v1 舊存檔時為 true，區段標示「舊
 $('ovZiweiMode').onchange=function(){ STATE.zmode=$('ovZiweiMode').value; renderOverlay(); };
 
 /* ============================================================
-   存檔／讀取（契約 xiaoliu.hebing-save/v2；v1 唯讀相容）
+   存檔／讀取（契約 xiaoliu.hebing-save/v3；v1／v2 唯讀相容，讀檔一律以引擎重算）
    v0.9.28 起升 v2：可信度語意由「最強支持線分數」改為「支持減反向」，
    band 新增 pairs.bazi.reliability / scoreRaw / oppositionRaw，
    並新增 wealthResonance 財運訊號快照。讀到 v1 舊檔時照原值呈現、
@@ -1940,7 +2058,7 @@ $('f-save').onclick=function(){
   var reso=resonance(buildSeries());
   var anchored=buildSyncSnapshot();
   var payload={
-    schema:'xiaoliu.hebing-save/v2',
+    schema:'xiaoliu.hebing-save/v3',
     savedAt:new Date().toISOString(),
     input:currentInput(),
     zmode:STATE.zmode,
@@ -1953,7 +2071,8 @@ $('f-save').onclick=function(){
                       syncAlerts:anchored, taiyiAnchoredZones:anchored,
                       turningAnalysis:buildTurningSnapshot(),
                       annualExtrema:buildAnnualExtremaSnapshot(),
-                      wealthResonance:buildWealthSnapshot() }:null
+                      wealthResonance:buildWealthSnapshot(),
+                      wealthTuixiang:buildWealthTuixiang() }:null
   };
   download('hebing-'+safeName(payload.input.name)+'-'+nowStamp()+'.json',
     new Blob([JSON.stringify(payload,null,2)],{type:'application/json'}));
@@ -1966,7 +2085,7 @@ $('f-load-file').onchange=function(ev){
   reader.onload=function(){
     try{
       var p=JSON.parse(reader.result);
-      if(p.schema!=='xiaoliu.hebing-save/v2'&&p.schema!=='xiaoliu.hebing-save/v1')
+      if(p.schema!=='xiaoliu.hebing-save/v3'&&p.schema!=='xiaoliu.hebing-save/v2'&&p.schema!=='xiaoliu.hebing-save/v1')
         throw new Error('不是合盤存檔（schema 不符）');
       SAVE_LEGACY=(p.schema==='xiaoliu.hebing-save/v1');
       var i=p.input;
@@ -2162,15 +2281,15 @@ function reportBandLine(b,mode,up){
   return line;
 }
 function reportBandReading(mode,up,b){
-  if(b.grade==='conflict') return '太乙訊號與八字、紫微均有明顯反向證據；保留太乙觀察，但不把本段當作三法共同結論。';
-  var trust=b.grade==='A'?'三法證據完整重疊，可優先列為高可信線索。':
-    (b.grade==='B'?'太乙獲八字或紫微其中一法完整支持，可列為重要線索。':'目前只有方向或位置等部分支持，宜先觀察、不宜單獨重押。');
+  if(b.grade==='conflict') return '太乙訊號與八字＋紫微曲線明顯反向；保留太乙觀察，但不把本段當作共同結論。';
+  var trust=b.grade==='A'?'太乙與八字＋紫微證據完整重疊，可優先列為高可信線索。':
+    (b.grade==='B'?'（舊檔口徑）至少一法完整支持，可列為重要線索。':'目前只有方向或位置等部分支持，宜先觀察、不宜單獨重押。');
   var action=up?'順勢規劃仍應分段驗證並保留彈性。':'宜放寬時程、分散風險並預留時間與資源緩衝。';
   return trust+action;
 }
 function reportSyncCopy(mode,up){
   if(mode==='daen') return up
-    ?{title:'運勢上升強旺期',remind:'代表整體運勢逐步往上、力量漸強；八字、紫微再依方向、位置與轉折提供佐證。'}
+    ?{title:'運勢上升強旺期',remind:'代表整體運勢逐步往上、力量漸強；八字＋紫微曲線再依方向、位置與轉折提供佐證。'}
     :{title:'運勢走弱低潮期',remind:'代表整體運勢逐步走弱、較容易面臨壓力與低潮；宜穩健調整並預留緩衝。'};
   return up
     ?{title:'連續流年形成的強旺帶',remind:'代表連續多個未來年份位於較有利、能量較強的位置；適合把握機會並分段推進。'}
@@ -2234,7 +2353,7 @@ function drawReportExtremaColumn(ctx,x,y,w,h,kind,rows){
   ctx.beginPath();ctx.moveTo(x+26,y+66);ctx.lineTo(x+w-26,y+66);ctx.stroke();
   if(!rows||!rows.length){
     reportFont(ctx,22,'400','#8a8071');
-    reportText(ctx,'共同資料範圍內沒有八字或紫微呼應的'+(peak?'高點':'低點')+'。',x+26,y+92,w-52,30,3);
+    reportText(ctx,'共同資料範圍內沒有八字＋紫微呼應的'+(peak?'高點':'低點')+'。',x+26,y+92,w-52,30,3);
     return;
   }
   var rowH=(h-82)/Math.max(6,rows.length),cy=y+82;
@@ -2245,8 +2364,7 @@ function drawReportExtremaColumn(ctx,x,y,w,h,kind,rows){
       reportExtremaSignalText(row.specialSignals);
     var ty=reportText(ctx,source,x+26,cy,w-52,24,2)+3;
     reportFont(ctx,17,'400','#665f55');
-    if(row.bazi) ty=reportText(ctx,reportExtremaMatchLine('八字',row.bazi),x+48,ty,w-74,23,1)+1;
-    if(row.ziwei) reportText(ctx,reportExtremaMatchLine('紫微',row.ziwei),x+48,ty,w-74,23,1);
+    if(row.hebing) reportText(ctx,reportExtremaMatchLine(HEBING_NAME,row.hebing),x+48,ty,w-74,23,1);
     cy+=rowH;
   });
 }
@@ -2255,7 +2373,7 @@ function drawReportExtremaPage(snapshot,pageNo){
   var ctx=cv.getContext('2d');
   reportFont(ctx,21,'400','#516b55');ctx.fillText(reportPersonLine(),70,174);
   reportFont(ctx,19,'400','#665f55');
-  reportText(ctx,'以太乙最高／最低各 6 個局部點為錨，只列八字或紫微在前後 1 年內出現同類高低點的年份；'+
+  reportText(ctx,'以太乙最高／最低各 6 個局部點為錨，只列八字＋紫微流年曲線在前後 1 年內出現同類高低點的年份；'+
     '佐證高點須在高檔、低點須在低檔。入選後依年齡排列；共同年份 '+snapshot.commonRange.y0+'–'+snapshot.commonRange.y1+'，連續同值平台只算 1 點。',70,211,1544,27,2);
   drawReportExtremaColumn(ctx,70,278,752,815,'peak',snapshot.peaks);
   drawReportExtremaColumn(ctx,862,278,752,815,'trough',snapshot.troughs);
@@ -2284,14 +2402,13 @@ function reportTurnMatch(m){
 function drawReportTurningPage(mode,snap,pageNo){
   var daen=mode==='daen';
   var cv=reportCanvas('轉折與領先／落後分析｜'+(daen?'大限':'流年'),'八字 × 紫微 × 太乙｜三法合盤曲線報告',pageNo);
-  var ctx=cv.getContext('2d'),bp=snap.pairs.bazi,zp=snap.pairs.ziwei;
-  var fit=snap.comparison==='close'?'太乙與八字、紫微貼合度接近':(snap.comparison==='bazi'?'太乙目前較貼近八字':'太乙目前較貼近紫微');
+  var ctx=cv.getContext('2d'),hp=snap.pairs.hebing;
+  var fit='太乙 × '+HEBING_NAME+'（五行主線曲線）綜合貼合度 '+hp.fitScore;
   reportFont(ctx,22,'400','#665f55');
-  reportText(ctx,fit+'。配對差＝另一法轉折年－太乙轉折年；正值代表太乙領先，±1 年歸為同期。'+
+  reportText(ctx,fit+'。配對差＝八字＋紫微轉折年－太乙轉折年；正值代表太乙領先，±1 年歸為同期。'+
     (daen?'大限為粗顆粒節點，領先年數只作區段近似。':'流年先做三年移動平均，並要求轉向至少維持兩年。'),70,178,1544,31,2);
-  drawReportTurningPair(ctx,70,250,752,'八字',bp);
-  drawReportTurningPair(ctx,862,250,752,'紫微',zp);
-  var x0=70,y0=500,widths=[255,170,520,529],heads=['太乙轉折','類型','八字對應','紫微對應'];
+  drawReportTurningPair(ctx,70,250,1544,HEBING_NAME,hp);
+  var x0=70,y0=500,widths=[355,240,949],heads=['太乙轉折','類型',HEBING_NAME+'對應'];
   ctx.fillStyle='#516b55';ctx.fillRect(x0,y0,1544,42);
   var xx=x0;
   heads.forEach(function(h,i){reportFont(ctx,20,'600','#fff');ctx.fillText(h,xx+12,y0+9);xx+=widths[i];});
@@ -2300,7 +2417,7 @@ function drawReportTurningPage(mode,snap,pageNo){
     var yy=y0+42+i*rowH;
     ctx.fillStyle=i%2?'#fffdf8':'#f7f3e9';ctx.fillRect(x0,yy,1544,rowH);
     reportFont(ctx,18,'400','#4e5b51');
-    var vals=[fmtYear(r.year)+'｜實歲 '+fmtYear(r.realAge)+'／虛歲 '+fmtYear(r.virtualAge),r.label,reportTurnMatch(r.bazi),reportTurnMatch(r.ziwei)];
+    var vals=[fmtYear(r.year)+'｜實歲 '+fmtYear(r.realAge)+'／虛歲 '+fmtYear(r.virtualAge),r.label,reportTurnMatch(r.hebing)];
     var px=x0;vals.forEach(function(v,k){reportText(ctx,v,px+12,yy+12,widths[k]-22,24,1);px+=widths[k];});
   });
   if(snap.rows.length>rows.length){
@@ -2380,5 +2497,6 @@ window.__hebingDiagnostics=Object.freeze({
 <script src="solar-lunar.js"></script>
 <script src="qimen/qimen-engine.js"></script>
 <script src="moment.js"></script>
+<script src="wuxing-mainline.js" onload="window.__wuxingLoaded&&window.__wuxingLoaded()"></script>
 </body>
 </html>
diff --git a/research-ui.css b/research-ui.css
index 3c75ae8..90bc9a2 100644
--- a/research-ui.css
+++ b/research-ui.css
@@ -30,7 +30,7 @@ button:focus-visible,input:focus-visible,select:focus-visible,summary:focus-visi
 .toolbar-group button:hover{border-color:var(--ink);}.toolbar-group button[aria-pressed=true]{background:var(--ink);color:white;border-color:var(--ink);}
 .toolbar-group input[type=number]{font:inherit;font-size:1rem;width:88px;min-height:38px;border:1px solid #c8bda6;border-radius:6px;padding:4px 8px;background:#fff;}
 .method-switches label{padding:3px 9px;border-radius:5px;background:#fffdf7;}
-.method-bazi{border-bottom:3px solid #b99b62;}.method-ziwei{border-bottom:3px solid #6b5b8f;}.method-taiyi{border-bottom:3px solid #8d6e63;}
+.method-bazi{border-bottom:3px solid #b99b62;}.method-ziwei{border-bottom:3px solid #6b5b8f;}.method-taiyi{border-bottom:3px solid #8d6e63;}.method-hebing{border-bottom:3px solid #2f7f8f;}
 .year-picker{padding-top:9px;border-top:1px solid var(--line);}
 #viewNotice{font-size:.875rem;color:var(--muted);margin-left:4px;}
 #ovChart{overflow-x:auto;overscroll-behavior-x:contain;}
@@ -52,6 +52,7 @@ button:focus-visible,input:focus-visible,select:focus-visible,summary:focus-visi
 .sync-col,.turn-pair{font-size:.9375rem;padding:14px;min-width:0;}
 .sync-col ol{padding:0;list-style:none;}.sync-col li{border-top:1px solid var(--line);padding:12px 0;margin:0;}
 .anchor-line{display:grid;grid-template-columns:minmax(100px,auto) 1fr;gap:8px 12px;align-items:center;}
+.anchor-line>.anchor-rel{grid-column:1/-1;white-space:normal;}
 .anchor-period{font-size:.875rem;}.anchor-grade{font-size:.8125rem;justify-self:start;padding:3px 10px;}
 .anchor-rel{font-size:.875rem;color:var(--muted);}.anchor-score{font-size:.875rem;justify-self:end;color:var(--ink);}
 .anchor-score strong{font-size:1.35rem;}.anchor-legend{font-size:.8125rem;}.sync-remind{font-size:.875rem;}
diff --git a/test-parent.js b/test-parent.js
index fbe15a5..9b8f7a0 100644
--- a/test-parent.js
+++ b/test-parent.js
@@ -71,12 +71,19 @@ const countPoly = svg => (svg.match(/<polyline/g) || []).length;
   for (let i = 0; i < 100 && !/svg/.test(doc.getElementById('ovChart').innerHTML); i++) await sleep(100);
   ok(!doc.getElementById('ovPng').disabled && !doc.getElementById('ovSvg').disabled, '排盤後輸出鈕解鎖');
 
-  // 等太乙 ext-curve 掛上（第三條線）
-  for (let i = 0; i < 100 && countPoly(doc.getElementById('ovChart').innerHTML) < 3; i++) await sleep(100);
+  // 等太乙 ext-curve 與五行主線模組掛上（圖例含限例太乙、共振卡 6 組配對）
+  for (let i = 0; i < 100 && !(/限例太乙（十二宮歲段/.test(doc.getElementById('ovLegend').textContent) && /八字＋紫微（五行主線） × 限例太乙/.test(doc.getElementById('resoGrid').textContent)); i++) await sleep(100);
+  /* v0.9.34 預設顯示：八字＋紫微（五行主線曲線）與太乙；八字、紫微單線預設關閉 */
+  const toggles = [...doc.querySelectorAll('[data-method]')];
+  ok(toggles.map(c => c.dataset.method).join(',') === 'bazi,ziwei,hebing,taiyi', '顯示曲線勾選項：八字／紫微／八字＋紫微／太乙');
+  ok(toggles.map(c => c.checked).join(',') === 'false,false,true,true', '預設開啟八字＋紫微與太乙、關閉八字與紫微');
+  ok(countPoly(doc.getElementById('ovChart').innerHTML) === 2, '預設圖上兩條線（五行主線曲線＋限例太乙）（實得 ' + countPoly(doc.getElementById('ovChart').innerHTML) + '）');
+  toggles.forEach(c => { if (!c.checked) c.click(); });
+  await sleep(300);
   let svg = doc.getElementById('ovChart').innerHTML;
   ok(!doc.getElementById('ovPdf').disabled, '大限與流年三法核心線齊備後 PDF 報告按鈕解鎖');
   ok(/<svg/.test(svg), '重疊圖 SVG 已產生');
-  ok(countPoly(svg) === 3, '大限合盤：八字大運＋紫微大限＋限例太乙 三條 polyline（實得 ' + countPoly(svg) + '）');
+  ok(countPoly(svg) === 4, '大限合盤全開：八字大運＋紫微大限＋五行主線曲線＋限例太乙 四條 polyline（實得 ' + countPoly(svg) + '）');
   ok(/#7d5a44/.test(svg), '限例太乙棕線在圖上');
   ok(/限例太乙（十二宮歲段/.test(doc.getElementById('ovLegend').textContent), '圖例含限例太乙說明與線色');
   ok(!/#46708a/.test(svg), '行年趨勢藍線不在大限層（level 過濾生效）');
@@ -91,7 +98,7 @@ const countPoly = svg => (svg.match(/<polyline/g) || []).length;
   ok(resoCard.style.display !== 'none', '共振分析卡顯示');
   const resoTxt = doc.getElementById('resoGrid').textContent;
   ok(/同向率/.test(resoTxt) && /形狀相似度/.test(resoTxt), '同向率與形狀相似度存在：' + resoTxt.replace(/\s+/g, ' ').slice(0, 110));
-  ok((resoTxt.match(/同向率/g) || []).length === 3 && /限例太乙/.test(resoTxt), '大限合盤三序列 3 組配對（含限例太乙）');
+  ok((resoTxt.match(/同向率/g) || []).length === 1 && /八字＋紫微（五行主線） × 限例太乙/.test(resoTxt), '共振卡只顯示核心配對：八字＋紫微 × 限例太乙（v0.9.35）');
   const rMatch = resoTxt.match(/形狀相似度 (-?\d\.\d\d)/);
   ok(!!rMatch && Math.abs(parseFloat(rMatch[1])) <= 1, 'Pearson r 在 [−1,1]（r=' + (rMatch ? rMatch[1] : '?') + '）');
   ok(/共高帶/.test(resoTxt) && /共同功課帶/.test(resoTxt), '共振帶清單存在');
@@ -103,12 +110,12 @@ const countPoly = svg => (svg.match(/<polyline/g) || []).length;
   const turnCard = doc.getElementById('turnCard');
   const turnModes = [...doc.querySelectorAll('#turnGrid .turn-mode')];
   ok(turnCard.style.display !== 'none' && turnModes.length === 2 && /大限轉折關係/.test(turnModes[0].textContent) && /流年轉折關係/.test(turnModes[1].textContent), '轉折卡同時呈現大限與流年兩層級');
-  ok((turnCard.textContent.match(/太乙 × 八字/g)||[]).length >= 2 && (turnCard.textContent.match(/太乙 × 紫微/g)||[]).length >= 2, '大限／流年皆分別比較太乙 × 八字與太乙 × 紫微');
+  ok((turnCard.textContent.match(/太乙 × 八字＋紫微/g)||[]).length >= 2 && (turnCard.textContent.match(/太乙 × 紫微/g)||[]).length === 0 && (turnCard.textContent.match(/太乙 × 八字[^＋]/g)||[]).length === 0, '大限／流年皆只比較太乙 × 八字＋紫微，不再分列八字、紫微（v0.9.35）');
   ok(/轉折吻合/.test(turnCard.textContent) && /最佳時間差相關/.test(turnCard.textContent) && /方向一致率/.test(turnCard.textContent) && /綜合貼合度/.test(turnCard.textContent), '卡片顯示轉折吻合、最佳時間差、方向一致率與綜合貼合度');
   ok(/三年移動平均/.test(turnModes[1].textContent) && /同方向配對窗 ±2 年/.test(turnModes[1].textContent) && /±1 年視為同期/.test(turnModes[1].textContent), '流年卡明示平滑、配對窗與同期容許值');
   const turnSnapshotActual = win.__hebingDiagnostics.buildTurningSnapshot();
   ok(turnSnapshotActual && turnSnapshotActual.version === 'turning-leadlag/v1' && turnSnapshotActual.daen.rows.length > 0 && turnSnapshotActual.year.rows.length > 0, '溫韻華命例產生大限／流年太乙轉折明細');
-  ok(turnSnapshotActual && turnSnapshotActual.daen.comparison === 'bazi' && turnSnapshotActual.year.comparison === 'bazi', '溫韻華命例初版門檻下，大限與流年皆判為太乙較貼近八字');
+  ok(turnSnapshotActual && turnSnapshotActual.daen.comparison === 'hebing' && turnSnapshotActual.year.comparison === 'hebing' && turnSnapshotActual.daen.pairs.hebing && !turnSnapshotActual.daen.pairs.bazi, '轉折快照只含 hebing 配對（v0.9.35）');
   ok([...turnCard.querySelectorAll('.turn-table tbody tr')].every(tr => /轉強|轉弱|未辨識/.test(tr.textContent)), '轉折明細逐筆標示轉強／轉弱與兩法配對');
 
   // 太乙錨定可信區：大限斜率主判、流年未來位置主判；方向／位置／轉折分級，兩層級上下各最多 6 組。
@@ -145,12 +152,12 @@ const countPoly = svg => (svg.match(/<polyline/g) || []).length;
     ok(/西元 1994–2008/.test(daDownText) && /西元 2013–2017/.test(daDownText),
        '溫韻華命例仍抓出原太乙下行連續區段');
     const daUpText = [...syncModes[0].querySelectorAll('.sync-col.up li')].map(li => li.textContent).join(' ');
-    ok(/B 雙法支持.*西元 2044–2052.*可信度 100%/.test(daUpText),
-       '溫韻華 2044–2052 以精簡格式列為太乙上行 B 級、可信度 100%');
+    ok(/A 強共識.*西元 1988–1993/.test(daUpText) && !/B 雙法支持/.test(daUpText+daDownText),
+       '溫韻華 1988–1993 列為太乙上行 A 強共識（八字＋紫微單一佐證線，無 B 級）');
     const anchorSnap = win.__hebingDiagnostics.buildSyncSnapshot();
     const wenUp = anchorSnap.daen.up.find(b => b.y0 === 2044 && b.y1 === 2052);
-    ok(wenUp && wenUp.pairs.bazi.score === 100 && wenUp.supporters.includes('八字'),
-       '畫面雖精簡，JSON／診斷快照仍保留八字支持分與支持線');
+    ok(wenUp && wenUp.pairs.hebing && typeof wenUp.pairs.hebing.score === 'number' && !wenUp.pairs.bazi,
+       '畫面雖精簡，JSON／診斷快照仍保留八字＋紫微支持分（pairs.hebing），不再有 pairs.bazi');
     const nowYear = new win.Date().getFullYear();
     const yrItems = [...syncModes[1].querySelectorAll('.sync-col li')];
     ok(yrItems.every(li => { const m=li.textContent.match(/西元 (\d{4})/); return m && +m[1] >= nowYear; }),
@@ -166,119 +173,94 @@ const countPoly = svg => (svg.match(/<polyline/g) || []).length;
        '每個太乙高低點皆標示年份、實歲與虛歲');
     const extremaActual = win.__hebingDiagnostics.buildAnnualExtremaSnapshot();
     const actualRows = extremaActual ? extremaActual.peaks.concat(extremaActual.troughs) : [];
-    ok(extremaActual && extremaActual.version === 'annual-extrema-echo/v5' && extremaActual.matchWindow === 1 && extremaActual.limit === 6 &&
+    ok(extremaActual && extremaActual.version === 'annual-extrema-echo/v6-hebing' && extremaActual.matchWindow === 1 && extremaActual.limit === 6 &&
        extremaActual.displayRule === 'at-least-one-qualified-support-match' &&
        extremaActual.positionThreshold.peak === .1 && extremaActual.positionThreshold.trough === -.1 && actualRows.length === peakRows.length + troughRows.length,
-       '高低點診斷快照使用 ±1 年規則，筆數與畫面一致');
-    ok(actualRows.every(row => [row.bazi,row.ziwei].every(m => !m || Math.abs(m.lag) <= 1)),
-       '八字／紫微的所有命中年份皆落在太乙點前後 1 年內');
-    ok(actualRows.every(row => row.bazi || row.ziwei) && peakRows.concat(troughRows).every(row => !/兩法皆未呼應/.test(row.textContent)),
-       '只保留八字或紫微至少一法有呼應的太乙點，兩法皆未呼應者不顯示');
+       '高低點診斷快照使用 ±1 年規則（v6 單一佐證線），筆數與畫面一致');
+    ok(actualRows.every(row => !row.hebing || Math.abs(row.hebing.lag) <= 1),
+       '八字＋紫微的所有命中年份皆落在太乙點前後 1 年內');
+    ok(actualRows.every(row => row.hebing && !row.bazi && !row.ziwei),
+       '只保留八字＋紫微有呼應的太乙點；快照不再有 bazi／ziwei 欄位（v0.9.35）');
     const extremaDomRows=peakRows.concat(troughRows);
-    ok(extremaDomRows.every((el,i) => el.querySelectorAll('.annual-extrema-method').length === [actualRows[i].bazi,actualRows[i].ziwei].filter(Boolean).length),
-       '八字與紫微各自分行，且只建立實際有呼應的方法列');
-    ok(extremaDomRows.every(el => [...el.querySelectorAll('.annual-extrema-method')].every(line => /^(八字|紫微) /.test(line.textContent.trim()))),
-       '每一條呼應資訊只呈現單一命法與其年份');
-    ok(!/皆有呼應|八字有呼應|紫微有呼應|無同類高點|無同類低點/.test(extremaBox.textContent),
+    ok(extremaDomRows.every(el => el.querySelectorAll('.annual-extrema-method').length === 1 && /^八字＋紫微 /.test(el.querySelector('.annual-extrema-method').textContent.trim())),
+       '每個太乙點只有一列「八字＋紫微」呼應資訊');
+    ok(!/皆有呼應|無同類高點|無同類低點|兩法/.test(extremaBox.textContent),
        '畫面移除呼應總結及未命中方法的說明文字');
-    const singleMatchIndex=actualRows.findIndex(row => Boolean(row.bazi) !== Boolean(row.ziwei));
-    ok(singleMatchIndex >= 0 && extremaDomRows[singleMatchIndex].querySelectorAll('.annual-extrema-method').length === 1 &&
-       (actualRows[singleMatchIndex].bazi ? !/紫微 /.test(extremaDomRows[singleMatchIndex].textContent) : !/八字 /.test(extremaDomRows[singleMatchIndex].textContent)),
-       '只有一法呼應時，只顯示該法資訊');
     const peakYears=extremaActual.peaks.map(row => row.year),troughYears=extremaActual.troughs.map(row => row.year);
     ok(peakYears.every((year,i) => i === 0 || peakYears[i-1] <= year) &&
        troughYears.every((year,i) => i === 0 || troughYears[i-1] <= year),
        '入選的太乙高點與低點皆依年份／年齡由小到大排列');
-    ok(!extremaActual.troughs.some(row => row.year === 1984 || row.year === 1986),
-       '溫韻華 1984、1986 太乙低點因八字與紫微皆未呼應而隱藏');
-    ok(actualRows.every(row => !row.bazi || (extremaActual.peaks.includes(row) ? row.bazi.value > .1 : row.bazi.value < -.1)) &&
-       actualRows.every(row => !row.ziwei || (extremaActual.peaks.includes(row) ? row.ziwei.value > .1 : row.ziwei.value < -.1)),
+    ok(actualRows.every(row => extremaActual.peaks.includes(row) ? row.hebing.value > .1 : row.hebing.value < -.1),
        '佐證線高點皆在 +0.1 以上，低點皆在 −0.1 以下');
-    const peak2026=extremaActual.peaks.find(row => row.year === 2026);
-    ok(peak2026 && peak2026.bazi === null && peak2026.ziwei && peak2026.ziwei.year === 2027,
-       '溫韻華 2026 太乙高點排除同年八字低檔反彈 −5，八字 2028 因差 2 年亦排除，只保留紫微 2027');
     ok(extremaActual.specialYearRule === 'taiyi-engine-virtual-age-overlap-on-source-and-matched-support-years' &&
        extremaActual.specialYears.luMaThreeWay.includes(62) && extremaActual.specialYears.luMaTwoWay.includes(48) &&
        extremaActual.specialYears.luZhuAndFeiLu.includes(49) && extremaActual.specialYears.luZhuAndFeiMa.includes(46),
        '高低點快照直接帶入太乙引擎的祿馬交馳、祿主＋飛祿與祿主＋飛馬歲數');
-    // v0.9.29 曆書延至 2100：2051 年後高點參賽，2024 被擠出前 6 組（規則「各取 6 組」待 Nakai 裁定是否上調／限窗）
-    ok(extremaActual.peaks.some(row => row.year > 2050),
-       '溫韻華 太乙高點名單含 2051 年後年份（曆書延長後參賽）');
-    const peak2024=extremaActual.peaks.find(row => row.year === 2024);
-    ok(!peak2024 || (peak2024.specialSignals.some(s => s.key === 'luZhuAndFeiLu' && s.age === 49) &&
-       peak2024.bazi && peak2024.bazi.year === 2023 && peak2024.bazi.specialSignals.some(s => s.key === 'luMaTwoWay' && s.age === 48)),
-       '溫韻華 2024 若入選則標記祿主＋飛祿、八字配對 2023 祿馬雙合（v0.9.29 起可能被後段高點擠出）');
     const specialChips=[...extremaBox.querySelectorAll('.extrema-signal')].map(el => el.textContent.trim());
-    ok(specialChips.includes('祿主飛祿') && specialChips.includes('祿馬交馳') &&
-       specialChips.every(text => text === '祿馬交馳' || text === '祿主飛祿'),
+    ok(specialChips.every(text => text === '祿馬交馳' || text === '祿主飛祿'),
        '畫面特殊年份只使用「祿馬交馳／祿主飛祿」兩種簡短標籤');
     ok([...extremaBox.querySelectorAll('.extrema-signal')].every(el =>
        !/太乙|八字|紫微|虛歲|事業財富|三合|雙合|開拓升遷/.test(el.textContent)),
        '標籤正文移除命法、歲數、三合／雙合及長描述，完整資訊只留在提示與 JSON');
   }
 
-  // 獨立規則測試：數值即使仍為正，只要太乙與任一法負斜率就成立；搭配線可連續切換。
+  // 獨立規則測試（v0.9.35 兩線）：數值即使仍為正，只要太乙與八字＋紫微同為負斜率就成立；太乙轉上即不成立。
   const syntheticDown = win.__hebingDiagnostics.slopeOverlapBands('daen', [
-    { id:'bazi', pts:[{x:2000,v:.8},{x:2002,v:.4},{x:2004,v:.6},{x:2005,v:.4}] },
-    { id:'ziwei-daen', pts:[{x:2000,v:.2},{x:2002,v:.5},{x:2004,v:.2},{x:2005,v:0}] },
+    { id:'wuxing-line', pts:[{x:2000,v:.8},{x:2002,v:.4},{x:2004,v:.2},{x:2005,v:0}] },
     { id:'taiyi-xianli', pts:[{x:2000,v:.9},{x:2004,v:.5},{x:2005,v:.8}] }
   ], 'lo');
   ok(syntheticDown.length === 1 && syntheticDown[0].y0 === 2000 && syntheticDown[0].y1 === 2004,
-     '曲線仍在中線上方也能依負斜率成立；太乙轉上時即使另兩線向下也不成立');
-  ok(syntheticDown[0] && syntheticDown[0].companions.join('、') === '八字、紫微',
-     '連續區段搭配線由八字切換紫微時合併並保留兩者標記');
+     '曲線仍在中線上方也能依負斜率成立；太乙轉上時即使八字＋紫微向下也不成立');
+  ok(syntheticDown[0] && syntheticDown[0].companions.join('、') === '八字＋紫微',
+     '下行區段搭配線標記為八字＋紫微');
   ok(syntheticDown[0] && syntheticDown[0].strength > 0 && syntheticDown[0].slopeStrength > 0 && syntheticDown[0].trendStart === 2000 && syntheticDown[0].trendEnd === 2004,
      '下行資料保留共同下降總幅度、平均斜率與精確趨勢起訖');
   const syntheticSnapshot = win.__hebingDiagnostics.syncSnapshotRows(syntheticDown, 1980);
-  ok(syntheticSnapshot[0] && syntheticSnapshot[0].companions.length === 2,
+  ok(syntheticSnapshot[0] && syntheticSnapshot[0].companions.length === 1,
      '同步區段快照保留太乙搭配線 companions');
   const syntheticUp = win.__hebingDiagnostics.slopeOverlapBands('daen', [
-    { id:'bazi', pts:[{x:2000,v:.1},{x:2004,v:.5}] },
-    { id:'ziwei-daen', pts:[{x:2000,v:.2},{x:2004,v:.6}] },
+    { id:'wuxing-line', pts:[{x:2000,v:.1},{x:2004,v:.5}] },
     { id:'taiyi-xianli', pts:[{x:2000,v:.3},{x:2004,v:.7}] }
   ], 'hi');
   ok(syntheticUp.length === 1 && syntheticUp[0].kind === 'hi' && syntheticUp[0].basis === 'slope',
-     '大限三線皆為正斜率時形成同步往上區段');
+     '大限兩線皆為正斜率時形成同步往上區段');
 
   const syntheticFallbackUp = win.__hebingDiagnostics.daenPositionFallbackBands([
-    { id:'bazi', pts:[{x:2000,v:.4},{x:2004,v:.8}] },
-    { id:'ziwei-daen', pts:[{x:2000,v:.8},{x:2004,v:.4}] },
+    { id:'wuxing-line', pts:[{x:2000,v:.8},{x:2004,v:.4}] },
     { id:'taiyi-xianli', pts:[{x:2000,v:.5},{x:2004,v:.9}] }
   ], 'hi');
-  ok(syntheticFallbackUp.length === 1 && syntheticFallbackUp[0].basis === 'position-fallback' && syntheticFallbackUp[0].nearSlopeCount === 2,
-     '大限上升只差一線斜率時，三線位置皆高可列為位置補判');
+  ok(syntheticFallbackUp.length === 1 && syntheticFallbackUp[0].basis === 'position-fallback' && syntheticFallbackUp[0].nearSlopeCount === 1,
+     '大限上升只有太乙上行時，兩線位置皆高可列為位置補判');
   const syntheticFallbackDown = win.__hebingDiagnostics.daenPositionFallbackBands([
-    { id:'bazi', pts:[{x:2000,v:-.8},{x:2004,v:-.6}] },
-    { id:'ziwei-daen', pts:[{x:2000,v:-.3},{x:2004,v:-.7}] },
-    { id:'taiyi-xianli', pts:[{x:2000,v:-.9},{x:2004,v:-.5}] }
+    { id:'wuxing-line', pts:[{x:2000,v:-.8},{x:2004,v:-.6}] },
+    { id:'taiyi-xianli', pts:[{x:2000,v:-.5},{x:2004,v:-.9}] }
   ], 'lo');
-  ok(syntheticFallbackDown.length === 1 && syntheticFallbackDown[0].basis === 'position-fallback' && syntheticFallbackDown[0].companions.includes('紫微'),
-     '大限下降斜率近似未符時，太乙與任一法同在低檔可列為位置補判');
+  ok(syntheticFallbackDown.length === 1 && syntheticFallbackDown[0].basis === 'position-fallback' && syntheticFallbackDown[0].companions.includes('八字＋紫微'),
+     '大限下降斜率未符時，太乙與八字＋紫微同在低檔可列為位置補判');
 
   // 流年位置測試：負值即使逐年回升，仍屬同一個下方區段；過去年份須排除。
   const syntheticFuture = win.__hebingDiagnostics.futurePositionBands('year', [
-    { id:'bazi-annual', pts:[{x:2029,v:-.8},{x:2030,v:-.7},{x:2031,v:-.6},{x:2032,v:-.5},{x:2033,v:-.4}] },
-    { id:'ziwei-year', pts:[{x:2029,v:.4},{x:2030,v:.4},{x:2031,v:.4},{x:2032,v:.4},{x:2033,v:.4}] },
+    { id:'wuxing-year', pts:[{x:2029,v:-.8},{x:2030,v:-.7},{x:2031,v:-.6},{x:2032,v:-.5},{x:2033,v:-.4}] },
     { id:'taiyi-xingnian', pts:[{x:2029,v:-.9},{x:2030,v:-.8},{x:2031,v:-.7},{x:2032,v:-.6},{x:2033,v:-.5}] }
   ], 2030);
   ok(syntheticFuture.length === 1 && syntheticFuture[0].kind === 'lo' && syntheticFuture[0].y0 === 2030 && syntheticFuture[0].y1 === 2033,
      '流年依位置把連續下方年份合併成一組，不拆成多個單年點，並排除未來起點之前');
 
-  // 太乙錨定分級：A／B／C／衝突門檻，以及流年連續至少兩年。
+  // 太乙錨定分級（v0.9.35 單一佐證線）：A／C／衝突門檻，以及流年連續至少兩年。
   const grade = win.__hebingDiagnostics.anchorBandGrade;
-  ok(grade({score:80,opposition:0},{score:75,opposition:0}) === 'A' &&
-     grade({score:75,opposition:0},{score:20,opposition:20}) === 'B' &&
-     grade({score:55,opposition:10},{score:10,opposition:20}) === 'C' &&
-     grade({score:20,opposition:50},{score:10,opposition:60}) === 'conflict',
-     '太乙錨定 A／B／C／衝突分級門檻成立');
+  ok(grade({score:80,opposition:0}) === 'A' &&
+     grade({score:75,opposition:40}) === 'C' &&
+     grade({score:55,opposition:10}) === 'C' &&
+     grade({score:20,opposition:50}) === 'conflict' &&
+     grade({score:20,opposition:10}) === null,
+     '太乙錨定 A／C／衝突分級門檻成立（無 B 級）');
   const anchorSynthetic = win.__hebingDiagnostics.buildAnchoredBands('year', [
-    {id:'bazi-annual',pts:[{x:2029,v:.4},{x:2030,v:.5},{x:2031,v:.6},{x:2032,v:.7},{x:2033,v:.8}]},
-    {id:'ziwei-year',pts:[{x:2029,v:.3},{x:2030,v:.4},{x:2031,v:.5},{x:2032,v:.6},{x:2033,v:.7}]},
+    {id:'wuxing-year',pts:[{x:2029,v:.4},{x:2030,v:.5},{x:2031,v:.6},{x:2032,v:.7},{x:2033,v:.8}]},
     {id:'taiyi-xingnian',pts:[{x:2029,v:.5},{x:2030,v:.6},{x:2031,v:.7},{x:2032,v:.8},{x:2033,v:.9}]}
   ],2030);
   ok(anchorSynthetic.length === 1 && anchorSynthetic[0].y0 === 2030 && anchorSynthetic[0].y1 === 2033 && anchorSynthetic[0].grade === 'A' &&
-     anchorSynthetic[0].pairs.bazi.score >= 70 && anchorSynthetic[0].pairs.ziwei.score >= 70,
-     '流年太乙高檔連續區合併，八字與紫微皆完整支持時列 A 級');
+     anchorSynthetic[0].pairs.hebing.score >= 70 && !anchorSynthetic[0].pairs.bazi,
+     '流年太乙高檔連續區合併，八字＋紫微完整支持時列 A 級');
   const rankedThenChronological=win.__hebingDiagnostics.pickAnchoredBands([
     {kind:'up',grade:'C',confidence:99,y0:1990,y1:1995},
     {kind:'up',grade:'A',confidence:80,y0:2030,y1:2035},
@@ -354,13 +336,13 @@ const countPoly = svg => (svg.match(/<polyline/g) || []).length;
   doc.getElementById('ovZiweiMode').onchange();
   await sleep(100);
   svg = doc.getElementById('ovChart').innerHTML;
-  ok(countPoly(svg) === 3, '流年合盤：八字流年＋紫微逐年＋太乙行年 三條 polyline（實得 ' + countPoly(svg) + '）');
+  ok(countPoly(svg) === 4, '流年合盤（全開）：八字流年＋紫微逐年＋八字＋紫微流年曲線＋太乙行年 四條 polyline（實得 ' + countPoly(svg) + '）');
   ok(/#46708a/.test(svg) && !/#7d5a44/.test(svg), '行年藍線進場、限例棕線退場（level 過濾生效）');
   ok(/太乙行年趨勢（虛歲逐歲/.test(doc.getElementById('ovLegend').textContent)
      && !/限例太乙（十二宮歲段/.test(doc.getElementById('ovLegend').textContent), '圖例隨層級切換太乙線說明');
   const resoTxtY = doc.getElementById('resoGrid').textContent;
-  ok(/八字流年 × 紫微逐年能量/.test(resoTxtY), '共振卡含 八字流年 × 紫微逐年');
-  ok(/太乙行年趨勢/.test(resoTxtY) && (resoTxtY.match(/同向率/g) || []).length === 3, '流年合盤三序列 3 組配對（含太乙行年）');
+  ok(!/八字流年 × 紫微逐年能量/.test(resoTxtY), '共振卡不再顯示 八字流年 × 紫微逐年（v0.9.35）');
+  ok(/八字＋紫微（五行主線・流年） × 太乙行年趨勢/.test(resoTxtY) && (resoTxtY.match(/同向率/g) || []).length === 1, '流年共振卡只顯示核心配對：八字＋紫微流年 × 太乙行年');
   // 流年合盤明細：同年並列八字流年讀數
   const hitY = [...doc.querySelectorAll('.ov-hit')].find(r => r.getAttribute('data-yr') === '2026');
   if (hitY) hitY.dispatchEvent(new win.MouseEvent('click', { bubbles: true }));
@@ -385,16 +367,16 @@ const countPoly = svg => (svg.match(/<polyline/g) || []).length;
     data: { series: [{ id: 'synth-x', label: '合成序列', color: '#888888', points: synth }] } }, '*');
   await sleep(300);
   svg = doc.getElementById('ovChart').innerHTML;
-  ok(countPoly(svg) === 4, '無層級合成序列於大限層顯示：4 條（實得 ' + countPoly(svg) + '）');
+  ok(countPoly(svg) === 5, '無層級合成序列於大限層顯示：4 條＋五行主線曲線＝5 條（實得 ' + countPoly(svg) + '）');
   doc.getElementById('ovZiweiMode').value = 'year';
   doc.getElementById('ovZiweiMode').onchange();
   await sleep(100);
-  ok(countPoly(doc.getElementById('ovChart').innerHTML) === 4, '無層級合成序列於流年層亦顯示：4 條');
+  ok(countPoly(doc.getElementById('ovChart').innerHTML) === 5, '無層級合成序列於流年層亦顯示：4 條＋八字＋紫微流年曲線＝5 條');
   doc.getElementById('ovZiweiMode').value = 'daen';
   doc.getElementById('ovZiweiMode').onchange();
   await sleep(100);
   const pairN = ((doc.getElementById('resoGrid').textContent).match(/同向率/g) || []).length;
-  ok(pairN === 6, '四序列兩兩共振：6 組配對（實得 ' + pairN + '）');
+  ok(pairN === 1, '共振卡只顯示核心配對 1 組（其餘配對仍在存檔 resonance.pairs）（實得 ' + pairN + '）');
 
   console.log('== 存檔／讀取／輸出圖檔 ==');
   // 存檔：攔 Blob 內容驗契約
@@ -411,7 +393,31 @@ const countPoly = svg => (svg.match(/<polyline/g) || []).length;
   ok(!!savedJson, '存檔產生 JSON');
   let saveObj = null;
   try { saveObj = JSON.parse(savedJson); } catch (e) {}
-  ok(!!saveObj && saveObj.schema === 'xiaoliu.hebing-save/v2', '契約 schema 升為 v2');
+  ok(!!saveObj && saveObj.schema === 'xiaoliu.hebing-save/v3', '契約 schema 升為 v3');
+  /* v0.9.30 五行主線：v3 新欄位齊備 */
+  const zd0 = saveObj && saveObj.curves && saveObj.curves.ziwei && saveObj.curves.ziwei.daen;
+  ok(Array.isArray(zd0) && zd0.length === 12 && zd0.every(d => d.zhiElement && Array.isArray(d.stars) && d.hua), '存檔 daen[] 含 zhiElement／stars／hua（v3）');
+  ok(!!(saveObj.curves.ziwei.palaces && saveObj.curves.ziwei.palaces.length === 12), '存檔 ziwei.palaces 十二宮（v3）');
+  ok(!!(saveObj.curves.bazi && saveObj.curves.bazi.dayMaster && saveObj.curves.bazi.dayMaster.gan === '己'), '存檔 bazi.dayMaster 為己（v3）');
+  const wei = zd0 && zd0.find(d => d.zhi === '未'), wu = zd0 && zd0.find(d => d.zhi === '午');
+  ok(!!(wei && wei.stars[0] && wei.stars[0].name === '天機' && wei.stars[0].daHua === '祿'), '未宮主星天機化祿');
+  ok(!!(wu && wu.stars[0] && wu.stars[0].name === '破軍' && wu.stars[0].daHua === '權'), '午宮主星破軍化權');
+  const wxRows = dom.window.document.querySelectorAll('#wuxingGrid .wx-block');
+  ok(wxRows.length === 12, '推象七層卡列出十二大限（實得 ' + wxRows.length + '）');
+  const wxTxt = dom.window.document.querySelector('#wuxingGrid').textContent;
+  ok(/巳為帝旺/.test(wxTxt) && /午為臨官（祿）/.test(wxTxt), '十二長生位：己在巳帝旺、午臨官（祿）');
+  ok(/辛卯運：辛金我生（食傷・喜・洩口）/.test(wxTxt), '大運兩面：辛金標為洩口');
+  ok(/出口在外（宜）/.test(wxTxt), '對宮規則：出口在外標籤出現');
+  ok(/題目 遷移＝外・出門/.test(wxTxt), '宮名題目層顯示');
+  ok(/求學（虛歲 13–22）：財＝零用錢/.test(wxTxt) && /初入職場（虛歲 23–32）：財＝薪水/.test(wxTxt), '年齡段貨幣換算顯示（求學／初入職場）');
+  ok(/被管得財/.test(dom.window.document.querySelector('#wuxingGrid').textContent), '五行主線判詞含已回測格「被管得財」');
+  /* v0.9.31 五行主線曲線 */
+  const wl = saveObj.resonance && saveObj.resonance.pairs && saveObj.resonance.pairs.filter(p => p.a === '八字＋紫微（五行主線）' || p.b === '八字＋紫微（五行主線）');
+  ok(!!(wl && wl.length >= 3), '共振 pairs 含五行主線曲線 × 其餘各線（實得 ' + (wl ? wl.length : 0) + '）');
+  const wxT = wl && wl.find(p => p.a === '限例太乙' || p.b === '限例太乙');
+  ok(!!(wxT && wxT.sameRate !== null), '五行主線曲線 × 限例太乙 有方向一致率（' + (wxT ? wxT.sameRate + '%' : '—') + '）');
+  ok(!!dom.window.document.querySelector('#ovChart polyline[stroke-dasharray="6 4"]'), '曲線圖畫出五行主線虛線');
+  ok(/五行主線曲線（假說級）/.test(dom.window.document.querySelector('#wuxingGrid').textContent), '五行主線卡顯示曲線說明與配對統計');
   ok(saveObj && saveObj.input.y === 1976 && saveObj.input.h === 11 && saveObj.input.gender === 'F', '存檔輸入欄位正確');
   ok(saveObj && saveObj.curves.bazi && saveObj.curves.bazi.pillars.length === 10, '存檔含八字曲線資料');
   ok(saveObj && saveObj.curves.bazi.specialScoring &&
@@ -441,19 +447,19 @@ const countPoly = svg => (svg.match(/<polyline/g) || []).length;
      '存檔含大限／流年上下各最多 6 組的太乙錨定快照');
   const savedDown = saveObj && saveObj.resonance && saveObj.resonance.syncAlerts
     ? saveObj.resonance.syncAlerts.daen.down.concat(saveObj.resonance.syncAlerts.year.down) : [];
-  ok(savedDown.every(b => ['A','B','C','conflict'].includes(b.grade) && Number.isFinite(b.confidence) && b.pairs && b.pairs.bazi && b.pairs.ziwei),
-     '存檔每個區段保留可信等級、可信度及八字／紫微證據');
+  ok(savedDown.every(b => ['A','C','conflict'].includes(b.grade) && Number.isFinite(b.confidence) && b.pairs && b.pairs.hebing && !b.pairs.bazi),
+     '存檔每個區段保留可信等級、可信度及八字＋紫微證據（無 B 級、無 bazi／ziwei 欄位）');
   const syncSave = saveObj && saveObj.resonance && saveObj.resonance.syncAlerts;
-  ok(syncSave && syncSave.version === 'taiyi-anchored-zones/v1' && syncSave.daenRule === 'taiyi-anchor-slope-primary' &&
+  ok(syncSave && syncSave.version === 'taiyi-anchored-zones/v2-hebing' && syncSave.companion && syncSave.daenRule === 'taiyi-anchor-slope-primary' &&
      syncSave.yearRule === 'taiyi-anchor-future-position-primary-3y-smooth' && Number.isFinite(syncSave.futureFromYear) &&
      syncSave.weights.daen.direction === 40 && syncSave.weights.year.position === 50 &&
      syncSave.displayOrder === 'chronological-after-grade-confidence-selection',
      '存檔標明太乙錨定版本、大限斜率主判、流年位置主判與權重');
-  ok(saveObj && saveObj.resonance.taiyiAnchoredZones && saveObj.resonance.taiyiAnchoredZones.version === 'taiyi-anchored-zones/v1',
+  ok(saveObj && saveObj.resonance.taiyiAnchoredZones && saveObj.resonance.taiyiAnchoredZones.version === 'taiyi-anchored-zones/v2-hebing',
      '存檔新增 taiyiAnchoredZones，並保留 syncAlerts 相容欄位');
   const savedDaen = syncSave ? syncSave.daen.up.concat(syncSave.daen.down) : [];
   ok(savedDaen.length > 0 && savedDaen.every(b => b.basis === 'slope' || b.basis === 'position-fallback') &&
-     savedDaen.every(b => Array.isArray(b.supporters) && b.pairs.bazi.directionRate >= 0 && b.pairs.ziwei.positionRate >= 0),
+     savedDaen.every(b => Array.isArray(b.supporters) && b.pairs.hebing.directionRate >= 0 && b.pairs.hebing.positionRate >= 0),
      '大限快照保留斜率／位置來源、支持線與方向／位置證據');
   ok(syncSave && syncSave.year.up.concat(syncSave.year.down).every(b => b.basis === 'position' && b.y0 >= syncSave.futureFromYear),
      '流年快照採 position basis，且只含今年起的未來區段');
@@ -463,25 +469,25 @@ const countPoly = svg => (svg.match(/<polyline/g) || []).length;
   const turnSave = saveObj && saveObj.resonance && saveObj.resonance.turningAnalysis;
   ok(turnSave && turnSave.version === 'turning-leadlag/v1' && turnSave.daen && turnSave.year,
      '存檔含大限／流年單盤轉折與領先／落後分析快照');
-  ok(turnSave && ['bazi','ziwei','close'].includes(turnSave.daen.comparison) &&
-     [turnSave.daen.pairs.bazi,turnSave.daen.pairs.ziwei,turnSave.year.pairs.bazi,turnSave.year.pairs.ziwei]
-       .every(p => Number.isFinite(p.fitScore) && p.fitScore >= 0 && p.fitScore <= 100 && Array.isArray(p.match.matches)),
+  ok(turnSave && turnSave.daen.comparison === 'hebing' &&
+     [turnSave.daen.pairs.hebing,turnSave.year.pairs.hebing]
+       .every(p => p && Number.isFinite(p.fitScore) && p.fitScore >= 0 && p.fitScore <= 100 && Array.isArray(p.match.matches)),
      '轉折快照保存比較結論、0–100 貼合度與逐筆配對');
   const extremaSave = saveObj && saveObj.resonance && saveObj.resonance.annualExtrema;
   const extremaSaveRows = extremaSave ? extremaSave.peaks.concat(extremaSave.troughs) : [];
-  ok(extremaSave && extremaSave.version === 'annual-extrema-echo/v5' && extremaSave.matchWindow === 1 && extremaSave.limit === 6 &&
+  ok(extremaSave && extremaSave.version === 'annual-extrema-echo/v6-hebing' && extremaSave.matchWindow === 1 && extremaSave.limit === 6 &&
      extremaSave.peaks.length <= 6 && extremaSave.troughs.length <= 6,
      '存檔含太乙最高／最低各最多 6 點及 ±1 年呼應規則');
   ok(extremaSaveRows.every(r => Number.isFinite(r.realAge) && Number.isFinite(r.virtualAge) &&
-     [r.bazi,r.ziwei].every(m => !m || Math.abs(m.lag) <= 1)),
-     '高低點存檔保留實歲／虛歲，所有八字／紫微命中差值均在 ±1 年內');
-  ok(extremaSaveRows.every(r => r.bazi || r.ziwei),
-     '高低點存檔只保留至少一法有合格呼應的太乙點');
+     [r.hebing].every(m => !m || Math.abs(m.lag) <= 1)),
+     '高低點存檔保留實歲／虛歲，所有八字＋紫微命中差值均在 ±1 年內');
+  ok(extremaSaveRows.every(r => r.hebing),
+     '高低點存檔只保留八字＋紫微有合格呼應的太乙點');
   ok([extremaSave.peaks,extremaSave.troughs].every(rows => rows.every((r,i) => i === 0 || rows[i-1].year <= r.year)),
      '高低點存檔依年份／年齡由小到大排列');
   ok(extremaSaveRows.every(r => Array.isArray(r.specialSignals) &&
      [r.bazi,r.ziwei].every(m => !m || Array.isArray(m.specialSignals))) &&
-     extremaSaveRows.some(r => r.specialSignals.length || (r.bazi&&r.bazi.specialSignals.length) || (r.ziwei&&r.ziwei.specialSignals.length)),
+     extremaSaveRows.some(r => r.specialSignals.length || (r.hebing&&r.hebing.specialSignals.length)),
      '高低點存檔保留太乙本點與八字／紫微配對年的特殊年份標籤');
   win.Blob = OrigBlob;
 
@@ -582,11 +588,11 @@ const countPoly = svg => (svg.match(/<polyline/g) || []).length;
   ok(!!pdfResult && fakePdf && fakePdf.pages === 5 && fakePdf.images === 5, 'PDF 報告固定輸出 5 頁（兩張曲線＋兩層太乙錨定區＋高低點呼應）');
   ok(fakePdf && /^hebing-curve-report-.*\.pdf$/.test(fakePdf.saved), 'PDF 報告以命主與日期命名並呼叫儲存');
   ok(fakePdf && /大限曲線、流年曲線、太乙錨定可信區段與太乙流年高低點 ±1 年呼應/.test(fakePdf.properties.subject) && !/轉折/.test(fakePdf.properties.subject), 'PDF metadata 明示兩張曲線、太乙錨定區與高低點呼應');
-  ok(pdfResult && pdfResult.annualExtrema && pdfResult.annualExtrema.version === 'annual-extrema-echo/v5' &&
+  ok(pdfResult && pdfResult.annualExtrema && pdfResult.annualExtrema.version === 'annual-extrema-echo/v6-hebing' &&
      fakeDrawnText.some(t=>/太乙流年高低點 ±1 年呼應/.test(t)) &&
      fakeDrawnText.some(t=>/太乙最高 6 個局部高點/.test(t)) && fakeDrawnText.some(t=>/太乙最低 6 個局部低點/.test(t)),
-     'PDF 第 5 頁使用既有 annual-extrema-echo/v5 快照並列出高低點雙欄');
-  ok(fakeDrawnText.some(t=>/^八字 /.test(t)) || fakeDrawnText.some(t=>/^紫微 /.test(t)), 'PDF 高低點頁只繪出實際呼應的八字／紫微資料行');
+     'PDF 第 5 頁使用 annual-extrema-echo/v6-hebing 快照並列出高低點雙欄');
+  ok(fakeDrawnText.some(t=>/^八字＋紫微 /.test(t)) && !fakeDrawnText.some(t=>/^紫微 /.test(t)), 'PDF 高低點頁只繪出八字＋紫微呼應資料行');
   ok(doc.getElementById('ovZiweiMode').value === modeBeforePdf && doc.getElementById('ovPdf').textContent === '輸出曲線 PDF 報告', 'PDF 擷取完成後還原原合盤模式與按鈕文字');
   const srcPdfHtml = require('fs').readFileSync(require('path').join(__dirname, 'index.html'), 'utf-8');
   ok(/太乙錨定可信區段｜/.test(srcPdfHtml) && /reportBandReading/.test(srcPdfHtml), 'PDF 原始碼包含太乙錨定區段逐組解讀內容');
@@ -612,19 +618,15 @@ const countPoly = svg => (svg.match(/<polyline/g) || []).length;
 
   console.log('== v0.9.28 可信度公式（純函式斷言）==');
   {
-    const conf = (grade,b,z) => {
-      if(grade==='conflict') return Math.min(b.opposition,z.opposition);
-      const bb=Math.max(0,b.score-b.opposition), zz=Math.max(0,z.score-z.opposition);
-      return grade==='A'?Math.min(bb,zz):Math.max(bb,zz);
-    };
+    /* v0.9.35 單一佐證線 */
+    const conf = (grade,h) => grade==='conflict'?h.opposition:Math.max(0,h.score-h.opposition);
     // 上列公式須與 index.html 的 anchorBandConfidence 完全一致；下方以實跑快照交叉驗證。
-    ok(conf('B',{score:72,opposition:10},{score:20,opposition:65})===62, 'B 級可信度＝支持減反向（72/10 vs 20/65 → 62，非 72）');
-    ok(conf('A',{score:80,opposition:10},{score:75,opposition:20})===55, 'A 級取兩法淨值較小者（→ 55）');
-    ok(conf('C',{score:40,opposition:50},{score:30,opposition:35})===0,  'C 級淨值為負時歸零，不得出現負可信度');
-    ok(conf('conflict',{score:10,opposition:60},{score:15,opposition:45})===45, '衝突區沿用原語意，取兩法反向較小者');
+    ok(conf('A',{score:80,opposition:10})===70, 'A 級可信度＝支持減反向（80/10 → 70）');
+    ok(conf('C',{score:40,opposition:50})===0,  'C 級淨值為負時歸零，不得出現負可信度');
+    ok(conf('conflict',{score:10,opposition:60})===60, '衝突區可信度＝反向分');
     const bandsAll = [syncSave.daen.up,syncSave.daen.down,syncSave.year.up,syncSave.year.down]
       .reduce((a,r)=>a.concat(r),[]);
-    ok(bandsAll.length>0 && bandsAll.every(b=>b.confidence===conf(b.grade,b.pairs.bazi,b.pairs.ziwei)),
+    ok(bandsAll.length>0 && bandsAll.every(b=>b.confidence===conf(b.grade,b.pairs.hebing)),
       '實跑快照每一區段的可信度皆等於新公式重算值');
     ok(bandsAll.every(b=>b.confidence>=0 && b.confidence<=100), '可信度恆落在 0–100');
   }
@@ -633,16 +635,14 @@ const countPoly = svg => (svg.match(/<polyline/g) || []).length;
   {
     const bandsAll = [syncSave.daen.up,syncSave.daen.down,syncSave.year.up,syncSave.year.down]
       .reduce((a,r)=>a.concat(r),[]);
-    ok(bandsAll.every(b=>b.pairs.bazi.reliability!==undefined &&
-        Number.isFinite(b.pairs.bazi.scoreRaw) && Number.isFinite(b.pairs.bazi.oppositionRaw)),
-      '每個區段保留八字可靠度係數與折扣前原始分數');
-    ok(bandsAll.every(b=>b.pairs.bazi.score===Math.round(b.pairs.bazi.scoreRaw*b.pairs.bazi.reliability) &&
-        b.pairs.bazi.opposition===Math.round(b.pairs.bazi.oppositionRaw*b.pairs.bazi.reliability)),
+    ok(bandsAll.every(b=>b.pairs.hebing.reliability!==undefined &&
+        Number.isFinite(b.pairs.hebing.scoreRaw) && Number.isFinite(b.pairs.hebing.oppositionRaw)),
+      '每個區段保留八字可靠度係數與折扣前原始分數（套在八字＋紫微線上）');
+    ok(bandsAll.every(b=>b.pairs.hebing.score===Math.round(b.pairs.hebing.scoreRaw*b.pairs.hebing.reliability) &&
+        b.pairs.hebing.opposition===Math.round(b.pairs.hebing.oppositionRaw*b.pairs.hebing.reliability)),
       '支持與反向同時套用可靠度係數，不只折扣支持');
-    ok(bandsAll.every(b=>b.pairs.ziwei.reliability===undefined),
-      '紫微不套用可靠度係數（不依賴喜用神）');
     const conf1976 = saveObj.curves.bazi.strength.confidence;
-    ok(conf1976==='明顯' && bandsAll.every(b=>b.pairs.bazi.reliability===1),
+    ok(conf1976==='明顯' && bandsAll.every(b=>b.pairs.hebing.reliability===1),
       '本盤旺衰判定為「明顯」且無疑似格備選，係數維持 1（回歸不變）');
   }
 
@@ -661,8 +661,8 @@ const countPoly = svg => (svg.match(/<polyline/g) || []).length;
     ok(o2.curves.bazi.strength.confidence==='臨界', '此盤旺衰判定確為「臨界」（折扣路徑的前提）');
     const b2 = [o2.resonance.syncAlerts.daen.up,o2.resonance.syncAlerts.daen.down,
                 o2.resonance.syncAlerts.year.up,o2.resonance.syncAlerts.year.down].reduce((a,r)=>a.concat(r),[]);
-    ok(b2.length>0 && b2.every(b=>b.pairs.bazi.reliability===0.6), '臨界盤的八字可靠度係數為 0.6');
-    ok(b2.some(b=>b.pairs.bazi.score<b.pairs.bazi.scoreRaw), '臨界盤實際發生折扣（折扣後分數低於原始分數）');
+    ok(b2.length>0 && b2.every(b=>b.pairs.hebing.reliability===0.6), '臨界盤的八字可靠度係數為 0.6');
+    ok(b2.some(b=>b.pairs.hebing.score<b.pairs.hebing.scoreRaw), '臨界盤實際發生折扣（折扣後分數低於原始分數）');
     ok(/八字可靠度 60%/.test(c2.getElementById('syncGrid').textContent), '畫面顯示「八字可靠度 60%」，折扣不隱形');
     d2.window.close();
   }
@@ -709,7 +709,16 @@ const countPoly = svg => (svg.match(/<polyline/g) || []).length;
     ok(/雙法財運共振/.test(doc.getElementById('wealthGrid').textContent) &&
        /甲午/.test(doc.getElementById('wealthGrid').textContent) &&
        /太陰化祿/.test(doc.getElementById('wealthGrid').textContent),
-      '財運訊號卡同時呈現雙法共振、八字與紫微三區');
+      '財運訊號卡摺疊區仍保留雙法共振、八字與紫微三區（舊口徑）');
+    /* v0.9.36 推象財路 */
+    const wt = saveObj.resonance.wealthTuixiang, wgTxt = doc.getElementById('wealthGrid').textContent;
+    ok(wt && wt.schema==='xiaoliu.wealth-tuixiang/v1' && wt.affectsCurve===false && wt.dayMaster.gan==='己', '存檔含推象財路快照（schema v1、不參與計分、日主己）');
+    ok(wt.daen.some(d=>d.y0===2018&&d.grade==='強'&&/被管得財/.test(d.reasons.join())) && wt.daen.some(d=>d.y0===2028&&/自己主導財/.test(d.reasons.join())) && !wt.daen.some(d=>d.y0===2008),
+      '推象財路：2018 未宮被管得財（強）、2028 午宮自己主導財；2008 申宮紫府不列');
+    ok(wt.annual.some(a=>a.year===2032&&a.grade==='強') && wt.annual.some(a=>a.year===2033&&/破軍化祿/.test(a.reasons.join())),
+      '推象財路流年：2032 壬子（干支皆財）強、2033 破軍化祿入大限命宮');
+    ok(/推象財路（日主 己土，財＝水）/.test(wgTxt) && /財路・大限/.test(wgTxt) && /財路・流年/.test(wgTxt) && /財＝資產與現金流/.test(wgTxt),
+      '財運卡以推象財路為主區，附年齡段貨幣');
     ok(!/財運/.test(doc.getElementById('syncGrid').textContent),
       '財運訊號自成一卡，未混入太乙錨定可信區段');
   }
@@ -753,16 +762,20 @@ const countPoly = svg => (svg.match(/<polyline/g) || []).length;
   for (let i = 0; i < 100 && !/svg/.test(doc.getElementById('ovChart').innerHTML); i++) await sleep(100);
   const svg2 = doc.getElementById('ovChart').innerHTML;
   ok(/八字：/.test(doc.getElementById('f-status').textContent), '狀態列標出八字端錯誤');
-  for (let i = 0; i < 50 && countPoly(doc.getElementById('ovChart').innerHTML) < 2; i++) await sleep(100);
+  for (let i = 0; i < 50 && !/限例太乙（十二宮歲段/.test(doc.getElementById('ovLegend').textContent); i++) await sleep(100);
+  ok(countPoly(doc.getElementById('ovChart').innerHTML) === 1, '八字壞掉＋預設勾選時只剩限例太乙一條（五行主線曲線需八字）（實得 ' + countPoly(doc.getElementById('ovChart').innerHTML) + '）');
+  [...doc.querySelectorAll('[data-method]')].forEach(c => { if (!c.checked) c.click(); });
+  await sleep(300);
   const svg2b = doc.getElementById('ovChart').innerHTML;
-  ok(countPoly(svg2b) === 2, '八字壞掉時紫微大限＋限例太乙仍繪出（實得 ' + countPoly(svg2b) + '）');
+  ok(countPoly(svg2b) === 2, '八字壞掉時全開仍只有紫微大限＋限例太乙兩條（實得 ' + countPoly(svg2b) + '）');
   ok(((doc.getElementById('resoGrid').textContent).match(/同向率/g) || []).length === 1, '八字壞掉時共振卡剩 1 組配對（紫微×限例）');
-  ok(win.getComputedStyle(doc.getElementById('syncCard')).display !== 'none' && /等待：八字大運/.test(doc.getElementById('syncGrid').textContent), '三線未齊時提醒卡不消失，明示缺少八字曲線');
+  ok(win.getComputedStyle(doc.getElementById('syncCard')).display !== 'none' && /等待：八字＋紫微大限曲線/.test(doc.getElementById('syncGrid').textContent), '兩線未齊時提醒卡不消失，明示缺少八字＋紫微曲線');
   ok(win.getComputedStyle(doc.getElementById('turnCard')).display !== 'none' && /三線資料尚未齊備/.test(doc.getElementById('turnGrid').textContent), '三線未齊時轉折卡不消失並顯示等待說明');
   ok(win.getComputedStyle(doc.getElementById('wealthCard')).display !== 'none' &&
      /紫微財運/.test(doc.getElementById('wealthGrid').textContent) &&
-     /八字尚未排盤/.test(doc.getElementById('wealthGrid').textContent),
-     '八字壞掉時財運卡不消失，紫微訊號照列並標明八字缺席');
+     /八字尚未排盤/.test(doc.getElementById('wealthGrid').textContent) &&
+     /需八字與紫微（v3 主星欄位）皆排盤完成/.test(doc.getElementById('wealthGrid').textContent),
+     '八字壞掉時財運卡不消失：推象財路標明缺件，舊口徑紫微訊號照列並標明八字缺席');
 
   console.log(fails === 0 ? '\nALL PASS' : '\nFAILED: ' + fails);
   process.exit(fails === 0 ? 0 : 1);
diff --git a/test-research-ui.js b/test-research-ui.js
index 57915ac..4192781 100644
--- a/test-research-ui.js
+++ b/test-research-ui.js
@@ -13,8 +13,12 @@ const pause=ms=>new Promise(r=>setTimeout(r,ms));
   for(let n=0;n<100;n++){await pause(100);if(['fr-bazi','fr-ziwei','fr-taiyi'].every(id=>$(id).contentDocument.readyState==='complete'))break;}
   for(const [id,v] of Object.entries({'f-name':'介面測試','f-year':'1990','f-month':'1','f-day':'15','f-hour':'6'}))$(id).value=v;
   $('f-go').click();
-  for(let n=0;n<200;n++){await pause(100);if(d.querySelectorAll('#ovChart polyline').length===3&&!$('f-save').disabled)break;}
-  assert.equal(d.querySelectorAll('#ovChart polyline').length,3);
+  for(let n=0;n<200;n++){await pause(100);if(d.querySelectorAll('#ovChart polyline').length===2&&!$('f-save').disabled&&/限例太乙（十二宮歲段/.test($('ovLegend').textContent))break;}
+  /* v0.9.34 預設：八字＋紫微（五行主線曲線）＋太乙 兩條；八字、紫微單線預設關閉 */
+  assert.equal(d.querySelectorAll('#ovChart polyline').length,2);
+  assert.deepEqual([...d.querySelectorAll('[data-method]')].map(c=>c.dataset.method+'='+c.checked),['bazi=false','ziwei=false','hebing=true','taiyi=true']);
+  [...d.querySelectorAll('[data-method]')].forEach(c=>{if(!c.checked)c.click();});
+  assert.equal(d.querySelectorAll('#ovChart polyline').length,4);
   assert.equal(d.querySelectorAll('.readout').length,3);
   for(const k of ['bazi','ziwei','taiyi'])assert.ok(d.querySelector('.readout.'+k).textContent.includes('分'));
   for(const id of ['fr-bazi','fr-ziwei']){
@@ -45,13 +49,15 @@ const pause=ms=>new Promise(r=>setTimeout(r,ms));
   $('viewFrom').value='2035';$('viewTo').value='2028';$('applyRange').click();
   assert.equal(d.querySelector('#ovChart svg').dataset.x0,'2028');
   const checks=[...d.querySelectorAll('[data-method]')];checks[0].click();
-  assert.equal(d.querySelectorAll('#ovChart polyline').length,2);
+  assert.equal(d.querySelectorAll('#ovChart polyline').length,3);
   assert.equal(snapshot(),initial);
   assert.equal(d.querySelectorAll('.readout').length,3);
   checks[1].click();checks[2].click();
   assert.equal(d.querySelectorAll('[data-method]:checked').length,1);
   assert.equal(d.querySelectorAll('#ovChart polyline').length,1);
-  checks[0].click();checks[1].click();
+  checks[3].click();
+  assert.equal(d.querySelectorAll('[data-method]:checked').length,1);
+  checks[0].click();checks[1].click();checks[2].click();
   $('viewBounds').click();$('viewMarks').click();
   assert.equal(snapshot(),initial);
   assert.ok(![...d.querySelectorAll('#ovChart text')].some(e=>e.textContent==='26.6歲'));
@@ -67,8 +73,9 @@ const pause=ms=>new Promise(r=>setTimeout(r,ms));
   assert.match($('ovTitle').textContent,/流年/);
   assert.match(d.querySelector('#ovChart svg').getAttribute('aria-label'),/流年/);
   assert.equal(snapshot(),initial);
-  assert.equal(d.querySelectorAll('.metric-grid').length,4);
-  assert.equal(d.querySelectorAll('.timing-grid').length,4);
+  /* v0.9.35：轉折卡每層只剩「太乙 × 八字＋紫微」一組 */
+  assert.equal(d.querySelectorAll('.metric-grid').length,2);
+  assert.equal(d.querySelectorAll('.timing-grid').length,2);
   // Verify PDF captures all curves/full range, then restores current viewing state.
   checks[0].click();
   const beforeSvg=$('ovChart').innerHTML,beforeYear=$('viewYear').value,images=[];
@@ -80,7 +87,7 @@ const pause=ms=>new Promise(r=>setTimeout(r,ms));
   w.jspdf={jsPDF:Pdf};
   const result=await $('ovPdf').onclick();assert.ok(result);
   assert.ok(images.length>=2);
-  for(const xml of images){assert.equal((xml.match(/<polyline/g)||[]).length,3);assert.ok(!xml.includes('id="ovSelection"'));const parsed=new w.DOMParser().parseFromString(xml.slice(xml.indexOf(',')+1),'image/svg+xml');assert.equal(parsed.getElementsByTagName('parsererror').length,0);}
+  for(const xml of images){assert.equal((xml.match(/<polyline/g)||[]).length,4);assert.ok(!xml.includes('id="ovSelection"'));const parsed=new w.DOMParser().parseFromString(xml.slice(xml.indexOf(',')+1),'image/svg+xml');assert.equal(parsed.getElementsByTagName('parsererror').length,0);}
   assert.equal($('ovChart').innerHTML,beforeSvg);assert.equal($('viewYear').value,beforeYear);
   assert.equal(snapshot(),initial);
   assert.deepEqual(errors,[]);
diff --git a/wuxing-mainline.js b/wuxing-mainline.js
new file mode 100644
index 0000000..7d4ec1f
--- /dev/null
+++ b/wuxing-mainline.js
@@ -0,0 +1,377 @@
+/* =============================================================================
+ * 五行主線  wuxing-mainline.js  (v0.9.36)
+ * 小六太乙 · 三盤合參
+ *
+ * 目的：把「八字命局（體質）× 紫微大限宮位（環境五行供給）」相乘，
+ *       為每一個紫微大限產出一條「五行生剋主線」＋判詞。
+ *
+ * 方法（Nakai 2026-09-08 五行基底法，三段大限回測三中）：
+ *   宮支五行 → 對日主的生剋關係 → 產出形態（做什麼）
+ *   主星五行 → 對日主的生剋關係 → 分配結果（成果歸誰）
+ *   大限四化落在主星上      → 方向（給／自主／名／收）
+ *   四化定形狀不定尺度；尺度由分數層（八字大運分、紫微能量分）與當期現實決定。
+ *
+ * 口徑固定：宮支＋主星，不取宮干、不取納音。雙五行主星以主五行判讀、次五行併列顯示。
+ * 純函式、零相依；瀏覽器（window.WuxingMainline）與 node 皆可用。
+ * 判詞表（VERDICT）是 Nakai 的口徑，改字只改表、不動邏輯。
+ * ============================================================================= */
+(function (global, factory) {
+  typeof module !== 'undefined' && module.exports ? (module.exports = factory())
+    : (global.WuxingMainline = factory());
+}(typeof globalThis !== 'undefined' ? globalThis : this, function () {
+
+  const GEN = { 木: '火', 火: '土', 土: '金', 金: '水', 水: '木' };   // 我生
+  const KE  = { 木: '土', 土: '水', 水: '火', 火: '金', 金: '木' };   // 我剋
+  const GAN_ELEMENT = { 甲: '木', 乙: '木', 丙: '火', 丁: '火', 戊: '土', 己: '土', 庚: '金', 辛: '金', 壬: '水', 癸: '水' };
+  const ZHI_ELEMENT = { 子: '水', 丑: '土', 寅: '木', 卯: '木', 辰: '土', 巳: '火', 午: '火', 未: '土', 申: '金', 酉: '金', 戌: '土', 亥: '水' };
+  const ZHI = ['子', '丑', '寅', '卯', '辰', '巳', '午', '未', '申', '酉', '戌', '亥'];
+  const STAGES = ['長生', '沐浴', '冠帶', '臨官', '帝旺', '衰', '病', '死', '墓', '絕', '胎', '養'];
+  const BIRTH_ZHI = { 甲: '亥', 乙: '午', 丙: '寅', 丁: '酉', 戊: '寅', 己: '酉', 庚: '巳', 辛: '子', 壬: '申', 癸: '卯' };
+  const YANG = { 甲: 1, 丙: 1, 戊: 1, 庚: 1, 壬: 1 };
+  // 日主在某地支的十二長生位（陽干順行、陰干逆行）；己土：酉長生、午臨官（祿）、巳帝旺
+  function stageOf(gan, zhi) {
+    const b = ZHI.indexOf(BIRTH_ZHI[gan]), z = ZHI.indexOf(zhi);
+    if (b < 0 || z < 0) return '';
+    const k = YANG[gan] ? ((z - b) % 12 + 12) % 12 : ((b - z) % 12 + 12) % 12;
+    return STAGES[k] + (STAGES[k] === '臨官' ? '（祿）' : '');
+  }
+
+  // 環境五行 el 相對日主 dm 的關係 → 十神類
+  function relation(el, dm) {
+    if (!el || !dm) return null;
+    if (el === dm) return '比劫';
+    if (GEN[dm] === el) return '食傷';   // 我生
+    if (KE[dm] === el) return '財';      // 我剋
+    if (KE[el] === dm) return '官殺';    // 剋我
+    if (GEN[el] === dm) return '印';     // 生我
+    return null;
+  }
+  const REL_VERB = { 比劫: '同我', 食傷: '我生', 財: '我剋', 官殺: '剋我', 印: '生我' };
+
+  /* ---------------- 判詞表（Nakai 口徑；可逐格修改） ---------------- */
+  const VERDICT = {
+    // 宮支 → 產出形態（環境讓你「做什麼」）
+    form: {
+      食傷: '洩：把體內多餘的力氣做成東西——作品、案子、技能',
+      官殺: '管：被框架、規矩、責任框著做出成果',
+      財:   '取：環境本身就是可取之物，主動去拿',
+      印:   '補：環境往裡補氣——資源、靠山、學習進來，動作偏靜',
+      比劫: '聚：環境送來同類，人多勢眾，力量疊加也互相分'
+    },
+    // 宮支對日主是喜／忌（由八字喜用神表決定）
+    formFav: {
+      喜: '（喜神地，這個形態對你是順的）',
+      忌: '（忌神地，會加重你原本過旺或過弱的那一行）',
+      中: '（中性地，形態成立、不加不減）'
+    },
+    // 主星 → 分配結果（成果「歸誰」）
+    share: {
+      財:   '主星是你的財，錢的形態就是這顆星',
+      比劫: '主星與你同類，成果由眾人共分',
+      官殺: '主星管你，成果由管你的人或制度定價',
+      食傷: '主星是你的產出，成果以作品、名聲的形式回來',
+      印:   '主星是你的靠山，成果以資源、庇護的形式回來'
+    },
+    // 大限四化落在主星 → 方向
+    dir: {
+      祿: '給——這條線給你，入袋',
+      權: '自主——這條線由你主導、握方向盤',
+      科: '名——這條線給名、給口碑，不直接給錢',
+      忌: '收——這條線收你的、綁你的；給形狀不給尺度',
+      無: '無明確方向——順著宮支形態走，尺度看分數層'
+    },
+    // 已回測的格子（主星關係 × 四化）— 覆蓋上面兩段的組合句
+    verified: {
+      '官殺|祿': '被管得財：管你的那條線帶糧來',
+      '比劫|無': '產出多、入袋少：作品是你的，錢是大家的',
+      '財|祿':   '穩定給：財慢慢來、留得住，尺度看當期現實',
+      '財|權':   '自己主導財：由你決定拆什麼、拿什麼，力道猛'
+    }
+  };
+
+  /* ---------------- 年齡段對照表（v0.9.33；象的貨幣隨年齡換，形狀不變）----------------
+   * 以大限中點虛歲取段。每段給四個貨幣：財（我剋）、管（剋我）、眾（比劫）、出口（往外走的形式）；可逐段修改。 */
+  const AGE_STAGES = [
+    { from: 1,  name: '童年',     cai: '家裡給的照顧與玩具', guan: '父母與長輩',         zhong: '玩伴與手足',     out: '寄居親戚家、外地生活' },
+    { from: 13, name: '求學',     cai: '零用錢、學業成果',   guan: '學校與家規',         zhong: '同學',           out: '外地求學、住校' },
+    { from: 23, name: '初入職場', cai: '薪水',               guan: '主管與公司制度',     zhong: '同事、同期競爭', out: '外派、換城市工作' },
+    { from: 33, name: '成家立業', cai: '收入與資產起步',     guan: '老闆、客戶、家庭責任', zhong: '合夥人、同行',   out: '創業、開拓外地市場' },
+    { from: 43, name: '中年主事', cai: '資產與現金流',       guan: '制度、責任、合約',   zhong: '同業分利者',     out: '跨領域、跨市場' },
+    { from: 53, name: '盛年轉向', cai: '資產配置與變現',     guan: '健康與時間',         zhong: '同輩競合',       out: '退居第二線、旅居' },
+    { from: 63, name: '退休初期', cai: '退休金、被動收入',   guan: '身體與家人安排',     zhong: '同輩往來',       out: '旅行、移居' },
+    { from: 73, name: '晚年',     cai: '照顧資源',           guan: '子女安排與醫療節奏', zhong: '老友',           out: '環境轉換' }
+  ];
+  function ageStageOf(start, end) {
+    const mid = (start + end) / 2;
+    let st = AGE_STAGES[0];
+    AGE_STAGES.forEach(a => { if (mid >= a.from) st = a; });
+    return st;
+  }
+  function favOf(el, favorable, unfavorable) {
+    if ((favorable || []).indexOf(el) >= 0) return '喜';
+    if ((unfavorable || []).indexOf(el) >= 0) return '忌';
+    return '中';
+  }
+
+  // 取日主：優先 bazi.dayMaster（v3 欄位），退而用 wealthSignals.luShen.dayStem（v2）
+  function dayMasterOf(B) {
+    if (!B) return null;
+    if (B.dayMaster && B.dayMaster.gan) {
+      return { gan: B.dayMaster.gan, element: B.dayMaster.element || GAN_ELEMENT[B.dayMaster.gan] };
+    }
+    const g = B.wealthSignals && B.wealthSignals.luShen && B.wealthSignals.luShen.dayStem;
+    return g ? { gan: g, element: GAN_ELEMENT[g] } : null;
+  }
+
+  // 主要方向：多顆主星時，祿＞權＞科＞忌；全無 → 無
+  function pickDir(stars) {
+    const order = ['祿', '權', '科', '忌'];
+    for (const h of order) if (stars.some(s => s.daHua === h)) return h;
+    return '無';
+  }
+
+  /**
+   * buildMainline(B, Z)
+   * B = curves.bazi（需 favorable/unfavorable；日主見 dayMasterOf）
+   * Z = curves.ziwei（需 daen[]，每列含 zhi/zhiElement/stars[]/hua；缺則回 []）
+   * 回傳每一大限一列。
+   */
+  function buildMainline(B, Z) {
+    const dm = dayMasterOf(B);
+    if (!dm || !Z || !Z.daen || !Z.daen.length) return [];
+    const fav = (B.favorable || []), unfav = (B.unfavorable || []);
+    const birth = Z.lunarBirthYear;
+    const luckByYear = {};
+    (B.annual || []).forEach(a => { if (a.luckGz) luckByYear[a.year] = a.luckGz; });
+    const favWord = el => favOf(el, fav, unfav);
+    // 對日主「有利」：喜神；或中性但成財／成洩口（食傷）
+    const helpful = (el, rel) => favWord(el) === '喜' ? true : favWord(el) === '忌' ? false : (rel === '財' || rel === '食傷');
+    return Z.daen.map(d => {
+      const zhiEl = d.zhiElement || ZHI_ELEMENT[d.zhi] || '';
+      if (!zhiEl || !d.stars) return null;
+      const zhiRel = relation(zhiEl, dm.element);
+      const zhiFav = favOf(zhiEl, fav, unfav);
+      const stars = (d.stars || []).map(s => ({
+        name: s.name, element: s.element, element2: s.element2 || '',
+        rel: relation(s.element, dm.element),
+        rel2: s.element2 ? relation(s.element2, dm.element) : null,
+        daHua: s.daHua || '', natalHua: s.natalHua || '', borrowed: !!s.borrowed
+      }));
+      const dir = pickDir(stars);
+      const rels = Array.from(new Set(stars.map(s => s.rel).filter(Boolean)));
+      const shareRel = rels.length === 1 ? rels[0] : null;
+      const key = shareRel ? shareRel + '|' + dir : null;
+      const verifiedText = key && VERDICT.verified[key] ? VERDICT.verified[key] : null;
+      const shareText = rels.map(r => VERDICT.share[r]).join('；');
+      /* --- 年齡段 --- */
+      const ageStage = ageStageOf(d.start, d.end);
+      const ageNote = ageStage.name + '（虛歲 ' + d.start + '–' + d.end + '）：財＝' + ageStage.cai + '、管＝' + ageStage.guan + '、眾＝' + ageStage.zhong + '、出口＝' + ageStage.out;
+      const verdict = {
+        form: VERDICT.form[zhiRel] + VERDICT.formFav[zhiFav],
+        share: shareText,
+        dir: VERDICT.dir[dir],
+        text: verifiedText || (shareText + '。' + VERDICT.dir[dir]),
+        verified: !!verifiedText,
+        ageNote: ageNote
+      };
+      /* --- 第 2 層補：日主在宮支的十二長生位 --- */
+      const stage = stageOf(dm.gan, d.zhi);
+      /* --- 第 5 層：大運兩面（對日主／對環境） --- */
+      const y0 = birth + d.start - 1, y1 = birth + d.end - 1;
+      const envEls = [zhiEl].concat(stars.map(s => s.element));
+      const luckMap = {};
+      for (let y = y0; y <= y1; y++) { const gz = luckByYear[y]; if (gz) luckMap[gz] = (luckMap[gz] || 0) + 1; }
+      const lucks = Object.keys(luckMap).map(gz => {
+        const g = gz.charAt(0), z = gz.charAt(1), ge = GAN_ELEMENT[g], ze = ZHI_ELEMENT[z];
+        const gr = relation(ge, dm.element), zr = relation(ze, dm.element);
+        let m = 0; envEls.forEach(E => { m += assist(ge, E) + 2 * assist(ze, E); }); m /= (envEls.length * 3);
+        const outlet = (gr === '食傷' && favWord(ge) !== '忌') || (zr === '食傷' && favWord(ze) !== '忌');
+        return { gz, years: luckMap[gz], gan: g, ganElement: ge, ganRel: gr, ganFav: favWord(ge),
+                 zhi: z, zhiElement: ze, zhiRel: zr, zhiFav: favWord(ze), outlet, m: Math.round(m * 100) / 100,
+                 text: gz + '運：' + g + ge + REL_VERB[gr] + '（' + gr + '・' + favWord(ge) + (gr === '食傷' ? '・洩口' : '') + '）／' +
+                       z + ze + REL_VERB[zr] + '（' + zr + '・' + favWord(ze) + '）；對環境' + (m > 0.1 ? '助' : m < -0.1 ? '拆' : '平') + ' m=' + (m > 0 ? '+' : '') + (Math.round(m * 100) / 100) };
+      });
+      /* --- 第 6 層：對宮主星 --- */
+      const opp = (d.frameStars && d.frameStars[0]) || null;
+      const oppStars = opp ? (opp.stars || []).map(s => ({ name: s.name, element: s.element, element2: s.element2 || '', rel: relation(s.element, dm.element), fav: favWord(s.element) })) : [];
+      const oppHelpful = oppStars.length ? oppStars.some(s => helpful(s.element, s.rel)) && !oppStars.some(s => s.fav === '忌') : false;
+      /* --- 空宮借對宮規則 → 標籤 --- */
+      let tag = '';
+      const borrowedPalace = !!d.starsBorrowed || (!!(d.stars && d.stars.length) && d.stars.every(x => x.borrowed));
+      if (borrowedPalace) {
+        const bh = stars.some(s => helpful(s.element, s.rel)) && !stars.some(s => favWord(s.element) === '忌');
+        tag = bh ? '機會在外（強制）' : '守本位・不外求';
+      } else if (oppHelpful) tag = '出口在外（宜）';
+      /* --- 第 7 層：宮名題目 --- */
+      const TOPIC = { 命宮: '自己', 兄弟: '手足同儕', 夫妻: '伴侶', 子女: '晚輩合夥', 財帛: '錢', 疾厄: '身', 遷移: '外・出門', 交友: '人脈眾人', 官祿: '事業', 田宅: '家與不動產', 福德: '心與福分', 父母: '長輩貴人' };
+      const topic = TOPIC[d.palaceName] || d.palaceName;
+      const line = zhiEl + '宮' + REL_VERB[zhiRel] + '（' + zhiRel + '・' + zhiFav + '）× ' + (borrowedPalace ? '借對宮 ' : '') +
+        stars.map(s => s.name + s.element + (s.element2 ? '(' + s.element2 + ')' : '') + REL_VERB[s.rel] + '（' + s.rel + (s.daHua ? '・' + s.daHua : '') + '）').join('＋');
+      return {
+        start: d.start, end: d.end, y0: birth + d.start - 1, y1: birth + d.end - 1,
+        ganZhi: d.ganZhi, palaceName: d.palaceName, zhi: d.zhi, zhiElement: zhiEl, zhiRel, zhiFav,
+        dayMaster: dm, stars, starsBorrowed: borrowedPalace, dir, stage, ageStage: ageStage.name, lucks, oppStars, oppZhi: opp ? opp.zhi : '', oppPalace: opp ? opp.palaceName : '', oppHelpful, tag, topic, huaText: d.huaText || '', line, verdict,
+        score: d.score, clsWord: d.clsWord || ''
+      };
+    }).filter(Boolean);
+  }
+
+  /* ---------------- 五行主線曲線（v0.9.31；假說級，供與太乙比對） ----------------
+   * 環境層：宮支喜忌 ±W.zhi、主星五行喜忌 ±W.star（每顆，借對宮 ×0.5，取平均）、大限四化落主星（祿權科忌）
+   * 大運層（B 案：大運對大限環境的生剋）：大運干（×1）支（×2）五行對「宮支＋主星」——
+   *   生環境 +1、同環境 +0.5、被環境洩（環境生大運）−0.5、剋環境 −1、被環境剋 −0.25 → 助拆指數 m∈[−1,1]
+   *   大運分 = m × W.luck × sign(環境分)：助喜環境為正、助忌環境為負、拆忌環境為正。逐年取 annual[].luckGz 對齊後平均。
+   * 權重皆為草案（待 Nakai 裁定）；此線不參與既有三線的任何計分，只多一條可被否定的曲線。 */
+  const W = { zhi: 10, star: 15, lu: 15, quan: 10, ke: 5, ji: -15, luck: 20, ganW: 1, zhiW: 2 };
+  function assist(D, E) {            // 大運五行 D 對環境五行 E
+    if (!D || !E) return 0;
+    if (GEN[D] === E) return 1;      // D 生 E
+    if (D === E) return 0.5;         // 同氣
+    if (GEN[E] === D) return -0.5;   // E 生 D＝洩環境
+    if (KE[D] === E) return -1;      // D 剋 E＝拆環境
+    if (KE[E] === D) return -0.25;   // E 剋 D＝大運無力
+    return 0;
+  }
+  /* 環境正負：環境分≠0 取其正負；環境分為 0（中性環境）時退而取主星關係：身強以食傷／財為正、印／比劫為負，身弱反之；再退宮支喜忌。 */
+  function envSignOf(B, r, envScore) {
+    if (envScore > 0) return 1; if (envScore < 0) return -1;
+    const strong = !(B.strength && /弱/.test(B.strength.level || ''));
+    const rels = r.stars.map(x => x.rel).filter(Boolean);
+    let v = 0; rels.forEach(x => { if (x === '財' || x === '食傷' || x === '官殺') v += strong ? 1 : -1; else if (x === '印' || x === '比劫') v += strong ? -1 : 1; });
+    if (v) return v > 0 ? 1 : -1;
+    return r.zhiFav === '喜' ? 1 : r.zhiFav === '忌' ? -1 : 0;
+  }
+  function favSign(el, fav, unfav) { return (fav || []).indexOf(el) >= 0 ? 1 : ((unfav || []).indexOf(el) >= 0 ? -1 : 0); }
+  function scoreMainline(B, Z, rows) {
+    rows = rows || buildMainline(B, Z);
+    const fav = B.favorable || [], unfav = B.unfavorable || [];
+    const luckByYear = {};
+    (B.annual || []).forEach(a => { if (a.luckGz) luckByYear[a.year] = a.luckGz; });
+    return rows.map(r => {
+      const parts = [];
+      const zhiS = favSign(r.zhiElement, fav, unfav) * W.zhi; parts.push({ label: '宮支' + r.zhi + r.zhiElement + r.zhiFav, delta: zhiS });
+      let starS = 0, huaS = 0;
+      r.stars.forEach(s => {
+        const v = favSign(s.element, fav, unfav) * W.star * (s.borrowed ? 0.5 : 1) / r.stars.length;
+        starS += v; parts.push({ label: '主星' + s.name + s.element + (s.borrowed ? '(借)' : ''), delta: Math.round(v * 10) / 10 });
+        const h = s.daHua === '祿' ? W.lu : s.daHua === '權' ? W.quan : s.daHua === '科' ? W.ke : s.daHua === '忌' ? W.ji : 0;
+        if (h) { huaS += h; parts.push({ label: '大限' + s.name + '化' + s.daHua, delta: h }); }
+      });
+      const envScore = zhiS + starS + huaS;
+      const sign = envSignOf(B, r, envScore);
+      const envEls = [r.zhiElement].concat(r.stars.map(s => s.element));
+      let mSum = 0, n = 0; const luckSeen = {};
+      for (let y = r.y0; y <= r.y1; y++) {
+        const gz = luckByYear[y]; if (!gz) continue;
+        const dg = GAN_ELEMENT[gz.charAt(0)], dz = ZHI_ELEMENT[gz.charAt(1)];
+        let m = 0;
+        envEls.forEach(E => { m += W.ganW * assist(dg, E) + W.zhiW * assist(dz, E); });
+        m /= (envEls.length * (W.ganW + W.zhiW));
+        mSum += m; n++; luckSeen[gz] = (luckSeen[gz] || 0) + 1;
+      }
+      const mAvg = n ? mSum / n : 0;
+      const luckS = Math.round(mAvg * W.luck * sign * 10) / 10;
+      parts.push({ label: '大運' + Object.keys(luckSeen).join('/') + (sign >= 0 ? '助' : '拆') + '環境 m=' + (Math.round(mAvg * 100) / 100), delta: luckS });
+      const total = Math.round((envScore + luckS) * 10) / 10;
+      return { y0: r.y0, y1: r.y1, start: r.start, end: r.end, ganZhi: r.ganZhi, palaceName: r.palaceName, envSign: sign,
+               envScore: Math.round(envScore * 10) / 10, luckScore: luckS, m: Math.round(mAvg * 100) / 100, score: total, parts, luckGz: Object.keys(luckSeen) };
+    });
+  }
+
+  /* ---------------- 流年五行主線曲線（v0.9.35）----------------
+   * 逐年分 = 所在大限的環境分（宮支＋主星＋大限四化）
+   *        ＋ 大運層（同 scoreMainline，該年 luckGz）
+   *        ＋ 流年層：流年干支對「宮支＋主星」的助拆指數 × W.year × sign(環境分)
+   *        ＋ 流年干飛四化落在大限命宮主星（祿+W.yLu／權/科/忌）
+   * 流年干支取八字 annual[].gz；流年四化採與 ziwei-engine 相同的中州派十干表。 */
+  const SI_HUA = {
+    甲: { 廉貞: '祿', 破軍: '權', 武曲: '科', 太陽: '忌' }, 乙: { 天機: '祿', 天梁: '權', 紫微: '科', 太陰: '忌' },
+    丙: { 天同: '祿', 天機: '權', 文昌: '科', 廉貞: '忌' }, 丁: { 太陰: '祿', 天同: '權', 天機: '科', 巨門: '忌' },
+    戊: { 貪狼: '祿', 太陰: '權', 右弼: '科', 天機: '忌' }, 己: { 武曲: '祿', 貪狼: '權', 天梁: '科', 文曲: '忌' },
+    庚: { 太陽: '祿', 武曲: '權', 太陰: '科', 天同: '忌' }, 辛: { 巨門: '祿', 太陽: '權', 文曲: '科', 文昌: '忌' },
+    壬: { 天梁: '祿', 紫微: '權', 左輔: '科', 武曲: '忌' }, 癸: { 破軍: '祿', 巨門: '權', 太陰: '科', 貪狼: '忌' }
+  };
+  W.year = 15; W.yLu = 10; W.yQuan = 7; W.yKe = 3; W.yJi = -10;
+  function scoreAnnual(B, Z, rows, daScores) {
+    rows = rows || buildMainline(B, Z);
+    daScores = daScores || scoreMainline(B, Z, rows);
+    const fav = B.favorable || [], unfav = B.unfavorable || [];
+    const luckByYear = {}, gzByYear = {};
+    (B.annual || []).forEach(a => { if (a.luckGz) luckByYear[a.year] = a.luckGz; if (a.gz) gzByYear[a.year] = a.gz; });
+    const out = [];
+    rows.forEach((r, i) => {
+      const ds = daScores[i]; if (!ds) return;
+      const envEls = [r.zhiElement].concat(r.stars.map(s => s.element));
+      const sign = envSignOf(B, r, ds.envScore);
+      for (let y = r.y0; y <= r.y1; y++) {
+        const gz = gzByYear[y]; if (!gz) continue;               // 流年干支只取八字逐年範圍
+        const parts = [{ label: '大限環境', delta: ds.envScore }];
+        // 大運層（該年）
+        let luckS = 0; const lgz = luckByYear[y];
+        if (lgz) {
+          const dg = GAN_ELEMENT[lgz.charAt(0)], dz = ZHI_ELEMENT[lgz.charAt(1)];
+          let m = 0; envEls.forEach(E => { m += W.ganW * assist(dg, E) + W.zhiW * assist(dz, E); }); m /= (envEls.length * (W.ganW + W.zhiW));
+          luckS = Math.round(m * W.luck * sign * 10) / 10; parts.push({ label: '大運' + lgz + (sign >= 0 ? '助' : '拆') + '環境', delta: luckS });
+        }
+        // 流年層：干支對環境
+        const yg = gz.charAt(0), yz = gz.charAt(1), ye = GAN_ELEMENT[yg], yze = ZHI_ELEMENT[yz];
+        let my = 0; envEls.forEach(E => { my += W.ganW * assist(ye, E) + W.zhiW * assist(yze, E); }); my /= (envEls.length * (W.ganW + W.zhiW));
+        const yearS = Math.round(my * W.year * sign * 10) / 10; parts.push({ label: '流年' + gz + (sign >= 0 ? '助' : '拆') + '環境 m=' + (Math.round(my * 100) / 100), delta: yearS });
+        // 流年四化落大限命宮主星
+        let huaS = 0; const hm = SI_HUA[yg] || {};
+        r.stars.forEach(s => { const h = hm[s.name]; if (!h) return; const v = h === '祿' ? W.yLu : h === '權' ? W.yQuan : h === '科' ? W.yKe : W.yJi; huaS += v; parts.push({ label: '流年' + s.name + '化' + h, delta: v }); });
+        const total = Math.round((ds.envScore + luckS + yearS + huaS) * 10) / 10;
+        out.push({ year: y, gz, daGanZhi: r.ganZhi, palaceName: r.palaceName, score: total, envScore: ds.envScore, luckScore: luckS, yearScore: yearS, huaScore: huaS, parts });
+      }
+    });
+    return out.sort((a, b) => a.year - b.year);
+  }
+
+  /* ---------------- 財運訊號（v0.9.36；推象七層口徑）----------------
+   * 財＝日主所剋之五行。訊號不是「某顆星化祿」，而是「體質 × 環境 × 四化」推出來的財路：
+   *   大限層：主星為財（我剋）帶祿／權、官殺帶祿（被管得財）、食傷帶祿（作品變現）、宮支為財且喜、
+   *           對宮主星為財（財在外）、宮名財帛／田宅；負向：主星比劫（分財）、主星化忌（收）。
+   *   流年層：流年干／支對日主為財、流年祿落大限命宮主星（主星為財加倍）、流年忌落主星（減）。
+   * 每筆帶 reasons 與分數；強≥3、中 2、弱 1；≤0 不列。不參與任何曲線計分。 */
+  function wealthSignals(B, Z, rows) {
+    rows = rows || buildMainline(B, Z);
+    const fav = B.favorable || [], unfav = B.unfavorable || [];
+    const gzByYear = {}; (B.annual || []).forEach(a => { if (a.gz) gzByYear[a.year] = a.gz; });
+    const dm = rows.length ? rows[0].dayMaster : null; if (!dm) return { daen: [], annual: [] };
+    const grade = n => n >= 3 ? '強' : n === 2 ? '中' : '弱';
+    const daen = [], annual = [];
+    rows.forEach(r => {
+      let n = 0; const why = [];
+      r.stars.forEach(s => {
+        if (s.rel === '財') { if (s.daHua === '祿') { n += 3; why.push(s.name + '為財・大限化祿（財給你）'); } else if (s.daHua === '權') { n += 2; why.push(s.name + '為財・大限化權（自己主導財）'); } else if (s.daHua === '忌') { n -= 2; why.push(s.name + '為財・大限化忌（財被收）'); } else { n += 1; why.push(s.name + '為財'); } }
+        else if (s.rel === '官殺' && s.daHua === '祿') { n += 2; why.push(s.name + '官殺化祿（被管得財）'); }
+        else if (s.rel === '食傷' && s.daHua === '祿') { n += 2; why.push(s.name + '食傷化祿（作品變現）'); }
+        else if (s.rel === '比劫') { n -= 1; why.push(s.name + '比劫（成果共分）'); }
+        else if (s.daHua === '忌') { n -= 1; why.push(s.name + '大限化忌'); }
+      });
+      if (r.zhiRel === '財' && r.zhiFav !== '忌') { n += 1; why.push(r.zhi + '宮為財（環境本身可取）'); }
+      if (r.oppStars.some(x => x.rel === '財' && x.fav !== '忌')) { n += 1; why.push('對宮' + r.oppStars.filter(x => x.rel === '財').map(x => x.name).join('') + '為財（財在外）'); }
+      if (r.palaceName === '財帛' || r.palaceName === '田宅') { n += 1; why.push('題目在' + r.palaceName); }
+      const ageStage = ageStageOf(r.start, r.end);
+      if (n > 0) daen.push({ y0: r.y0, y1: r.y1, start: r.start, end: r.end, ganZhi: r.ganZhi, palaceName: r.palaceName, score: n, grade: grade(n), reasons: why, tag: r.tag, currency: ageStage.cai, ageStage: ageStage.name });
+      /* 流年層 */
+      for (let y = r.y0; y <= r.y1; y++) {
+        const gz = gzByYear[y]; if (!gz) continue;
+        let m = 0; const w2 = [];
+        const g = gz.charAt(0), z = gz.charAt(1), ge = GAN_ELEMENT[g], ze = ZHI_ELEMENT[z];
+        if (relation(ge, dm.element) === '財') { m += 1 + (favOf(ge, fav, unfav) === '喜' ? 1 : 0); w2.push('流年干' + g + ge + '為財' + (favOf(ge, fav, unfav) === '喜' ? '・喜' : '')); }
+        if (relation(ze, dm.element) === '財') { m += 1; w2.push('流年支' + z + ze + '為財'); }
+        const hm = SI_HUA[g] || {};
+        r.stars.forEach(s => { const h = hm[s.name]; if (!h) return;
+          if (h === '祿') { m += 2 + (s.rel === '財' ? 1 : 0); w2.push('流年' + s.name + '化祿入大限命宮' + (s.rel === '財' ? '（主星為財）' : '')); }
+          else if (h === '權' && s.rel === '財') { m += 1; w2.push('流年' + s.name + '化權（財星主導）'); }
+          else if (h === '忌') { m -= 2; w2.push('流年' + s.name + '化忌入大限命宮'); } });
+        if (n >= 2) { m += 1; w2.push('大限財路成立（' + grade(n) + '）'); }
+        if (m >= 2) annual.push({ year: y, gz, daGanZhi: r.ganZhi, palaceName: r.palaceName, score: m, grade: grade(m), reasons: w2, currency: ageStage.cai });
+      }
+    });
+    return { schema: 'xiaoliu.wealth-tuixiang/v1', method: 'bazi-body x ziwei-daen-environment x sihua', affectsCurve: false, dayMaster: dm, daen, annual };
+  }
+
+  return { buildMainline, scoreMainline, scoreAnnual, wealthSignals, relation, dayMasterOf, ageStageOf, AGE_STAGES, VERDICT, W, GAN_ELEMENT, ZHI_ELEMENT, version: '0.9.36' };
+}));
diff --git a/ziwei-engine.js b/ziwei-engine.js
index bd0a9bf..e8e1fb8 100644
--- a/ziwei-engine.js
+++ b/ziwei-engine.js
@@ -29,6 +29,15 @@
     '巳(09-11)', '午(11-13)', '未(13-15)', '申(15-17)', '酉(17-19)', '戌(19-21)', '亥(21-23)'];
   const PALACE_NAMES = ['命宮', '兄弟', '夫妻', '子女', '財帛', '疾厄',
     '遷移', '交友', '官祿', '田宅', '福德', '父母'];
+  // --- 五行表（v0.9.30 五行主線用）---
+  // 主星五行取「主／次」兩欄；Nakai 2026-09-08 裁定：廉貞木火、貪狼水木、巨門水、七殺金。
+  // 主五行暫依通行：廉貞火（次木）、貪狼木（次水）——若主次要對調，只改此表。
+  const ZHI_ELEMENT = { 子: '水', 丑: '土', 寅: '木', 卯: '木', 辰: '土', 巳: '火', 午: '火', 未: '土', 申: '金', 酉: '金', 戌: '土', 亥: '水' };
+  const GAN_ELEMENT = { 甲: '木', 乙: '木', 丙: '火', 丁: '火', 戊: '土', 己: '土', 庚: '金', 辛: '金', 壬: '水', 癸: '水' };
+  const STAR_ELEMENT = {
+    紫微: ['土'], 天機: ['木'], 太陽: ['火'], 武曲: ['金'], 天同: ['水'], 廉貞: ['火', '木'], 天府: ['土'],
+    太陰: ['水'], 貪狼: ['木', '水'], 巨門: ['水'], 天相: ['水'], 天梁: ['土'], 七殺: ['金'], 破軍: ['水']
+  };
 
   const NOTES = []; // 收集本次排盤採用的流派說明，回傳給 UI 顯示
 
@@ -404,7 +413,7 @@
     // --- 組裝 12 宮資料 ---
     const palaces = []; // 以地支索引 0..11 為 key 的陣列
     for (let z = 0; z < 12; z++) palaces[z] = {
-      zhiIndex: z, zhi: ZHI[z], gan: GAN[zhiGan[z]], ganIndex: zhiGan[z], ganZhi: GAN[zhiGan[z]] + ZHI[z],
+      zhiIndex: z, zhi: ZHI[z], zhiElement: ZHI_ELEMENT[ZHI[z]], gan: GAN[zhiGan[z]], ganIndex: zhiGan[z], ganZhi: GAN[zhiGan[z]] + ZHI[z],
       stars: [], palaceName: '', isMing: false, isShen: false,
       daXian: daXian[z] || null
     };
@@ -422,7 +431,7 @@
     for (let z = 0; z < 12; z++) {
       if (!daXian[z]) continue;
       daList.push({
-        zhiIndex: z, zhi: ZHI[z], ganIndex: zhiGan[z], gan: GAN[zhiGan[z]],
+        zhiIndex: z, zhi: ZHI[z], zhiElement: ZHI_ELEMENT[ZHI[z]], ganIndex: zhiGan[z], gan: GAN[zhiGan[z]],
         ganZhi: GAN[zhiGan[z]] + ZHI[z], palaceName: palaces[z].palaceName,
         start: daXian[z].start, end: daXian[z].end,
         hua: siHuaByGan(zhiGan[z]), huaText: huaTextOf(zhiGan[z])
@@ -438,11 +447,34 @@
       palaces[s.zhi].stars.push({
         name: name, type: s.type,
         brightness: s.type === '主' ? brightnessOf(name, s.zhi) : '',
-        hua: huaMap[name] || ''
+        hua: huaMap[name] || '',
+        element: (STAR_ELEMENT[name] || [])[0] || '',
+        element2: (STAR_ELEMENT[name] || [])[1] || ''
       });
     });
     palaces.forEach(p => p.stars.sort((a, b) => (ORDER[a.type] - ORDER[b.type])));
 
+    // --- 大限宮位主星與三方四正（v0.9.30 五行主線用；派星完成後補入 daList）---
+    function mainStarsOf(z, daHua) {
+      return palaces[z].stars.filter(s => s.type === '主').map(s => ({
+        name: s.name, element: s.element, element2: s.element2, brightness: s.brightness,
+        natalHua: s.hua, daHua: (daHua && daHua[s.name]) || ''
+      }));
+    }
+    daList.forEach(d => {
+      const z = d.zhiIndex;
+      d.stars = mainStarsOf(z, d.hua);
+      d.starsBorrowed = false;
+      if (!d.stars.length) { // 空宮借對宮主星（標 borrowed，判讀時可辨）
+        d.stars = mainStarsOf((z + 6) % 12, d.hua).map(s => Object.assign(s, { borrowed: true }));
+        d.starsBorrowed = true;
+      }
+      d.frame = [z, (z + 6) % 12, (z + 4) % 12, (z + 8) % 12].map(i => ZHI[i]);
+      d.frameStars = [(z + 6) % 12, (z + 4) % 12, (z + 8) % 12].map(i => ({
+        zhi: ZHI[i], zhiElement: ZHI_ELEMENT[ZHI[i]], palaceName: palaces[i].palaceName, stars: mainStarsOf(i, d.hua)
+      }));
+    });
+
     return {
       input: o,
       lunar: lunar,
@@ -476,6 +508,7 @@
     computeChart: computeChart,
     solarToLunar: solarToLunar, lunarToSolar: lunarToSolar, lunarMonthLen: lunarMonthLen, leapMonthOf: leapMonthOf,
     siHuaByGan: siHuaByGan, huaTextOf: huaTextOf, liuNian: liuNian,
-    GAN: GAN, ZHI: ZHI, ZODIAC: ZODIAC, HOUR_LABEL: HOUR_LABEL, PALACE_NAMES: PALACE_NAMES
+    GAN: GAN, ZHI: ZHI, ZODIAC: ZODIAC, HOUR_LABEL: HOUR_LABEL, PALACE_NAMES: PALACE_NAMES,
+    ZHI_ELEMENT: ZHI_ELEMENT, GAN_ELEMENT: GAN_ELEMENT, STAR_ELEMENT: STAR_ELEMENT
   };
 }));
diff --git a/ziwei.html b/ziwei.html
index 01c2e50..ab318dc 100644
--- a/ziwei.html
+++ b/ziwei.html
@@ -919,6 +919,15 @@ table.node tr.drill-row td{background:#fbf6ea;padding:9px 12px;}
     '巳(09-11)', '午(11-13)', '未(13-15)', '申(15-17)', '酉(17-19)', '戌(19-21)', '亥(21-23)'];
   const PALACE_NAMES = ['命宮', '兄弟', '夫妻', '子女', '財帛', '疾厄',
     '遷移', '交友', '官祿', '田宅', '福德', '父母'];
+  // --- 五行表（v0.9.30 五行主線用）---
+  // 主星五行取「主／次」兩欄；Nakai 2026-09-08 裁定：廉貞木火、貪狼水木、巨門水、七殺金。
+  // 主五行暫依通行：廉貞火（次木）、貪狼木（次水）——若主次要對調，只改此表。
+  const ZHI_ELEMENT = { 子: '水', 丑: '土', 寅: '木', 卯: '木', 辰: '土', 巳: '火', 午: '火', 未: '土', 申: '金', 酉: '金', 戌: '土', 亥: '水' };
+  const GAN_ELEMENT = { 甲: '木', 乙: '木', 丙: '火', 丁: '火', 戊: '土', 己: '土', 庚: '金', 辛: '金', 壬: '水', 癸: '水' };
+  const STAR_ELEMENT = {
+    紫微: ['土'], 天機: ['木'], 太陽: ['火'], 武曲: ['金'], 天同: ['水'], 廉貞: ['火', '木'], 天府: ['土'],
+    太陰: ['水'], 貪狼: ['木', '水'], 巨門: ['水'], 天相: ['水'], 天梁: ['土'], 七殺: ['金'], 破軍: ['水']
+  };
 
   const NOTES = []; // 收集本次排盤採用的流派說明，回傳給 UI 顯示
 
@@ -1294,7 +1303,7 @@ table.node tr.drill-row td{background:#fbf6ea;padding:9px 12px;}
     // --- 組裝 12 宮資料 ---
     const palaces = []; // 以地支索引 0..11 為 key 的陣列
     for (let z = 0; z < 12; z++) palaces[z] = {
-      zhiIndex: z, zhi: ZHI[z], gan: GAN[zhiGan[z]], ganIndex: zhiGan[z], ganZhi: GAN[zhiGan[z]] + ZHI[z],
+      zhiIndex: z, zhi: ZHI[z], zhiElement: ZHI_ELEMENT[ZHI[z]], gan: GAN[zhiGan[z]], ganIndex: zhiGan[z], ganZhi: GAN[zhiGan[z]] + ZHI[z],
       stars: [], palaceName: '', isMing: false, isShen: false,
       daXian: daXian[z] || null
     };
@@ -1312,7 +1321,7 @@ table.node tr.drill-row td{background:#fbf6ea;padding:9px 12px;}
     for (let z = 0; z < 12; z++) {
       if (!daXian[z]) continue;
       daList.push({
-        zhiIndex: z, zhi: ZHI[z], ganIndex: zhiGan[z], gan: GAN[zhiGan[z]],
+        zhiIndex: z, zhi: ZHI[z], zhiElement: ZHI_ELEMENT[ZHI[z]], ganIndex: zhiGan[z], gan: GAN[zhiGan[z]],
         ganZhi: GAN[zhiGan[z]] + ZHI[z], palaceName: palaces[z].palaceName,
         start: daXian[z].start, end: daXian[z].end,
         hua: siHuaByGan(zhiGan[z]), huaText: huaTextOf(zhiGan[z])
@@ -1328,11 +1337,34 @@ table.node tr.drill-row td{background:#fbf6ea;padding:9px 12px;}
       palaces[s.zhi].stars.push({
         name: name, type: s.type,
         brightness: s.type === '主' ? brightnessOf(name, s.zhi) : '',
-        hua: huaMap[name] || ''
+        hua: huaMap[name] || '',
+        element: (STAR_ELEMENT[name] || [])[0] || '',
+        element2: (STAR_ELEMENT[name] || [])[1] || ''
       });
     });
     palaces.forEach(p => p.stars.sort((a, b) => (ORDER[a.type] - ORDER[b.type])));
 
+    // --- 大限宮位主星與三方四正（v0.9.30 五行主線用；派星完成後補入 daList）---
+    function mainStarsOf(z, daHua) {
+      return palaces[z].stars.filter(s => s.type === '主').map(s => ({
+        name: s.name, element: s.element, element2: s.element2, brightness: s.brightness,
+        natalHua: s.hua, daHua: (daHua && daHua[s.name]) || ''
+      }));
+    }
+    daList.forEach(d => {
+      const z = d.zhiIndex;
+      d.stars = mainStarsOf(z, d.hua);
+      d.starsBorrowed = false;
+      if (!d.stars.length) { // 空宮借對宮主星（標 borrowed，判讀時可辨）
+        d.stars = mainStarsOf((z + 6) % 12, d.hua).map(s => Object.assign(s, { borrowed: true }));
+        d.starsBorrowed = true;
+      }
+      d.frame = [z, (z + 6) % 12, (z + 4) % 12, (z + 8) % 12].map(i => ZHI[i]);
+      d.frameStars = [(z + 6) % 12, (z + 4) % 12, (z + 8) % 12].map(i => ({
+        zhi: ZHI[i], zhiElement: ZHI_ELEMENT[ZHI[i]], palaceName: palaces[i].palaceName, stars: mainStarsOf(i, d.hua)
+      }));
+    });
+
     return {
       input: o,
       lunar: lunar,
@@ -1366,7 +1398,8 @@ table.node tr.drill-row td{background:#fbf6ea;padding:9px 12px;}
     computeChart: computeChart,
     solarToLunar: solarToLunar, lunarToSolar: lunarToSolar, lunarMonthLen: lunarMonthLen, leapMonthOf: leapMonthOf,
     siHuaByGan: siHuaByGan, huaTextOf: huaTextOf, liuNian: liuNian,
-    GAN: GAN, ZHI: ZHI, ZODIAC: ZODIAC, HOUR_LABEL: HOUR_LABEL, PALACE_NAMES: PALACE_NAMES
+    GAN: GAN, ZHI: ZHI, ZODIAC: ZODIAC, HOUR_LABEL: HOUR_LABEL, PALACE_NAMES: PALACE_NAMES,
+    ZHI_ELEMENT: ZHI_ELEMENT, GAN_ELEMENT: GAN_ELEMENT, STAR_ELEMENT: STAR_ELEMENT
   };
 }));
 
@@ -2495,7 +2528,10 @@ enhanceAllSelects(); // 把原生 <select> 換成自製下拉（Artifact 沙箱
           return { start:r.d.start, end:r.d.end, ganZhi:r.ganZhi, palaceName:r.palaceName,
                    score:r.score, label:r.label, jiTotal:r.jiTotal,
                    clsKey:cls.key, clsWord:cls.word, clsColor:cls.color,
-                   advice:DAEN_ADVICE[cls.key], isCur:r.isCur, isShen:r.isShen };
+                   advice:DAEN_ADVICE[cls.key], isCur:r.isCur, isShen:r.isShen,
+                   /* v0.9.30 五行主線用：宮支五行、大限命宮主星（含主／次五行、生年四化、大限四化）、大限干四化、三方四正主星 */
+                   zhi:r.d.zhi, zhiElement:r.d.zhiElement, gan:r.d.gan, hua:r.d.hua, huaText:r.d.huaText,
+                   stars:r.d.stars, starsBorrowed:!!r.d.starsBorrowed, frameStars:r.d.frameStars };
         });
         post({type:'ziwei-curve', ok:true, data:{
           lunarBirthYear: CHART.lunarBirthYear,
@@ -2504,6 +2540,9 @@ enhanceAllSelects(); // 把原生 <select> 換成自製下拉（Artifact 沙箱
           daList: CHART.daList.map(function(x){ return {start:x.start,end:x.end,ganZhi:x.ganZhi,palaceName:x.palaceName}; }),
           years: data.years.map(function(y){ return {age:y.age,year:y.year,score:y.score,label:y.label,liuGZ:y.liuGZ,daGZ:y.daGZ,daRange:y.daRange}; }),
           daen: daen,
+          /* v0.9.30：十二宮主星＋五行（供回測過去大限、技能查財帛／田宅） */
+          palaces: CHART.palaces.map(function(p){ return { zhi:p.zhi, zhiElement:p.zhiElement, gan:p.gan, palaceName:p.palaceName, isMing:p.isMing, isShen:p.isShen,
+            stars:p.stars.filter(function(s){return s.type==='主';}).map(function(s){ return {name:s.name, element:s.element, element2:s.element2, brightness:s.brightness, natalHua:s.hua}; }) }; }),
           wealthSignals: ziweiWealthSignals()
         }});
       } else post({type:'ziwei-curve', ok:false, error:'排盤未成功'});
