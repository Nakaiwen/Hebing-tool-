# 合盤工具｜私人研究介面 v1

基於上傳的 v0.9.29 製作。保留術語、原始分數、可靠度、可信度、等級、分項及所有分盤操作；沒有更動三法引擎、計分權重、百分比公式或歲數換算。

## 套用方式

將本資料夾的 `index.html` 與 `research-ui.css` 放回原專案根目錄。前者覆蓋同名檔案，後者為新增樣式檔；兩個檔案必須一起使用。其餘既有檔案已隨附，內容沿用原版。更新網站後若仍看到舊畫面，請重新整理並清除該頁快取。

## 本版調整

- 桌面工作區恢復原版最大寬度 980px、左右內距 14px，保留米白、墨綠、淡金配色；重疊曲線頁面的標題、操作、數據與圖表文字統一沿用奇門遁甲工具的楷體字型設定。
- 原始讀數依八字、紫微、太乙並列，放大原始分數，保留所有分項文字。
- 可信區段固定排列等級、年份／歲段、可靠度與可信度；可信度數值放大。
- 轉折分析以固定格位並列轉折吻合率、方向一致率、同期相關、最佳時間差相關；保留有效筆數、年數與時間差。
- 領先／同期／落後各自一欄，便於横向比較。
- 曲線新增全人生、今年前後十年、自訂起迄年。
- 八字、紫微、太乙可個別隱藏，至少保留一條；交界／干支及轉折文字可切換。
- 選定年份支援直接輸入、前一年、後一年、今年及點圖選年；選年超出目前視野時會移動圖表範圍。
- 以虛線標出選定年份，流年模式同步更新標題及圖表標籤。
- 曲線顯示偏好、逐筆轉折配對展開狀態記在目前瀏覽器；不寫入出生資料或改動既有存檔格式。
- 小螢幕使用單欄讀數及可橫向捲動的圖表，避免將整張曲線壓縮成小字。

## 範圍與匯出

時間範圍、曲線顯示切換只控制圖表呈現，所有共振、可信度、財運及轉折分析仍使用完整原始資料。三法原始讀數仍全部顯示。

PNG／SVG 沿用目前可見曲線與時間範圍。PDF 保留原有五頁完整報告：擷取全範圍三法圖表，完成後還原畫面範圍、線條開關、模式與選定年份。

## 驗證

- 原附 `test-parent.js` 回歸測試通過。
- 新增 `test-research-ui.js`：檢查三法讀數、數值格位、時間範圍、無效輸入、曲線隱藏、選年、流年標題、完整分析快照不變及 PDF 畫面還原。
- JavaScript 語法與父頁本地資源引用檢查通過。
- 本次未進行實機手機或瀏覽器視覺測試；PDF 測試使用模擬繪圖環境，未逐頁檢查實際輸出排版。

可在有 Node.js 的環境執行 `npm ci` 後，以 `node test-parent.js` 或 `node test-research-ui.js` 重跑檢查。


## 太乙工具更新（2026-09-06）

`taiyi/` 已換成使用者提供的 `my-taiyi-tool2(1).zip` 版本，完整保留其中的字型、圖片、曆法及太乙國運子工具。

新增區塊：
- 百六小限論月落宮：十二個節氣月落宮、星曜與本月提示。
- 太乙小限／小限月運：小限起宮、當歲所在宮及十二個節氣月月運。

獨立版原本沒有合盤橋接，本次接回原有 `merge-bridge v1.3`，讓父頁仍可傳入生辰並取得限例太乙、行年趨勢及特殊年份資料。上傳版其他程式邏輯未改動。父頁維持 980px 最大寬度。

若已套用先前私人介面版，只需以本包的 `taiyi/` 完整覆蓋原 `taiyi/` 資料夾（包含所有子資料夾）。不必再替換父頁或樣式檔。


## 分盤頁面精簡

- 八字盤、紫微盤移除可見的命主資料輸入區與重複排盤按鈕。命主由合盤首頁統一輸入；保留必要的隱藏欄位供原有橋接與計算讀取。
- 八字盤的「讀取檔案」與紫微盤的「存檔 JSON／讀檔 JSON」及格式說明已從介面移除；存讀檔统一使用合盤首頁。紫微基準年與盤別操作保留。
- 太乙盤移除頁首 Logo 與標題容器，保留資料設定及新增月運區塊。
- 已使用前版時，本次替換 `bazi.html`、`ziwei.html`、`taiyi/index.html` 即可。

本次精簡存讀檔列，只需替換 `bazi.html` 與 `ziwei.html`。


## 紫微流月／流日／流時自動帶入當下時間

每次透過盤別選單切入流月盤、流日盤、流時盤，都重新讀取裝置當地日期與時間，同步國曆年、月、日、時辰及基準年後更新命盤。午夜 00:00–00:59 與晚上 23:00–23:59 對應子時，01:00 起為丑時，依此類推；原本命理日界算法維持不變。

切入後可手動調整日期或時辰，手動操作不會被重新覆寫。再次切換到上述三種盤別時才重新套用當下時間。頁面初始查詢日期也改為系統日期。

本次只需更新 `ziwei.html`。新增 `test-ziwei-current-time.js` 驗證三種盤別、自製選單、跨年、閏日、時辰交界、手動輸入與盤面更新，測試通過。


## 重疊曲線資訊字體

重疊曲線頁面統一使用與奇門遁甲工具相同的字型順序：BiauKaiWeb、cwTeXKai、Noto Serif TC、Kaiti TC、serif，涵蓋標題、資訊卡、數據、選單、按鈕與 SVG 曲線文字。維持原有字級、980px 最大寬度與計算功能；字型可用性及替代字型沿用裝置環境。

已使用前版時，只需替換 `research-ui.css`，並重新整理頁面。


## 紫微十二宮資訊呈現優化

- 保留原四乘四命盤位置、中宮與 Hebing 首頁最大寬度。
- 每宮新增獨立表頭，醒目列出宮名、宮干支及命／身標記；大限歲段移到表頭下方，不再壓在星曜旁邊。
- 主星、輔星／煞曜、雜曜與流曜分區列示，增加各區實際星數。空主星宮標示「無主星」。
- 主星放大，亮度獨立排列；四化移到星名下方，可依宮寬換行，保留生年、大限、小限、流年、流月、流日、流時的完整標籤。
- 運限宮職標籤改為正常文件流中的底列，避免絕對定位壓住星曜內容。命宮及三方保留淡色區別，身宮以「身」標籤辨識。
- 十二宮盤面最低閱讀寬度為 840px；手機在命盤區左右滑動，其他頁面內容維持原寬度，星曜不再縮到過小字級。
- 中宮資訊放大並整理標籤與數值的對齊方式。
- 七種盤別與前版逐宮比對，確認宮位、星曜名稱／類型／亮度、全部四化、運限宮职、歲段及命身標記一致；排盤引擎不變。原流月／流日／流時自動帶入當下時間測試通過。
- 本次未進行實際瀏覽器視覺驗收。

本次只需替換 ziwei.html，樣式已包含在檔案內。


## 紫微宮內標題與歲段微調

移除主星區的「主星／顆數」標題列，保留主星、亮度、四化及無主星提示。大限歲段改放每宮最下方，位於運限宮職標籤之後。本次只需替換 ziwei.html。


## 四化移至星名後方、移除顆數

祿權科忌接在所屬主星／輔星的星名與亮度後方，多層標籤仍保留，空間不足時逐一換行。移除各星曜區的顆數標示；大限歲段維持在每宮最下方。本次只需替換 ziwei.html。


## 空主星區留白

移除「無主星」提示，沒有主星的區域留白。本次只需替換 ziwei.html。


## 四盤合參：奇門接進 Hebing（2026-09-07，v0.1）

新增檔案：`qimen/`（奇門遁甲工具整包，v0.3 引擎，含置閏表與 CLI）、`moment.js`（時盤合參父頁邏輯）、`test-moment.js`。
修改檔案（皆為附加式，不動原有邏輯與契約）：
- `index.html`：新增「奇門盤」分頁（iframe `qimen/index.html`）與「時盤合參」分頁；頁尾加載 `solar-lunar.js`、`qimen/qimen-engine.js`、`moment.js`。
- `bazi.html`：追加 moment-bridge v1（`type:'moment'`／`'moment-scan'` → `bazi-moment`／`bazi-moment-scan`），流年／流月／流日／流時四柱由 `calculateFourPillars` 取得，引動走原 `analyzeActivation`。
- `ziwei.html`：追加 moment-bridge v1（同名訊息 → `ziwei-moment`／`ziwei-moment-scan`），七層四化疊加走原 `stackCount()`，流時命宮＝流日命宮順數時辰、流時四化取時柱干（與流時盤同一程式路徑）。

時盤合參：輸入日期＋時辰，五盞燈——八字流日、八字流時、紫微流日、紫微流時、奇門時局——算一致度 N/5；區間掃描（≤93 天）列出達門檻的時辰與亮燈分布。

**v0.1 假設（待 Nakai 依實證定案）**：八字時刻分＝干喜 ±10、支喜 ±20、引動淨值 ×10；紫微燈＝分數>0 且非疊忌；奇門局分＝青龍返首／飛鳥跌穴 +3、玉女守門 +2、三詐／九遁 +2、奇得時 +1、五假 0；符伏吟／反吟 −2；值符或值使落宮帶門迫／擊刑／入墓各 −1。奇門夜子時依引擎慣例歸次日；紫微子時沿用本工具 00:00 日界（兩者日界不同，屬已知差異，已在 UI 註明）。博弈濾網不寫入本層，仍由 nakai-piancai-timing 技能疊加。

驗證：`node test-moment.js` 通過（Nakai 命例 2026-12-04 申時、12 月整月掃描、晚子時歸次日、超長區間攔截）；`test-parent.js`、`test-research-ui.js` 回歸通過。太乙時層（五福）未接，列為 v0.2。未做真瀏覽器視覺測試。
